package com.research.assistant.service.pdf.layout;

/** Canonical provenance. Only CURRENT_LAYOUT and CONFIRMED_FORMULA are clickable answer evidence. */
public enum EvidenceOrigin {
    CURRENT_LAYOUT,
    CONFIRMED_FORMULA
}

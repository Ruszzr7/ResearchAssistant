package com.research.assistant.service.workbench;

/** User-visible explanation of which context policy governed one turn. */
public enum WorkbenchContextMode {
    SELECTION,
    FOLLOW_UP,
    PAPER_QUERY,
    GENERAL_CHAT,
    ACTION_EXPLICIT,
    ACTION_REFERENTIAL
}

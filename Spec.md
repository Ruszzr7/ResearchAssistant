# Research Assistant - Spec

> 最后更新：2026-06-27

---

## 一、项目定位

**本地的AI驱动工科科研Agent**，帮助研究生和科研人员进行：
- 文献智能检索与发现
- 论文分析对比
- 研究领域Gap识别

**不仅仅是**文献管理工具（如Zotero），而是基于AI的智能化科研助手，文献检索与管理只是基础。

**设计原则** 每一步执行前，先让用户确认 Agent 的理解是否正确。Agent 不做黑箱操作，用户始终可控可修正。
---

## 二、目标用户与领域

| 维度 | 决策 |
|------|------|
| **主用户** | 工科研究生（博士、硕士） |
| **次用户** | 科研人员（博后、青年教师） |
| **聚焦领域** | CS、AI、EE等工科 |

> CS/AI 领域以 CCF 会议为主，EE 领域以中科院分区期刊为主。

---

## 三、技术栈

| 层 | 选型 | 说明 |
|------|------|------|
| **前端框架** | Vue 3 | JavaScript 渐进式框架，构建用户界面 |
| **UI 组件库** | Element Plus | Vue 3 生态的组件库，提供按钮/表单/表格等现成 UI 组件 |
| **状态管理** | Pinia | Vue 3 官方推荐，管理页面间共享数据 |
| **前端构建** | Vite | 前端打包工具，替代 Webpack，更快更轻 |
| **后端框架** | Spring Boot 3.x | Java 生态最主流的后端框架，快速构建 REST API |
| **Java 版本** | Java 17 | 长期支持版本（LTS），Spring Boot 3.x 最低要求 |
| **构建工具** | Maven | 管理 Java 项目依赖和打包，国内资料最多 |
| **数据库操作** | MyBatis Plus | Java 对象与 MySQL 之间的映射层，灵活可控 |
| **数据库** | MySQL 8.0 | 关系型数据库，存论文、用户、文件夹等结构化数据 |
| **缓存** | Redis | 内存数据库，加速高频查询、存储用户会话 |
| **大模型 API** | DeepSeek（用户自配） | 提供对话、分析等 AI 能力。用户可接入自己的 API Key |
| **PDF 文本提取** | Apache PDFBox（V2 引入） | Java 原生 PDF 解析库，MVP 先不做 |

> **设计原则**：MVP 阶段尽量不引入额外中间件，降低学习成本。

---

## 四、功能一：论文库（基础）

论文库是所有上层功能的数据基础。

### 论文数据

每篇论文存储以下信息：
- 基础元数据：标题、作者（标记一作/通信）、年份、来源（期刊/会议/预印本）、DOI、摘要、关键词
- 文件信息：本地 PDF 路径、获取方式（OA 下载 / 浏览器下载 / 手动上传）
- 用户组织：所属文件夹、自定义标签、阅读状态（未读 / 略读 / 精读 / 已归档）
- Agent 生成：建议标签、内容概要、论文之间的引用关系

### 文件夹

用户创建和管理，支持嵌套。文件夹用于组织论文，非必填（允许暂不分类）。

### 入库方式

| 方式 | 触发场景 | 元数据提取 | 文件夹选择 |
|------|---------|-----------|-----------|
| 检索入库 | 功能二搜索结果 → 勾选 → 下载 | 自动，来自搜索结果 | 用户选择 / Agent推荐 |
| 手动上传 | 输入DOI / 拖入PDF | 自动，DOI查询Crossref + LLM提取 | 用户选择 / Agent推荐 |

### 分类设计

- 文件夹由用户创建和管理，支持嵌套
- 入库时用户选择目标文件夹，或选「暂不分类」
- 「Agent 推荐文件夹」按钮：调用一次 LLM，根据论文内容和用户现有文件夹列表推荐归属或建议新建，用户可确认或忽略
- 标签由 Agent 自动建议，用户可确认、增删、修改

### Agent 异步处理（入库后自动执行，不阻塞用户操作）

1. PDF 文本提取（并行）
2. 元数据补全
3. 建议标签生成

入库后论文立即可见，处理中显示状态，处理完成后自动刷新。

### 库内查找

- 文件夹浏览 + 标签筛选 + 阅读状态筛选

---

## 五、功能二：智能文献检索与发现

### 设计原则

每一步执行前，先让用户确认 Agent 的理解是否正确。Agent 不做黑箱操作，用户始终可控可修正。

### 检索流程

**Step 1：用户输入**
用户用自然语言描述研究方向，或提供种子论文。

**Step 2：Agent 提炼并确认**
Agent 提取关键检索要素，展示给用户确认：
- 核心领域与子方向
- 中英文关键词
- 时间范围
- 论文类型（期刊 / 会议 / 预印本）

用户可修改任何要素，确认后进入搜索。

**Step 3：精选搜索**
Agent 执行搜索，返回 Top 10-15 篇最相关论文。每篇附带：标题、摘要要点、来源、年份、推荐理由。不做穷举，质量优先于数量。

**Step 4：论文选取与入库**
用户勾选真正相关的论文（预期 3-5 篇）。若无相关论文，可回溯到 Step 1 修改描述，或回到 Step 2 修改关键词。
用户勾选论文后，选择目标文件夹入库：
- 选择已有文件夹
- 新建文件夹
- 暂不分类
- 点「Agent 推荐」让 Agent 根据论文内容推荐归属

**Step 5：扩展策略确认**
Agent 对已入库的种子论文提出扩展策略，展示给用户确认：
- Cited by（后续引用者）
- Related articles（相关工作）
- 作者追踪（聚焦一作、二作、通信作者）
- 扩展深度（1-2 层）

**Step 6：扩展执行**
Agent 执行扩展 → 多路结果合并去重 → 按相关度排序 → 标注推荐理由 → 返回新论文列表 → 回到 Step 4.5 入库。

### PDF 获取

- arXiv 及其他开放获取论文 → Agent 直接下载 PDF 入库
- IEEE / Elsevier 等付费论文 → Agent 提示用户手动下载

---

## 六、功能三：论文理解与分析

### 设计原则

- Agent 根据论文实际内容自动判断领域，自适应选择分析框架，不做固定模板
- 每一步执行前，先让用户确认 Agent 的理解是否正确
- 分析报告生成后，用户可追问进入对话模式

### 整体入口

```
用户选中论文 → 选择分析模式：
  ├─「精读」→ Agent 自适应领域，生成结构化报告 → 可追问
  ├─「对比」→ 勾选额外论文 → Agent 生成对比表 → 可追问
  └─「推荐」→ 纯库内匹配，不调 API
```

### 3.1 单篇精读

**MVP（摘要级）**：Agent 基于论文的标题、摘要、关键词进行分析。

Agent 会根据论文内容自动判断所属领域（AI / CV / NLP / 通信 / 控制 / …），采用对应的分析框架。不同领域的分析侧重点示例：

| 领域 | 分析侧重 |
|------|------|
| AI / CV / NLP | 模型架构、训练策略、Benchmark 提升、Ablation |
| 通信 / 信号处理 | 系统模型、信道假设、理论推导、仿真设置 |
| 控制 / 机器人 | 控制策略、稳定性分析、实验平台 |

**报告后追问**：用户可在报告下方与 Agent 持续对话，深入特定章节或公式。

### 3.2 多篇对比阅读

用户从库中勾选 2-5 篇论文，Agent 自动识别论文间的共同维度，生成对比表。

**默认对比维度**（Agent 自适应调整）：
- 解决的问题
- 方法 / 模型
- 使用的数据集 / 实验设置
- 核心指标 / 效果
- 局限性

用户可自定义对比维度，如「只对比它们的损失函数设计」「只对比实验部分」。

### 3.3 相关推荐（库内）

纯本地计算，不调用 API。基于以下维度匹配库内相关论文：

| 匹配维度 | 规则 |
|------|------|
| 同作者 | 库里有哪些论文作者重叠 |
| 同关键词 | 库里有哪些论文关键词重叠 |
| 同文件夹 | 库里同一文件夹下还有哪些 |

返回推荐列表，标注每篇的匹配原因（如"同一作者""共享 3 个关键词"）。

---

## 七、功能四：研究 Gap 分析

### 设计原则

- Gap 分析起点为库内论文
- Agent 做摘要级评估，诚实标注搜索深度，不做绝对验证
- 用户是最终判断者：Agent 缩小范围，用户用领域知识确认

### 流程

```
用户从库中勾选一组论文（代表某个方向的现状）
          ↓
   Step 1：库内分析
   Agent 分析这组论文的研究脉络 → 找出未被覆盖的组合/方向
   → 输出「潜在 Gap 列表」
          ↓
   Step 2：外部验证
   Agent 针对每个潜在 Gap 生成检索关键词 → 检索外部文献
   → 每个 Gap 标注验证等级
```

### 分析维度

| 维度 | 问题 |
|------|------|
| **方法 Gap** | 主流方法/算法有什么已知缺陷？有没有被忽视的替代思路？ |
| **场景/数据 Gap** | 现有实验在什么条件下做的？什么场景没人研究过？ |
| **理论 Gap** | 哪些结论缺乏理论证明？ |
| **比较 Gap** | 几篇论文的方法有没有被公平比较过？ |
| **交叉 Gap** | 有没有其他领域的方法可以迁移过来？ |

### 验证等级标注

| 等级 | 含义 |
|:---:|------|
| 🔴 未发现相关研究 | 摘要级搜索未找到，潜在 Gap（非 OA 正文无法验证） |
| 🟡 有相关工作 | 摘要提到类似方向，需阅读正文确认是否真正重合 |
| 🟢 已被明确解决 | 摘要即声明了该成果，此方向已被覆盖 |

### 输出

Agent 输出 Gap 报告，每个 Gap 包含：
- 描述：Gap 是什么
- 依据：为什么库内这些论文的覆盖有空白
- 验证等级：🔴 / 🟡 / 🟢
- 外部搜索结果摘要（若 🟡 或 🟢）
- 建议：是否值得深入，可能的切入思路

报告生成后用户可追问进入对话模式。

> **局限性说明**：非 OA 论文正文无法检索，验证仅到摘要级别。

---

## 八、用户界面设计

### 全局布局

顶部导航，四个一级页面 + 设置入口。默认首页为论文库。

```
┌─────────────────────────────────────────────────────┐
│  Research Assistant       论文库 │ 检索 │ 分析 │ Gap │ ⚙ │
├─────────────────────────────────────────────────────┤
│                    主内容区（路由切换）                  │
└─────────────────────────────────────────────────────┘
```

**通用交互规范**：
- 所有 Agent 生成内容流式输出，非一次性展示
- Agent 关键决策步展示确认界面，用户可修改、确认或拒绝
- 请求进行中显示加载状态，完成后自动刷新受影响区域
- 首次启动论文库为空时，显示引导——「你的论文库是空的。导入第一篇论文，或开始一次 AI 检索。」→ 两个按钮分别跳转检索页、弹出导入对话框

---

### 页面 1：论文库

```
┌────────────┬──────────────────┬───────────────────┐
│  文件夹树    │    论文列表        │    详情面板         │
│            │  ┌────────────┐  │  (选中论文后展开)    │
│  📁 全部    │  │ 论文A       │  │                   │
│  📁 AI     │  │ 标题/年份    │  │  标题 / 作者 / 年    │
│    📁 NLP  │  │ 🏷️ 标签     │  │  来源 / DOI        │
│    📁 CV   │  │ 阅读状态     │  │  摘要              │
│  📁 通信    │  └────────────┘  │  关键词             │
│  📁 未分类  │  ┌────────────┐  │  标签 / 阅读状态      │
│            │  │ 论文B       │  │                   │
│  ── 筛选   │  │ ...        │  │  [精读] [对比]      │
│  标签：    │  └────────────┘  │  [推荐] [编辑] [删除] │
│  □ DL □优化│                  │                   │
│  状态：    │                  │                   │
│  □ 未读    │  分页 / 加载更多   │                   │
│  □ 精读    │                  │                   │
│  [+ 导入]  │                  │                   │
└────────────┴──────────────────┴───────────────────┘
```

- 点击文件夹 → 过滤论文列表；勾选标签/状态 → 追加筛选
- 点击论文卡片 → 右侧展开详情面板
- 点击 [精读]/[对比]/[推荐] → 携带当前论文跳转至分析页
- [+ 导入] → 弹出导入对话框：DOI 输入框 + PDF 拖拽区；入库前可选目标文件夹或点「Agent 推荐文件夹」

---

### 页面 2：文献检索

```
┌─────────────────────────────────────────────────────┐
│  检索对话区                                          │
│                                                     │
│  ┌─ Step 1: 用户输入 ───────────────────────────────┐ │
│  │  输入框: "我想研究大规模MIMO系统的资源分配问题..."    │ │
│  └─────────────────────────────────────────────────┘ │
│                                                     │
│  ┌─ Step 2: Agent 提炼确认 ────────────────────────┐ │
│  │  Agent: 我已理解：领域 / 关键词 / 时间 / 类型        │ │
│  │  [✓ 确认] [✏ 修改]                               │ │
│  └─────────────────────────────────────────────────┘ │
│                                                     │
│  ── 搜索结果 (Top 15) ──── [全选] [入库选中] ──────   │
│  ┌─────────────────────────────────────────────────┐ │
│  │ ☑ 论文1  标题 / 摘要要点 / 来源 / 推荐理由          │ │
│  │ ☐ 论文2  ...                                   │ │
│  └─────────────────────────────────────────────────┘ │
│                                                     │
│  ── 扩展结果 (Step 5-6 触发后) ────────────────────   │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 扩展策略: Cited by / Related / 作者追踪 / 深度 1-2  │ │
│  │ ☐ 新论文X  ...                                  │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

- 对话式逐步展开，当前步骤高亮
- 每个 Agent 输出均有确认/修改入口
- 检索结果和扩展结果统一勾选 → 入库对话框（选文件夹 / 暂不分类 / Agent 推荐）
- 无相关结果时可回溯修改关键词或研究描述

---

### 页面 3：论文分析

```
┌──────────────┬─────────────────────────────────────┐
│  论文选择区    │  分析主区域                            │
│              │  [精读]  [对比]  [推荐]                 │
│  当前论文:    │                                     │
│  · 论文A     │  ┌─────────────────────────────────┐ │
│              │  │  分析报告 / 对比表 / 推荐列表       │ │
│  对比论文:    │  │  (Agent 流式输出)                │ │
│  · 论文B  ✕  │  └─────────────────────────────────┘ │
│  · 论文C  ✕  │                                     │
│  [+添加]     │  ── 追问 ───────────────────────────── │
│              │  ┌─────────────────────────────────┐ │
│              │  │ 用户: 这个方法的复杂度是多少？       │ │
│              │  │ AI: 作者在定理3证明了...           │ │
│              │  │ [输入框]                    [发送] │ │
│              │  └─────────────────────────────────┘ │
└──────────────┴─────────────────────────────────────┘
```

- 从论文库跳转时自动带入已选论文；也可在页面内 [+添加] 从库中选择
- 三模式切换（精读/对比/推荐），当前高亮
- 精读：Agent 自适应领域，流式输出结构化分析报告
- 对比：勾选 2-5 篇，Agent 自动识别共同维度生成对比表，用户可自定义维度
- 推荐：纯本地匹配（同作者/同关键词/同文件夹），标注匹配原因
- 追问区始终在底部，历史可滚动

---

### 页面 4：Gap 分析

```
┌──────────────┬─────────────────────────────────────┐
│  论文选择区    │  Gap 报告区                           │
│              │  已选 N 篇论文  [开始 Gap 分析]          │
│  ☑ 论文 A    │                                     │
│  ☑ 论文 B    │  ┌─────────────────────────────────┐ │
│  ☑ 论文 C    │  │ 🔴 Gap 1：描述 / 依据 / 建议       │ │
│  ☐ 论文 D    │  │   外部验证：未发现相关研究          │ │
│  ☐ 论文 E    │  │                                 │ │
│              │  │ 🟡 Gap 2：描述 / 依据 / 建议       │ │
│  [全选/取消]  │  │   外部：1篇摘要涉及，需阅读确认      │ │
│              │  │                                 │ │
│              │  │ 🟢 Gap 3：...（已被解决）          │ │
│              │  └─────────────────────────────────┘ │
│              │  ── 追问 ── (同论文分析)               │
└──────────────┴─────────────────────────────────────┘
```

- 论文列表从库中拉取，支持搜索筛选
- 点击 [开始 Gap 分析] → Agent 库内分析 + 外部验证，流式输出
- 每个 Gap 标注验证等级 🔴🟡🟢
- 追问区在底部

---

## 九、API 接口

### 命名规范（RESTful）

| HTTP 方法 | 含义 | 示例 |
|:---:|------|------|
| `GET` | 读取 | `GET /papers` 获取论文列表 |
| `POST` | 创建 / 执行动作 | `POST /papers` 导入论文 |
| `PUT` | 更新 | `PUT /papers/5` 修改第 5 号论文 |
| `DELETE` | 删除 | `DELETE /papers/5` 删除第 5 号论文 |

- URL 中 `:id` 为路径变量占位符，实际为数字 ID（如 `/papers/5`）
- 简单查询用 `?key=value` 参数；复杂动作加动词后缀（如 `/import`、`/expand`）

### 文件夹

| 接口 | 说明 |
|------|------|
| `GET /folders` | 获取文件夹树结构 |
| `POST /folders` | 新建文件夹 |
| `PUT /folders/:id` | 重命名或移动文件夹 |
| `DELETE /folders/:id` | 删除文件夹 |

### 论文

| 接口 | 说明 |
|------|------|
| `GET /papers?folder=&tag=&status=&page=` | 按条件分页查询论文列表 |
| `GET /papers/:id` | 获取单篇论文详情 |
| `POST /papers` | 导入单篇论文（DOI 或 PDF） |
| `PUT /papers/:id` | 编辑论文信息、标签、阅读状态 |
| `DELETE /papers/:id` | 删除论文 |

### 文献检索

| 接口 | 说明 |
|------|------|
| `POST /search` | 提交检索描述，开始对话式检索 |
| `POST /search/expand` | 对已入库种子论文执行扩展检索 |
| `GET /search/:sessionId/results` | 获取某次检索会话的结果列表 |
| `POST /search/:sessionId/import` | 将检索结果中勾选的论文批量入库 |

### 论文分析

| 接口 | 说明 |
|------|------|
| `POST /analysis/read` | 对指定论文执行精读分析（流式） |
| `POST /analysis/compare` | 对多篇论文执行对比分析（流式） |
| `GET /analysis/recommend?paperId=` | 库内匹配推荐（纯本地，不调 API） |
| `POST /analysis/chat` | 分析追问对话 |

### Gap 分析

| 接口 | 说明 |
|------|------|
| `POST /gap/analyze` | 执行 Gap 分析（流式） |
| `POST /gap/chat` | Gap 追问对话 |

---

## 待讨论

### Agent 核心智能能力
- [x] 功能一：论文库（基础）
- [x] 功能二：智能文献检索与发现
- [x] 功能三：论文理解与分析（单篇精读、多篇对比、库内推荐）
- [x] 功能四：研究 Gap 分析

### 其他待定项
- [x] 技术栈最终确定（MVP 不引入向量检索，PDF 解析用 PDFBox）
- [x] MVP 范围确定：功能一 + 功能二 + 功能三
- [x] 用户界面布局与导航结构

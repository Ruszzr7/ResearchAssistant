# 开发进度（精简版）

> 详细变更和历史讨论以 Git 为准；本文只保留阶段、交付和验证结果。

## 当前状态

- 2026-07-22，PDF 内容管线阶段 7（上下文、记忆与引用，已完成）：选区问答快照升级为 `paper-context-v2`，除论文 hash、解析器、问题和会话外，再用 canonical 选区的页码、类型、原文、block、token range 与坐标生成 SHA-256 指纹；任务重试只有在指纹一致时才复用快照，避免同一运行错误套用旧选区。检索扩展查询统一限制为 6000 字符并严格按当前问题、当前选区、最近对话、同版本证据化观察、同版本论文画像依次填充；模型上下文执行严格总长上限，同时为最长合法问题动态保留完整问题预算。模型输入新增去重的 PDF hash/解析器版本清单，以及每条 evidence 的 block、页码、坐标、阅读顺序和置信度；历史、画像和长期观察仍只用于理解与召回，不能替代本轮 evidence，冲突时当前 PDF 版本证据优先。上下文、模型、Evidence Gate、记忆和执行引擎定向 31 项通过。
- 2026-07-22，PDF 内容管线阶段 3（精确搜索，已完成）：全文索引改为直接消费 `PdfTextMap`，命中结果保存原始 `itemIndex + charOffset`，空白 text item 不再压缩编号；页面使用 CSS Custom Highlight Range 精确标出命中字符并据其矩形滚动，旧浏览器保留整 item 降级。索引显式区分 READY、NO_TEXT_LAYER、FAILED，失败页可重试且不再被静默固化为空页面；黄金用例锁定 IEEE 页眉前空白项、断词、连字和失败状态。
- 2026-07-22，PDF 内容管线阶段 2（统一文字与坐标契约，已完成）：新增页级 `PdfTextMap`，完整保留 PDF.js 原始 itemIndex（包括空白项），把 NFKC/断词规范化后的每个字符反向映射到原始 item 与字符偏移，并显式区分 READY、NO_TEXT_LAYER 和 FAILED；新增 viewport 与归一化页面矩形的双向纯函数，黄金用例要求 50%–200% 缩放下区域不变。搜索和选区后续统一消费该契约，禁止自行重新编号或隐式混用坐标空间。
- 2026-07-22，PDF 内容管线阶段 1（黄金样本与验收标准，已完成）：新增统一验收规范和前端确定性黄金契约，固定原始 PDF.js item 编号、字符反向映射、单栏选择、跨栏拒绝、无文字层状态及公式区域缩放不变性；后端继续复用既有 layout/evidence 黄金集，真实 PDF 仍只通过环境变量 manifest 寻址。后续搜索、选择、公式和 LLM 阶段必须逐项通过对应契约，不能再仅以结果数量或页面跳转作为验收。
- 2026-07-21，PDF 阅读器细节修正（已完成）：页面大小改为点击当前百分比后展开 `50 / 100 / 125 / 150 / 200%` 预设；缩放与标记色菜单统一传送到顶层浮层，五色色圈缩小并确保高于论文助手。新建高亮、下划线、选区笔记和选区批注后均退出编辑态，只有再次点击已有标注才显示拖柄或删除按钮；`COMMENT` 改为必须绑定当前文字选区，且批注列表和页面批注默认关闭，仅在打开列表时显示，旧 `anchorPoint` 数据仍可读取。PDF 搜索改用保留原文偏移的规范化投影，兼容 PDF.js 文本项拆分、换行断词、软字符和连字，搜索栏字号缩小并删除空状态提示。前端全量 84 项、后端全量 449 项（0 失败、0 错误，5 项可选真实样本跳过）及生产构建通过；真实论文页面验收确认五档缩放、顶层 14px 色圈、搜索短语命中 9 处、批注关闭时 0 个页面图标/打开后显示列表与锚点，临时验收批注已删除。
- 2026-07-20，仓库清理（已完成）：删除无生产调用方的旧文本预处理、旧论文分析 repair、AI 自动批注和手工异步建表代码，同时移除对应测试、废弃接口、孤立前端组件、已被 Flyway V12.1–V26 完整取代的 `schema.sql / schema-upgrade-*.sql` 以及两份被最终验收文档取代的阶段性 PDF 规格；数据库结构统一由 Flyway 管理。清除 `node_modules / dist / target`、浏览器测试输出、本地编辑器配置和陈旧日志/PID，释放约 271 MiB，可通过依赖安装与构建重新生成；`backend/data`、`backups`、`runtime` 未改动。后端干净构建 448 项（0 失败、0 错误，5 项可选真实样本跳过）、前端 83 项及生产构建通过；真实 MySQL 启动健康状态 `UP`、schema V26 最新，且不再运行手工建表初始化器。
- 2026-07-19，重设计阶段 8（联合验收、旧契约清理与文档收口，已完成）：删除已无生产调用方、仍按 12,000 字符截断整篇调用的旧 `PaperProcessingService`，只保留分块理解主路径；工作台请求和持久化 invocation 正式移除客户端 `conversationContext`，DTO 与旧 run JSON 通过忽略未知字段保持读取兼容，伪造历史不能再进入运行时。新增 `docs/redesign-acceptance.md`，把八阶段、13 项界面需求、5 项确认细节、单篇精读工作流、数据库职责和延期边界逐项映射到实现与验证；当前规格同步移除便签/手写圈注/独立 notes API 等旧描述。后端使用干净构建验证 455 项（0 失败、0 错误，5 项可选真实样本跳过），前端 83 项与生产构建通过；开发 MySQL 从 V25 实际升级到 V26，健康状态 `UP`。1280×720 真实论文浏览器验收确认工具栏、五色菜单、37 条 PDF 搜索结果、搜索/批注/助手宽度分配及横向滚动，所有业务请求 200、控制台 0 error；未启动全文理解或调用外部 API。
- 2026-07-19，重设计阶段 7（上下文编排、对话记忆与可追溯长期观察，已完成）：选区问答不再由前端拼接并上传历史正文，前端只提交稳定 `conversationId`；更换选区会建立新的服务端对话边界，同一选区的连续追问保持同一 ID，刷新后从研究档案关联 run 恢复最近 ID。后端新增 `paper-context-v1`，按“当前问题—本轮选区/检索证据—服务端对话历史—当前版本论文画像—当前版本长期观察”的优先级组装上下文，对选区、历史、画像和观察分别限额，并为实际模型上下文保留独立 1200–2000 字符预算。首次执行冻结上下文快照，任务重试复用同一快照；运行 trace 只暴露来源数量、预算、裁剪状态和优先级，不泄露原始 prompt。V26 新增服务端逐轮 `paper_conversation_turn` 和按 claim 去重的 `paper_memory_observation`：只有通过 Evidence Gate 且 citation 落在本轮当前 PDF evidence 中的 claim 才能晋升为长期观察；相同 claim 跨运行合并证据并增加确认次数，同一 run 重放保持幂等，不同或冲突 claim 并存等待本轮原文证据裁决。研究档案仍是用户可见历史，论文观察是逐篇、按 PDF/解析版本隔离的 Agent 记忆；暂不构造全库 Obsidian 式知识库或跨论文记忆。V26 已在隔离 MySQL 8 schema 验证字段、索引、写入和去重后清理；后端全量 459 项（0 失败，5 项可选样本跳过）、前端 83 项和生产构建通过。
- 2026-07-19，重设计阶段 6（异步分块理解与全局论文画像，已完成）：在 `paper-structure-v1` 事实层之上新增 `paper-understanding-v1` 语义层，按章节边界和字符上限生成稳定指纹分块，以最多 2 路并发执行受约束 JSON 摘要，再基于有效分块归约论文全局画像；所有论断引用必须落在当前分块允许的稳定 block ID 中，越界引用会被丢弃。V25 为 `paper_memory` 增加流水线版本、阶段、分块进度、token、起止时间和索引；每个分块完成后立即检查点保存，重试时按指纹复用成功结果，只补失败或失效分块。长模型调用移出数据库事务，失败状态区分 `PARTIAL / FAILED`，不伪装成功，也不阻塞用户先进行选区问答。论文助手新增轻量进度条、非阻塞加载态和失败重试；全局画像投影到旧分析/RAG 兼容层时不再次调用模型。V25 已在隔离 MySQL 8 schema 验证并清理；后端全量 453 项（0 失败，5 项可选样本跳过）、前端 81 项、生产构建及本地 PDF/助手分栏界面验收通过。
- 2026-07-19，重设计阶段 5（PDF 结构事实与版本化论文记忆，已完成）：沿用本地 PDFBox `PaperLayoutArtifact` 作为坐标、角色、阅读顺序与原文 block 的唯一事实源，不启用云解析，也不把 GROBID 设为部署依赖；新增确定性的 `paper-structure-v1` 构建器，生成元数据、章节层级、稳定 block 阅读顺序、公式/图表/表格元素索引、跨页续接关系、统计和质量问题。V24 新增 `paper_memory`，以 `paperId + PDF SHA-256 + layout parser version + memory schema version` 唯一标识一份记忆，结构 JSON 只保存稳定 block 引用而不复制全文，并预留分块摘要、全局画像、状态、错误与修订字段。现有全文分析在调用 LLM 前强制确保结构事实已建立，结构解析失败不会继续消耗模型。V24 已在隔离 MySQL 8 schema 验证并清理；定向 9 项、后端全量 442 项（0 失败，5 项可选样本跳过）、前端 80 项和生产构建通过。
- 2026-07-19，重设计阶段 4（论文精读工作台，已完成）：论文助手重排为顶部“论文精读 / 缺陷分析 / 论文对比”三个横向选项卡，当前只开放论文精读，另两项保留明确的后续入口；精读区按“内容选取 / 公式框选”滑动切换并直接控制 PDF 鼠标工具。文字选区在内容框中原文展示，必须点击“确认并固定”后才能提问；公式继续使用服务端裁剪、KaTeX 实时预览和可编辑 LaTeX，只有确认固定后的公式锚点才能进入问答。下方改为 GPT 式连续对话和固定输入区，保留翻译、研究会话归档、最近对话上下文、运行状态点和证据回链。旧全文分析、改进空间和跨文对比的后端能力未删除，但不再挤占当前精读界面。前端全量 80 项、后端全量 436 项（0 失败，5 项可选样本跳过）和生产构建通过。
- 2026-07-19，重设计阶段 3（笔记、批注与文字标记，已完成）：标注语义收敛为选区 `NOTE`（笔记）与页面锚点 `COMMENT`（批注），两者采用不同 SVG 图标、保留固定内容锚点与可独立拖动的位置，并用虚线连接；批注列表可跳转、完成和删除，完成状态持久化后统一显示绿色。高亮/下划线不再需要“调整”模式，创建或点击后直接显示首尾拖柄和右上角 ×，× 即删除。旧独立文本笔记 UI、API、Controller/Service/Mapper/Entity 及 `note / paper_note_link` 表由 V23 删除；按已确认的数据策略清空旧测试标注，不猜测迁移旧语义，写作助手改为直接聚合选区笔记。标注更新/删除增加论文归属校验，后端拒绝无选区锚点的笔记和无页面锚点的批注。V23 已在独立临时 MySQL schema 验证并清理测试 schema；前端全量 83 项、后端全量 436 项（0 失败，5 项可选样本跳过）和生产构建通过。
- 2026-07-19，重设计阶段 2（PDF 阅读器框架与搜索，已完成）：工具栏按“缩放—页码—文字标记—笔记—批注/批注列表—单一色号”严格居中，搜索、论文助手与关闭右对齐；移除显式选择、顶部公式入口、调整入口、映射百分比、并排五色圈和 RGB 取色器，默认交互恢复为文字选择，标记色默认为红色并通过单一按钮展开五色列表。新增纯本地 PDF.js 全文索引、左侧搜索栏、结果计数/上下跳转及文字层命中高亮；搜索栏只压缩 PDF，批注栏从右侧助手既有宽度配额中分配，窄屏仍保留 PDF 横向滚动。批注语义与完成/删除操作留待阶段 3，公式框选入口留待阶段 4。前端全量 82 项、后端全量 439 项（0 失败，5 项可选样本跳过）和生产构建通过。
- 2026-07-19，重设计阶段 1（需求与领域清理，已完成）：顶层“论文研究”统一更名为“论文分析”；阅读计划从导航、命令面板、路由、文库嵌入、首页统计以及后端 Controller/Service/Mapper/Entity/DTO 中完整移除，写作助手保持可用。V22 按外键顺序删除 `reading_plan_item / reading_plan`，历史 Flyway 文件保留以维持升级链；当前 schema 与 H2 测试 schema 同步删除旧表。运行时代码静态残链检查通过，后端全量 439 项（0 失败，5 项可选样本跳过）、前端 78 项和生产构建通过。
- 2026-07-18，整合路线 R5（证据驱动写作工作台，已完成）：写作项目由一次性生成页面升级为持久化 Claim–Evidence Matrix，V21 新增论点及支持/反驳/背景证据，并保存论文、页码、定位、原文、说明和可选研究会话；`NEEDS_EVIDENCE / SUPPORTED / CONFLICTING / MIXED` 由证据关系自动计算。服务层强制证据论文属于当前项目、关联会话包含该论文，仍被引用的论文不能直接移出。前端新增证据覆盖看板、论点/证据编辑及原页回链，写作项目通过 URL 恢复；草稿、大纲与相关工作均可持久化。后端全量 447 项（0 失败，5 项可选样本跳过）、前端 80 项和生产构建通过；真实论文 175 已完成“建项目—加论文—建论点—加证据—刷新恢复—第 3 页回链—返回项目”验收，临时数据已清理，浏览器无错误，后端迁移至 V21 后 `UP`。
- 2026-07-17，整合路线 R4（目标驱动阅读计划，已完成）：阅读计划由日期清单升级为“研究目标—完成标准—逐篇阅读问题—预期产出—研究会话—阅读结论”闭环；论文条目可创建或恢复研究档案并从 PDF 关闭后回到原计划，只有留下持久阅读产出才能标记完成。V20 为旧计划补齐目标，保留有内容的历史备注为产出，无产出的旧完成项诚实退回研究中。后端全量 441 项（5 项可选样本跳过）、前端 77 项和生产构建通过。
- 2026-07-17，整合路线 R3（研究档案，已完成）：新增持久化 `research_session / research_session_paper / research_message`，将工作台运行、选区问答、论文集合、页码、模式与语言统一归档，并自动接管既有未关联运行；顶层“任务中心”改为可搜索、归档、续读和证据回链的“研究档案”，运行态任务收敛到全局抽屉，完整任务操作页仍保留。刷新与跨路由返回可通过 session URL 恢复研究上下文。后端全量 440 项（5 项可选样本跳过）、前端 75 项和生产构建通过。
- 2026-07-17，PDF 工作台 P4-F（已完成实现与真实链路验收）：PDF 工具栏新增与文字层独立的“公式”矩形工具，前端只提交页码和左上角归一化区域，后端以原始 PDF 在 200 DPI 下重新渲染并安全裁剪。识别优先复用版面制品中的结构化 LaTeX，未命中时调用当前多模态模型；模型结果仅保存为可编辑 `CANDIDATE`，低置信度/不支持图片/无效输出安全降级为 `REGION`，用户确认后才进入 V18 `paper_formula_region`、生成版本绑定的 `SelectionAnchor(FORMULA)` 和稳定公式 evidence。右侧同步显示服务端裁剪图、来源、置信度、KaTeX/MathML 预览和原始 LaTeX，可重新识别、手工校正及保存；确认前禁止证据化问答，确认后复用连续选区对话与 Evidence Gate。WY 真实论文第 6 页式（21）已完成大型根号和 `Q^{-1}` 的框选识别、编号校正、确认入库、公式证据问答及原页回链；125% 缩放下区域坐标/宽高严格按 1.25 倍变化且继续贴合原式。后端全量 439 项、前端 70 项和生产构建通过，Flyway 已迁移至 V18，后端重启后 `UP`。
- 2026-07-17，PDF 工作台 P4-E 交互收口（已完成实现，待用户确认）：选区翻译改为右侧“当前选区”中的明确主操作，译文原位展示且保留原文；选区问答改为连续对话，选区变化会建立新会话，每轮仍保存独立可恢复 run。同一会话使用稳定 `conversationId` 和最近 8 条、最多 6000 字符的对话上下文；后端以精确选区为焦点，并用“当前问题 + 选区文本 + 最近追问”检索整篇论文相关证据，对话历史仅帮助理解代词、不能替代 evidence。选区默认总预算由 6000 提升到 10000 tokens，保留一次精简恢复，回答约束为最多 1200 汉字/6 条 claims。API 设置和快捷键弹窗追加到 `body`，层级固定高于 PDF 全屏工作区。后端全量 426 项通过（0 失败、5 项可选样本跳过），前端 64 项与生产构建通过；真实论文 175 完成 DeepL 选区翻译及两轮连续问答，两轮共享会话 ID，第二轮携带 1128 字符上下文，分别消耗 2719/5276 tokens、均一次成功、0 repair、`finishReason=STOP`；设置与快捷键弹窗层级均为 20020，高于 PDF 的 9999，浏览器无 error 日志，重启后端后 `UP`。
- 2026-07-17，PDF 工作台 P4-E（已完成实现，待用户确认）：新增独立 `TranslationService`、可替换 `TranslationProvider` 与默认 `DeepLTranslationProvider`，翻译不调用论文问答 LLM、不执行新 Workflow 或 Evidence Gate。DeepL Free/Pro 端点按密钥类型自动选择，支持最多 50 段批量请求、长文本按段落安全分块；LaTeX、IEEE 引用、DOI、URL、数值和缩写通过 XML `ignore_tags` 保护后还原。缓存键包含 provider、源/目标语种、内容 SHA-256 与术语表版本；未配置、无效凭据、额度、限流和上游错误均返回安全且可恢复的状态，不静默降级到 LLM。右侧精确选区新增按需翻译；历史或新生成的分析答案与 claims 可在“中文 / English”间切换，证据链接保持原样且不重新分析；新模型回答默认中文，并约束术语首次出现格式。真实凭据仅保存在 Windows 用户环境变量，状态接口不暴露密钥。后端全量 424 项通过（0 失败、5 项可选样本跳过），前端 62 项与生产构建通过；真实 DeepL 请求和二次缓存均成功，公式 `$R_k$`、引用 `[12]` 与数值 `1.25` 保持，浏览器已验证英文历史结果转中文且证据回链仍可用，后端重启后 `UP`。
- 2026-07-16，论文研究任务语义拆分（已完成并纳入稳定提交）：原顶层“研究 Gap”改为只接受当前论文的“论文改进空间”，后端新增独立 `PAPER_IMPROVEMENT` Workflow，从假设边界、方法、数据/场景、指标、实验设计与可复现性中生成有论文内证据、验证方式和研究切入点的改进建议，禁止上升为领域结论。“多篇对比”更名为“跨论文对比”；只有 3–8 篇对比完成后，结果区才提供第二阶段“领域研究空白”。该阶段必须提交源对比 `sourceRunId`，后端强制检查源运行已完成且论文集合完全一致，不能跳过对比或偷换论文。旧 `/gap` 兼容入口转到单篇改进空间，历史 `RESEARCH_GAP` run 仍可读取。前端 59 项、后端 415 项（5 项可选本机样本跳过）及生产构建通过。
- 2026-07-16，PDF 工作台 P4-D（已完成并纳入稳定提交）：移除 PDF 工具栏“AI 批注”、论文助手“批注建议”以及助手结果“添加内容/添加选中内容”，保留历史 run/后端接口兼容；助手结果只保留原生选择和复制。PDF 工具栏新增独立“批注”入口：用户精确选中文字后填写自己的批注，保存为带 `anchorQuads/anchorText/anchorKind=SELECTION/notePosition` 的 `NOTE`，复用可拖动 emoji、指向线、自动保存、编辑和删除；“便签”只负责页面任意位置放置。取消弹窗保留选区，保存成功后才清除，锚定 `NOTE` 在新建和编辑界面统一称为“批注”。运行恢复窗口由最近 5 条扩到后端既有的有界 20 条，再按当前功能筛选，避免历史运行挤掉全文分析。笔记 Controller 改为统一 `Result` 包络，修复后端实际写入成功但前端拦截器误报“请求失败”的契约不一致。前端全量 59 项、后端全量 415 项（5 项可选本机样本跳过）和生产构建通过；真实论文 175 已验证选取标题片段、填写、保存、刷新恢复、编辑和删除完整链路，临时批注已清理，最终页面无添加助手内容入口且无 error 日志。
- 2026-07-16，PDF 工作台 P4-C（已完成实现，待用户确认）：顶层入口合并为“论文研究”，`/workbench` 复用文库中已缓存的 PDF 工作区，在同一右侧面板切换选区问答、全文分析、批注建议、多篇对比和研究 Gap；`mode/paperId/paperIds` 可回链，旧 `/analysis`、`/gap` 保留参数后兼容跳转，重复的 `AnalysisView/GapView` 已移除，任务中心、阅读计划和写作助手保持独立。后端新增受规则路由约束的第五条 `RESEARCH_GAP` Workflow，要求 3–8 篇、逐篇本地 evidence、每篇 claim 引用覆盖、候选空白措辞和外部检索/实验验证步骤，禁止把“当前证据未覆盖”表述为“领域不存在”。前端全量 55 项、后端全量 407 项（5 项可选本机样本跳过）和生产构建通过；真实浏览器确认两个旧入口跳转、单论文 Gap 门槛、跨任务页面返回恢复，且无控制台错误。
- 2026-07-16，PDF 工作台 P0–P3 最终审计（已完成）：按阶段将 PDF 渲染/批注、受控选区、后端版面制品、SelectionAnchor、局部证据、四条固定 Workflow、Evidence Gate、可恢复 trace、自适应解析、区域降级、真实评测、质量面板和多篇对比逐项映射到实现与测试。后端在配置 WY 样本后全量 400/400 通过且 0 跳过，前端 42/42 通过，Vite 生产构建通过；在线后端 `UP`，确定性评测 7/7、WY 1/1、其余 3 个可选真实样本未配置，当前 artifact 平均质量 0.9561、claim 覆盖 34/34。旧阶段规格与验收文档已在最终重设计后移除，当前结论统一见 `docs/redesign-acceptance.md`，详细历史保留在 Git。
- 2026-07-16，PDF 工作台 P4 方案确认（待实施）：后续按 A–F 小步推进问答可靠性与精简 trace、可拖动且可恢复的阅读工作区、单篇分析/Gap/多篇对比统一入口、助手输出快捷批注、中英翻译和公式区域识别。翻译从问答 LLM 中拆出为独立服务，默认使用 DeepL API，按需调用并缓存；凭据仅从本机环境变量或加密设置读取，不写入仓库。
- 2026-07-16，PDF 工作台 P4-B（已完成）：PDF/助手改为目标约 60/40 的可拖动分栏，支持方向键微调、双击复位及 20%–65% 比例记忆；助手左移上限按 PDF.js 实际页面宽度动态保证 100% 页面完整显示，1280px 真实工作区中 918px 论文页保留 938px PDF 区，继续左拖 100px 仍被限制且无横向裁切。仅缓存文库路由，使 PDF 论文、页码、缩放、滚动、选区、助手输入和 run 视图在跨页面返回后原样恢复，显式关闭仍销毁现场。停用时释放滚动锁/窗口监听并暂停阅读计时，避免其他页面被 PDF 状态干扰或虚增阅读时长。真实论文 175 从第 3 页、125% 和自定义问题切到看板再返回后完整保持，关闭重开回到第 1 页、100% 和空输入；新增 6 项测试，前端全量 50 项与生产构建通过，浏览器无 error 日志。
- 2026-07-16，PDF 工作台 P4-A（已完成并提交 `4d66d10`）：选区局部证据收敛为直接选中块与前后各一个上下文块，前端不再为展示邻近段落重复请求证据；选区模型调用在同一总 token 预算内预留一次“仅直接证据”的空输出/截断恢复，并持久化失败 token、attempt count 和 provider finish reason。论文助手只显示精确选区，运行 trace 聚合为可悬停的四个横向状态点。真实论文 175 首页选区以 92% 锚点完成中文问答，使用 3 条证据、1781 tokens、约 23 秒、一次成功且 `finishReason=STOP`；后端 403 项、前端 44 项与生产构建通过，运行态确定性 7/7、WY 1/1，后端重启后 `UP`。
- 2026-07-16，PDF 工作台 P3-B2（已完成）：首页新增 PDF 工作台质量面板，可切换 7/30/90 天窗口，展示确定性/真实 PDF 评测、最新版面质量、内容精度与区域降级、解析回退收益、Workflow 终态成功率/repair/耗时/token、claim 证据覆盖及多篇逐论文覆盖；面板明确声明不展示正文、问题和路径。PDF 论文助手的多篇对比将当前论文固定为基准，要求至少再选一篇、总数最多 8 篇，提供受限比较维度；未满足条件时按钮禁用。对比结果按论文标题展示证据数、被引用 claim 数、页码与覆盖状态；当前论文证据原地跳转，其他论文证据会切换并重新挂载目标 PDF 后定位 bbox，不再把同页码错误套到当前论文，也不再以裸数字 ID 呈现。四种 Workflow 的最近运行按功能隔离，切换功能不会继续展示另一 Workflow 的旧结果。当前库只有一篇论文，真实浏览器已验证 1/8、无其他论文提示及禁用状态，全文分析入口保持可用；逐论文 2/2 覆盖矩阵由组件测试验证，后端双论文 Evidence Gate 由既有集成测试覆盖。前端 42 项与生产构建通过。P1–P3 功能开发完成，下一步为最终全链路审计与文档收口。
- 2026-07-16，PDF 工作台 P3-B1（已完成）：提交 `pdf-workbench-golden.json` 与可选真实 PDF manifest，新增不调用模型的可重复评测器，覆盖语义角色/reading order、双栏正文锚定、页眉/参考文献隔离、公式/表格安全区域降级和结构化 LaTeX 保留；真实样本只通过环境变量寻址，API 不返回路径或正文，并按文件指纹缓存 10 分钟。新增 `/api/workbench/metrics` 与 `/api/workbench/evaluation`，从每篇最新 artifact 和最近 1–365 天持久化 run 聚合版面质量、解析回退收益、内容精度、锚点类型/置信度、区域降级、claim 覆盖、Evidence Gate 失败、逐 Workflow 完成率/repair/token/耗时和多篇覆盖率，最多读取 5000 条并显式报告截断/坏数据。确定性 7/7、WY 真实 manifest 1/1 通过，后端全量 400 项通过（启用 WY 环境样本时 0 跳过）。下一增量为 P3-B2 产品指标面板与多论文对比呈现。
- 2026-07-16，PDF 工作台 P3-A（已完成）：新增 `adaptive-layout-v1` 快速优先解析策略，PDFBox 制品先经过文本密度、乱码、坐标、块置信度和 reading-order 六类确定性质量检查；只有低于 0.68 门槛时才执行显式启用的 GROBID/MinerU 外部命令，外部结果必须含页码/坐标且质量至少提升 0.04 才采用，超时、坏格式或无增益均安全保留 PDFBox。MinerU layout JSON、带坐标 GROBID TEI 与统一 layout JSON 已归一化为同一制品，XML 禁止 DTD/外部实体。Flyway V17 持久化主/最终解析器、回退原因与质量增益；公式/表格新增 `TEXT/STRUCTURED/REGION` 内容精度，缺少可信 LaTeX/单元格结构时只返回视觉区域和核对提示，不能进入全文文本上下文。WY 真实论文质量 0.956、91,430 字符、坐标有效率 100%，保持 PDFBox 快速路径；后端全量 393 项通过（4 项可选真实样例跳过），真实样例定向 12 项、前端 35 项和生产构建通过。下一阶段为 P3-B 真实评测集、指标面板与多篇对比完善。
- 2026-07-16，PDF 工作台 P2-C（已完成）：PDF 阅读器右侧新增可显隐论文助手，固定提供选区问答、全文分析、批注建议和多篇对比四个入口；前端只提交 intent/scope/论文/锚点，不接受任意 Skill。助手可恢复最近 5 次持久化运行，逐步展示状态、证据数、repair、tokens、耗时和安全错误；结构化 claim 引用与批注建议证据均可跳回 PDF 坐标。批注建议必须用户确认后才保存为锚定 NOTE，并以 `workbenchRunId` 防止刷新后重复应用；模型 Markdown 经转义后按受限语法渲染。新增启动即执行、每 30 秒巡检的 run/task 终态对账，修复旧任务已 `DEAD_LETTER` 而 run 仍 `RUNNING` 的状态。真实论文 175 浏览器验收完成：恢复 48 条证据全文分析；选区问答以 98% 锚点、1 条证据、5 条 claim、3272 tokens、0 repair 完成；批注建议确认后生成 3 个锚点框，刷新后仍显示且不可重复确认。后端全量 378 项通过（3 项可选真实样例跳过），前端 35 项与生产构建通过，后端重启后 `UP`。下一阶段为 P3-A 低置信度外部解析回退与公式/表格区域模式。
- 2026-07-16，PDF 工作台 P2-B（已完成）：选区问答、批注建议、全文分析和多篇对比四条固定 Workflow 已接入可恢复异步执行；每次运行按当前版面制品检索有界 evidence，模型只返回结构化 JSON，Evidence Gate 强制选区引用或逐篇覆盖，最多 repair 一次。门禁通过的结果先写恢复 checkpoint，再持久化全文分析报告及 evidence IDs、run ID、PDF hash 与 parser version；Flyway V16 已在真实 MySQL 完成迁移。真实论文 175 在线完成选区问答（5 条证据、9 条 claim、3345 tokens）、批注建议（5 条证据、2701 tokens）和全文分析（48 条证据、11 条 claim、9906 tokens），三者均 0 repair；开发库仅有一篇论文，因此多篇对比由双论文集成测试覆盖。后端全量 375 项通过（3 项可选真实样例跳过），重启后保持 `UP`。当前配置的 Kimi K2.7 Code 强制 thinking，使全文分析约耗时 108 秒；后续 UI 会明确运行状态，通用论文分析更适合配置非代码专用模型。下一增量为 P2-C PDF 右侧论文助手、trace 与证据跳转。
- 2026-07-16，PDF 工作台 P2-A（已完成）：Flyway V15 新增 `paper_workbench_run / paper_workbench_step`，持久化固定计划、artifact 版本、步骤状态、证据/repair/token/耗时与安全错误码；规则路由只生成四条编译期 Skill 白名单计划，限制 1–8 篇论文、最多 6 步、token 预算和一次 repair，scope 冲突或隐式扩权直接拒绝。`WorkbenchEvidenceGate` 以本次 evidence set 校验逐条 claim 引用与覆盖率，未知引用第一次进入 repair、第二次明确 reject。真实 MySQL 已迁移至 v15，论文 175 在线创建并回读 `SELECTION_QA / TEXT / 4 steps` 的版本绑定 trace；定向 15 项与后端全量 350 项通过（3 项可选真实样例跳过），后端重启后 `UP`。下一增量为 P2-B 四条固定 Workflow 的真实执行。
- 2026-07-16，PDF 工作台 P1-B3b（已完成）：PDF.js 受控选区现在会转换为左上角归一化 line boxes，调用后端生成版本绑定的 `SelectionAnchor` 并拉取有界局部证据；右侧证据面板展示锚点类型、置信度、页码和白名单证据，点击证据可跳页并按 bbox 临时高亮。窗口级 `pointerup` 兜底覆盖拖出文字层后松开的结束路径，证据侧栏只在拖选完成后挂载，避免中途改变 PDF 宽度；默认 CORS 同步覆盖启动脚本使用的 IPv4 loopback。WY 真实论文浏览器验收得到 `TEXT` 锚点、98% 置信度和 5 条局部证据，回链框可见；前端 30 项单测、生产构建和后端全量 335 项通过（3 项可选真实样例跳过）。下一增量为 P2-A 工作台 run/step 持久化、规则路由与 Evidence Gate。
- 2026-07-16，PDF 工作台 P1-B3a（已完成）：新增版本绑定的 `SelectionAnchor`、几何/文本联合块映射、`TEXT/FORMULA/TABLE` 精确锚点与 `REGION` 安全降级；局部检索会再次按 boxes/text 规范化客户端锚点，只返回 evidence 白名单角色，并为每条证据保留稳定 ID、页码、块 ID、坐标和解析版本。WY 论文在线验证命中 `p1-b0063`，`TEXT` 置信度 0.964，返回 5 条证据且无非法角色；旧解析器锚点返回 HTTP 409。定向 13 项和后端全量 332 项通过（3 项未配置真实样例时跳过），后端已重启并保持 `UP`。下一增量为 P1-B3b 前端选区接入与证据跳转。
- 2026-07-16，PDF 工作台 P1-B2（已完成）：在 B1 行级版面制品上增加确定性语义角色、同页同栏保守段落合并、章节路径和正文 evidence 白名单；Flyway V14 将制品按论文、PDF SHA-256 和 `pdfbox-layout-v1+semantic-v1` 版本化持久化，并提供按需重建/缓存读取 API。WY 真实论文由 2236 个视觉行收敛为 1079 个语义块，其中 841 个可作为证据；首次重建 1289 ms、二次缓存读取 82 ms，身份字段与内容严格一致。后端全量 319 项通过，时间精度回读修正的定向 3 项通过。
- 2026-07-16，PDF 工作台 P1-B1（已完成并提交）：新增后端 `PaperLayoutArtifact`、`DocumentBlock`、归一化坐标与角色契约；PDFBox 版面解析不再只返回无坐标全文，而是按字形坐标寻找跨多行稳定中缝，生成跨页连续、双栏左后右的行级阅读顺序，并将旋转页边出版信息保留为独立 `MARGIN_METADATA` 块。合成单栏/双栏/竖排样例通过；WY 真实论文生成 16 页、2236 个可回链块，首页和第 13 页顺序抽查通过。提交：`14ce7c8`。
- 2026-07-15，PDF 精确选取第二轮：选择索引按真实中缝拆分混合基线，标题/作者行保留居中通道，图片旁的缩进文字按栏中心连续归属；行内数学字形略跨中缝时随唯一相邻段落归栏，而真正夹在左右栏内容间的公式、页眉仍不作为文字选择桥接。公式的上标/下标/重音被 PDF.js 拆成多条片段时，选择排序改按共享基线再按横坐标，避免从公式起点向下一行拖拽时回吸前文。阅读器已锁住 PDF 背后的页面滚动，提供“当前页 / 总页数”与输入页码跳转，并采用当前视口优先、可取消的单队列虚拟渲染：卸载后的 canvas 不再被误标为完成。已覆盖多行标题、作者简介绕排、左右文/公式混排和行内公式回归；WY 测试 PDF 第 13 页真实交互复核通过，快速跳页和跨页滚动不会再出现空白 canvas。
- 最近一次交付：2026-07-13，Mission 13 旧库兼容修复与启动脚本修正。
- 2026-07-14，文库管理回归修复：统一会议来源/出版年份识别，取消年份 2025 默认值，恢复详情区元数据/标签/文件夹按需推荐应用，并让论文表格总宽度填满中栏、操作列固定、标题和来源单元格独立滚动。
- 2026-07-13，文库管理回归修复：PDF DOI 截断、文件夹推荐兜底和导入元数据入口说明。
- 2026-07-13，文库导入质量与速度优化：标题/摘要本地兜底提取、摘要长度上限、文件夹推荐快速匹配。
- 2026-07-13，文件夹推荐恢复层级语义：导入时传递摘要、展示完整文件夹路径；明确命中时优先选择最深层子文件夹，否则再使用 Agent；同时简化推荐结果提示。
- 2026-07-13，文库管理继续修复：补充 DOI 来源页/公开 PDF 提示、解码来源 HTML 实体、移除 AI 推荐标签入口，并将论文表格“操作”列固定在右侧。
- 2026-07-13，文库管理继续修复：更换待导入 PDF 时清空旧元数据，补充 Crossref 会议事件和 PDF 首页页眉中的会议/期刊来源识别。
- 2026-07-13，文库管理继续修复：为元数据识别增加保留首页页眉的 PDF 版面解析模式，修复会议 PDF 的 DOI、作者、来源和年份无法回填的问题。
- 2026-07-13，文库管理继续修复：修复 PDF.js 私有字段导致的空白预览，接入阅读进度回传，移除阅读状态旁的 AI 推荐入口，详情保存后同步论文列表，并修复入库工作流标量参数反序列化错误。
- 2026-07-13，文库管理继续修复：隐藏后台 PDF 提取文本，阅读进度改为页面稳定停留约 2 秒后计入，并将 AI 入库推荐与深度分析拆分，避免分析质量门禁失败阻断入库建议。
- 2026-07-13，文库管理继续修复：阅读状态修改立即同步到论文列表，修复双栏 PDF 摘要串栏，并移除阅读进度展示，仅保留阅读时间统计。
- 2026-07-13，文库管理继续修复：增强会议 PDF 的作者、来源和拆行 DOI 识别；移除入库推荐中的阅读状态和暗色 AI 提示，增加元数据应用、标签选择、文件夹确认及 AI 建议新建子文件夹，并同步论文库与文件夹树。
- 2026-07-14，文库管理补充修复：支持左侧页边距多行会议出处和关键词提取；详情元数据分析改为预览后确认应用；父文件夹已有子目录且无匹配子目录时建议新建同级子文件夹；修复 IPv6 本机开发地址的 CORS 403，并验证推荐状态与导入任务状态互不覆盖。
- 2026-07-14，PDF 阅读器批注修复：统一批注接口响应格式，允许未完成论文分析时基于 PDF 生成 AI 批注；补齐 AI 批注锚点坐标、文本选择、高亮、下划线、便签、圈注、删除和红黄蓝绿黑颜色工具。
- 2026-07-14，文库管理继续修复：按用户提供的两篇会议 PDF 验证左侧竖排出处、年份和完整 DOI；元数据解析合并内容流与坐标排序流，保留双栏摘要并恢复页边距出版信息；关键词支持连续换行。
- 2026-07-14，文库管理继续修复：文件夹推荐依据同级子文件夹的指标/场景/方法类型生成新子文件夹名称，并在导入前通过确认弹窗创建；详情入口统一为“推荐”，作者 JSON 改为姓名列表；重复 DOI/文件上传改为 409 覆盖确认。
- 2026-07-14，PDF 阅读器继续修复：文本选择按页面和行矩形分组，手动批注使用 viewport 归一化坐标，兼容已有 PDF 坐标批注，避免双栏/跨行选择错位。
- 2026-07-14，文库管理与 PDF 阅读器优化：文件夹推荐直接以已识别的标题、关键词、摘要和目录树进行一次结构化决策，区分主指标与 URLLC/低时延等约束，并可复用“和速率”等现有子目录；PDF 缩放菜单提供 50–300% 选项，移除新建自由画笔，增加可拖动自由/选区锚定便签。PDF 精确选取与页面内论文助手的后续路线已单独写入规格文档。
- 2026-07-14，文库管理与 PDF 阅读器继续修复：PDF 身份标识仅从首页识别，并以本地标题校验 Crossref/arXiv 返回，避免参考文献污染元数据；高亮、下划线和便签改为逐条自动保存，已有批注可编辑/删除，便签以可点击展开内容的 emoji 显示。
- 2026-07-14，阅读与文件夹体验优化：高亮/下划线支持选中后删除及左右手柄微调范围；摘要和关键词仅清理断行软连字符，不破坏学术术语中的正常连字符；编辑文件夹删除子目录后保持父目录展开。PDF 工作台规格收敛为可测试的 layout artifact、受限 Skill 编排与有界证据循环。
- 2026-07-14，PDF 精确选取基础修复：为直接使用的 PDF.js TextLayer 补齐 viewport 缩放变量和官方选择样式，修复文字层回退到 16px 后与 canvas 字形错位的问题；真实论文首页标题选区已验证落在页面内。
- 2026-07-14，PDF 精确选取继续修复：原生文本选择仅可从真实文字字形或其 4px 容错范围内发起；空白页边距和段首缩进改为普通光标并阻止大范围误选。
- 2026-07-14，PDF 工作台 P1 基础：阅读器在 PDF.js 文字层渲染后建立仅前端使用的 viewport 版面索引（文字片段、行、单双栏与候选段落），以单元测试覆盖分栏和阅读顺序；当前仍保留原生文字选择，尚未替换交互。
- 2026-07-14，PDF 工作台 P1 第一步：阅读器改为基于版面索引的同页同栏受控文本选择。仅从真实水平文字片段（2px 命中容错）发起，使用 DOM Range 的精确矩形绘制蓝色选区；端点落在空白、页边距竖排文字、全宽标题或另一栏时不扩张、不串栏，拖拽中保留最近一次有效同栏选区。跨栏/跨页连续段落与公式区域仍留待下一小步处理。
- 2026-07-15，PDF 精确选取回归修复：识别并利用多条基线重复出现的中央窄栏缝，避免 PDF.js 将左右栏合并成整行；真实双栏论文右栏多行选择及行内短选均已验证不再吸入左栏文字。
- 2026-07-15，启动脚本收敛：仅保留 Windows 的数据库、后端、前端和全部启动四个开发入口；以数据库端口、后端健康检查和前端 HTTP 可访问性判定就绪，`start-all.bat` 依次编排三项服务。
- 2026-07-15，仓库治理：删除零引用、与主 Compose 中 Qdrant profile 重复的独立 Compose 文件，并清理历史运行日志；保留用户数据、备份、依赖缓存和当前运行日志。
- 2026-07-22，PDF 内容管线重构第 4 阶段：文本选区同时保留 viewport 归一化几何、PDF.js 原始 itemIndex、TextLayer spanIndex、字符偏移和文档指纹；笔记、批注与文字标记共享该锚点，真实论文验证默认不进入编辑、点击后显示首尾手柄与删除按钮。
- 2026-07-22，PDF 内容管线重构第 5 阶段：公式框选复用统一 viewport 坐标契约，后端以当前版面产物的 PDF SHA-256 校验原文件后再按 200 DPI 裁剪；测试覆盖缩放、90° 旋转、边界限制与陈旧 PDF 拒绝。
- 2026-07-22，PDF 内容管线重构第 6 阶段：论文结构升级为 `paper-structure-v2`，新增逐页 block/content 索引与稳定元素 ID；结构缓存加载和生成均对照事实层校验 PDF hash、parser、页码、block 引用及跨页关系，拒绝可解析但失真的 JSON。
- 当前主链路：论文入库 → PDF/元数据处理 → AI 分析 → RAG 索引 → 阅读、检索、Gap 与写作。
- 后端测试使用 `test` profile + H2；前端生产构建和单元测试已通过。
- 仓库当前记录到 Mission 13；部署与数据安全能力已并入 Mission 13，不再重复创建历史任务记录。

## 阶段索引

| 阶段 | 关键交付 | 验证 |
|---|---|---|
| 1–8 基础建设 | Vue 3、Spring Boot、MySQL、论文库、PDF、AI、RAG、阅读写作、性能与代码卫生 | 后端测试与前端构建持续通过 |
| 9 AI Quality Gate | 结构化输出门禁、repair、Prompt/Token 预算、质量事件、Golden Eval | Maven clean test：226 项通过 |
| 10 可靠任务与可观测性 | RAG 真实成功/失败、任务状态机、超时、TTL、MySQL 可恢复调度、幂等、指标与脱敏日志 | 后端 247 项通过；前端构建通过 |
| 11 安全与 API 契约 | 敏感配置脱敏、生产加密 fail-closed、请求 ID、异常安全响应、DTO 校验、MockMvc 契约测试 | 后端 266 项通过；前端构建通过 |
| 12 RAG 质量与证据 | 证据身份、版本、内容 hash、候选校验、Gap/写作引用、RAG Golden Eval、外部调用可靠性 | 后端 282 项通过；前端构建通过 |
| 13 交付与数据安全 | Flyway、Docker Compose、Nginx/SSE、健康检查、备份恢复、RAG 一致性巡检、前端产品化、旧库修复 | 后端 286 项通过；前端单测 5 项；前端构建通过 |
| 14 文库阅读体验 | 文件夹推荐使用标题、关键词、摘要及内容指纹；PDF 采用“先选择、再应用批注”交互并加入缩放 | 文件夹 Skill 测试通过；前端单测 5 项；前端构建通过 |

## 关键架构决策

1. Spring Boot 负责业务、持久化和 Agent 编排，LangChain4j 1.15.1 负责模型、工具、记忆和 RAG 接入。
2. Skill Registry、Planner 和顺序 Workflow Engine 保持业务编排可测试；复杂并行或补偿需求出现后再评估专用引擎。
3. RAG 默认使用内存向量存储，MySQL 保存分片和证据元数据，Qdrant 为可选 provider 并保留内存降级。
4. MySQL 只由 Flyway 迁移目录维护结构，不保留手写 schema、启动时 DDL 或第二套升级脚本。
5. 任务状态、步骤、结果和可恢复上下文写入 MySQL；单机线程池负责执行，不引入 Redis。
6. Actuator/Micrometer、结构化日志和健康检查只暴露低基数、低敏信息，不记录密钥、提示词或论文正文。

## 当前待办

- 在真实部署环境执行多节点压力测试，并将指标接入告警平台。
- 继续按实际数据优化前端大 chunk 和复杂页面拆分。
- 如出现并行、分支、循环或补偿需求，再评估 Temporal/Camunda 等工作流引擎。
- 多模型 fallback 仍是可选增强，不影响当前单一有效配置。

## 文档维护

- 源码、Markdown、配置和脚本统一使用 UTF-8；PowerShell 读取中文文件显式使用 `-Encoding utf8`。
- 新功能只追加一行阶段记录；设计决策写入 `docs/knowledge.md`，不要复制完整 diff。
- 已完成事项不在 `docs/improvements.md` 重复维护。

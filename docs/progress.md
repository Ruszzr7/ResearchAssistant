# 开发进度

> 只记录当前状态、里程碑和验证基线；完整历史与 diff 由 Git 保存。

## 当前状态

- 论文研究页已切换到持久化通用 Agent：统一 Turn 输入、原生 Tool Calling、自然语言澄清、严格 cite、ActionTicket 回执和刷新恢复共用一条写链路。
- 论文理解由用户打开论文后手动启动；优先达到 `PROFILE_READY`，连续三次真实失败且本地来源可读时才开放明确的受限兜底。
- 对话与文档解析使用双角色模型配置；图片能力必须实测通过，原生 PDF 由连接测试自动探测，不显示 Token 或成本。
- 当前实现不使用 Embedding、向量数据库或独立 RAG；论文事实通过版本化本地来源的确定性搜索、读取、逐字引用和页面定位完成。
- 同一论文可建立多个上下文完全隔离的对话；只共享论文来源和画像，不共享消息、摘要、指代焦点或选区。
- 旧 Workbench run/step、大路由、分类器和重复会话写链路已删除；确定性选区/公式区域能力继续作为 PDF 输入工具保留。
- PDF.js 负责渲染，PDFium 负责选择和搜索，PDFBox 负责服务端版面事实。
- 论文事实由当前 PDF 版本的本轮 evidence 支撑；会话历史和论文画像只帮助理解与检索。
- PDF 页面操作覆盖高亮、下划线、笔记、批注和跳转；所有目标由服务端当前版本来源解析，成功以客户端真实回执为准。
- 项目简化已移除写作助手、旧独立论文对比、Gap 分析、自然语言计划、BibTeX/Obsidian/Zotero 导出、引用网络扩展和废弃的 RAG 诊断；论文研究页保留“添加对比文献”入口，等待后续统一实现。

## 主要里程碑

| 日期 | 交付 |
|---|---|
| 2026-07-13 前 | 完成文库、AI 质量门禁、RAG 版本、可恢复任务、Flyway、部署与安全基线 |
| 2026-07-16 | 建立 PDF 工作台、SelectionAnchor、版面 evidence、固定 Workflow 和 Evidence Gate |
| 2026-07-17 | 完成公式区域确认、研究档案和证据驱动写作 |
| 2026-07-19 | 删除阅读计划，统一笔记/批注，建立结构化论文记忆与服务端对话上下文 |
| 2026-07-22 | 完成 PDF 内容管线验收，并将浏览器文字事实迁移到 PDFium/WASM |
| 2026-07-24 | 修复数学密集选区、可读文本、视觉降级和公式边界问题 |
| 2026-07-25–26 | 优化全文理解和公式识别耗时，完成 KeepAlive 恢复、富文本渲染和多供应商接入 |
| 2026-07-29 | 一个研究会话对应一个独立对话；研究档案改为论文—对话两栏；精简首页看板 |
| 2026-07-30 | 合并论文助手模式，恢复全文自由问答，修复混合术语检索和公式位置证据 |
| 2026-08-02 | 复盘真实 SINR 问答，确认通用检索、引用绑定和目标框仍需结构性重构 |
| 2026-08-02 | 清理临时诊断产物与重复历史文档，将受版本控制的 Markdown 总量由约 155 KB 精简到约 31 KB |
| 2026-08-02 | 完成可定位证据的混合检索、结构化回答块、引用短句校验和无 Embedding 本地索引重构 |
| 2026-08-04 | 修复真实 SINR 验收中“已检索但门禁修复超预算”的通用问答故障；增加服务端引文规范化、精简修复上下文和门禁原因审计 |
| 2026-08-04 | 修复门禁修复超时后的状态机故障：取消无效的首轮预算预留、拒绝非空截断 JSON、限制仅首轮模型调用可安全重放，并确保步骤清空字段真正写入数据库；同时安全清除通用知识块上的误附论文引用 |
| 2026-08-04 | 统一回答内与证据列表的来源级编号；合并同一公式的文本/区域证据，扩展公式目标框，按原文短句定位正文，并在跳页后滚动到实际目标；增加技术标识引文覆盖门禁及修复 evidenceId 的唯一原文回绑 |
| 2026-08-04 | 修正文本证据归并粒度：同一 block 的不同原文句保留独立目标，同一句跨版面 block/物理换行则合并为一个逻辑来源，并由 PDFium 分片搜索全部字符框 |
| 2026-08-05 | 为通用论文问答增加轻量回答契约：正式回答块必须绑定用户当前问题，完整性门禁可拒绝“有引用但未回答”的结果，并复用唯一一次定向修复；真实运行发现独立要求规划调用会耗尽单轮预算后已移除，不增加额外模型调用 |
| 2026-08-05 | 修复普通提问回归：移除额外规划模型后，同一篇论文的“SINR 公式在哪”真实运行由 Token 超限/证据误拒恢复为完成；门禁保留对 SINR、SIC 等事实术语的短引文校验，但不再把 `SYSTEM MODEL` 等章节元数据当作必须重复的事实术语 |
| 2026-08-05 | 按确认设计稿完成前端 UI 重构：主页面使用 220px 导航，子页面使用 52px 图标栏并支持覆盖展开；统一亮暗 Apple 风格，看板、文库、检索、论文工作台和研究档案完成换肤，写作助手保持原样；主题/设置/帮助统一移至左下角并与任务清单分组，页面导航快捷键连续使用 Ctrl+1–6；移除重复的顶部命令搜索框、整块空白顶栏及子页面右上角工具占位，但保留 Ctrl+K 命令面板；未修改后端接口或业务逻辑 |
| 2026-08-07 | Windows 本地运维脚本收敛为五个 `.cmd`：数据库启动、前端启动、后端启动/重启、整体启动和整体终止；删除重复的 Bash、备份、恢复和部署包装脚本，并加入 PID 归属校验、未知端口保护及就绪检查 |
| 2026-08-07 | 本地论文目录由 `backend/data/papers` 迁至项目根目录 `data/papers`；文献库移除“+”导出菜单并替换为受限的“查看文献文件夹”入口，数据库相对文件名保持不变 |
| 2026-08-07 | 论文删除统一纳入事务后文件生命周期：数据库级联删除标注/解析数据，事务提交后删除本地 PDF 和派生图片；同时修复批注保存、标记失焦退出及看板/文库/PDF 工具栏布局细节 |
| 2026-08-07 | 将固定选区改为下一条消息附件；无选区对话按明确指代决定是否继承检索焦点，并允许无论文证据的普通知识回答；证据回链改为唯一原句优先、跨回答块合并物理换行；加入经引用证据约束的“找出并高亮”命令 |
| 2026-08-09 | 用真实失败分块确认 Kimi 隐藏思考挤占结构化输出预算；论文理解改为低推理且取消同参数盲重试，失败时保留成功分块供定向重试。公式框选升级为一个区域内按视觉顺序识别、保存、编辑和确认最多 8 条公式。 |
| 2026-08-09 | 已固定公式改为显式生命周期：普通点击与工具切换不再清除，新的文字/公式选区必须确认覆盖；公式转换和确认按钮移至状态行，公式内容区压缩到助手高度的 34%，优先保障对话空间。 |
| 2026-08-10 | 修复无选区论文问答的零召回：中文提问清理套话，“信噪比”同时检索 SNR/SINR；无选区时按明确指代和技术主题重合判定是否继承前文，无关问题不再注入旧对话。 |
| 2026-08-10 | 回答中的证据限制块改为仅供系统内部判断，不再展示给用户；API 设置新增基于当前 Base URL 与 API Key 的临时模型目录查询，结果列表限高滚动并显示查询状态，不改变原有保存与测试连接逻辑。 |
| 2026-08-10 | 论文对话输入区新增附件、LaTeX 与图标发送入口：附件在浏览器本地提取 PDF/文本内容后作为本轮辅助上下文，最多 3 个且不冒充论文证据；LaTeX 通过独立输入框插入并在用户消息中渲染。 |
| 2026-08-10 | 论文助手进一步让位于核心对话：内容/公式滑动选择器移入顶部标题栏；文字选区确认后折叠，公式在首次“确认固定”或后续“保存校正”成功后折叠，均可重新展开。取消收益有限的对话展开模式。 |
| 2026-08-10 | 修复直接高亮命令与研究档案生命周期：支持“将 XX 对应区域高亮”的自然表达，避免结构化推断标签触发无意义的二次模型修复；工作台运行在执行前绑定当前研究会话，历史回填只接收已有结果的成功运行，失败运行不再生成空白对话。 |
| 2026-08-10 | 重构论文对话路由与命令边界：上下文模式改为显式结果；“高亮 XX”在检索前隔离旧话题，“把它高亮”才继承上轮目标；一个命令可生成多个经引用验证的公式/正文高亮动作。引用列表按 PDF 物理阅读顺序合并跨行原句，不再受模型引用插入顺序影响。 |
| 2026-08-11 | 修复“本文最重要的公式/方法/结果”等全文判断题的零召回：判断类问题进入跨章节综合检索；公式问题直接以带编号公式为结构锚点，组合所属章节与相邻解释段，不再要求中文问题字面命中英文正文；明确论文问题若仍无证据会在模型调用前终止。 |
| 2026-08-11 | 重构连续科研对话的上下文边界：所有通过门禁的轮次（包括普通对话）均持久化，历史展示与检索焦点继承分离；统一识别指代、省略、备选、原因和展开型追问，论文追问零证据时在模型调用前终止。 |
| 2026-08-11 | 采用新的 Research Assistant 品牌标识：从确认的 D3 高清稿精确提取带弧形长腿的衬线 RA 连字和蓝色研究焦点，生成透明底明暗版本并同步侧栏与 favicon；保留旧版资源以便回退。 |
| 2026-08-11 | 统一收起侧栏的品牌标识与导航图标视觉尺寸：D3 标识缩至 30px、导航图标调整为 20px；隐藏标签不再占位，针对字形偏左向右增加 1px 光学补偿，并移除展开/收起按钮的焦点白框。 |
| 2026-08-11 | 修复引用跳转与选区命令边界：公式引用的证明文字只用于展示，点击始终回到公式区域框；“将这段文字高亮”直接绑定本轮规范化选区并以零模型调用执行。PDFium 自绘选区补充 Ctrl+C/右键复制，任务中心按钮支持返回进入前页面，论文对话标题与历史区增加分隔。 |
| 2026-08-11 | 正式落地可信来源索引与命令优先执行：为编号公式建立“定义/提及”和“定理结论/证明步骤”关系，公式型全文问题优先召回定理结果，Evidence Gate 拒绝用证明中间式冒充定理结论；当前选区高亮绕过检索和模型并直接使用浏览器原始字符框。版面语义升级为 v4，真实双栏论文的式 (21)/(22)/(31)/(34) 已与相邻证明文字拆分。 |
| 2026-08-12 | 完成论文助手关键链路重构：建立七类统一轮次路由；上下文装配只消费该路由；编号公式按同页、同阅读流绑定 Theorem/Lemma/Proposition 所有权，再统一为文档来源索引；PDF 操作扩展为高亮、下划线、笔记、批注和跳转，并以确定性证据坐标执行。模型提示改为逐事实回答块与就近引用，不再向用户暴露内部证据/转写状态。 |

| 2026-08-13 | 对话运行改为服务端完成即归档：浏览器离开论文页面只会停止当前观察器，不会取消后台任务或丢失回答；重新进入后从研究会话恢复结果。对话历史新增“回到最新消息”按钮，内容/公式尚未选取时的占位区改为无装饰图标的两行紧凑提示。 |
| 2026-08-13 | 完成统一路由、来源定位与命令链路的必要加固：确定性规则优先、短歧义追问才使用低预算模型兜底；论文独立问题、上下文追问和普通对话共用同一权威路由。跨行来源保留多个物理框，引用按 PDF 阅读顺序编号；门禁会拒绝定理/引理结论与公式编号不匹配的回答。五类 PDF 命令均绕过回答模型执行，笔记/批注正文与目标分离，页码跳转无需伪造证据。 |
| 2026-08-13 | PDFBox 版面解析升级到 v2、语义层升级到 v5；数学字符破坏普通空白检测时，仅对“正文与带编号公式并排”的行启用栏间线拆分。16 页真实双栏论文回归由 2236 个块变为 2237 个块，式 (16) 不再把左右栏合成整页宽证据框，来源索引同时识别至少两条定理结果公式。 |
| 2026-08-21 | 冻结 Agent 重构 v0.9 方案并完成阶段 0：提交重构前基线 `3bdffec`，建立 Agent 轮次黄金集、指标门槛、零容忍停止条件和旧 Workbench/会话依赖清单。 |
| 2026-08-21 | 完成 Agent 重构阶段 1：新增轮次/运行/工具调用 Runtime、附件和版本化会话摘要，建立单活动轮次、条件状态迁移、配置快照和幂等工具调用边界；完整后端回归 598 项通过。 |
| 2026-08-21 | 完成 Agent 重构阶段 2：复用现有版面索引建立稳定来源对象与严格引用回绑；论文改为手动理解，三次失败后才开放本地受限降级，不引入 Embedding/RAG；完整后端回归 602 项通过。 |
| 2026-08-21 | 完成 Agent 重构阶段 3–5：贯通只读 Agent、自然语言澄清、严格 cite、五类 ActionTicket 页面操作，以及对话/文档双角色配置和分项能力探测。 |
| 2026-08-21 | 完成阶段 6 单写切换和清理：前端统一 Turn API，附件改为服务端暂存/认领；备份真实数据库后执行 V32–V36，论文 3、版面制品 3、标注 8 条迁移前后不变。 |
| 2026-08-21 | 完成阶段 7 自动回归：后端全量测试、前端 118 项单元测试及生产构建通过；真实供应商、陌生论文和页面操作保留为发布前人工验收。阶段实现保持未提交。 |
| 2026-08-31 | 按简化范围删除写作助手及已确认退役的研究自动化、导出、引用扩展和 RAG 诊断代码；增加 V39 清理对应数据表，保留论文研究页对比文献入口。后端 456 项测试通过、9 项跳过；前端 129 项测试及生产构建通过。 |
| 2026-08-21 | 修复重构后的两项界面回归：旧版面缓存不可读或 readiness 请求失败时仍稳定显示手动“开始理解”；API 设置改为对话/解析双段切换，每侧仅 URL、Model、Key，均可查询模型，协议由 URL 自动识别。真实 MySQL readiness 返回 200；后端 463 项、前端 119 项测试及生产构建通过。 |
| 2026-08-21 | 收敛模型配置交互：移除供应商预置选择遗留的模型下拉箭头，模型保持自由文本输入；查询结果改为输入框下方横向换行的可点击候选标签。弹窗与独立设置页行为一致，前端 119 项单元测试及生产构建通过。 |
| 2026-08-21 | 修复 Agent 人工验收首批运行问题：同步长请求改为“持久化受理后由专用线程池后台执行”，前端凭 `runId` 轮询并可从历史未完成轮次恢复，页面切换不再拥有或取消服务端运行；移除未判定上下文时误导性的“基于论文理解”标签。后端 465 项、前端 120 项测试及生产构建通过。 |
| 2026-08-21 | 修复 Agent 人工验收第二批问题：进入论文助手时自动恢复该论文最近活跃的对话，无历史才创建新对话，同时保留显式切换/新建入口；Runtime 强制每轮使用原生工具，消除普通问题因模型直接输出文本而发生的重复催促调用；Agent 证据补齐 `paperId`，旧消息缺少该字段时按当前论文兼容跳转。后端 465 项、前端 120 项测试及生产构建通过。 |
| 2026-08-21 | 修复 Agent 人工验收第三批问题：从文库重新进入已缓存的同一论文时重建助手会话面板并恢复最新对话；模型正文中的 `[1]` 等自编号在持久化前和旧消息渲染时清理，只保留系统生成的可点击引用；页面操作工具只在明确操作词出现时开放，并修复 ActionTicket 旧版本冲突、citation 轻微措辞差异误拒、累计 Token 预算与 8 轮上限不匹配及旧失败 run 被误恢复。真实原问题最终以 4 次模型调用完成且证据可跳转；后端 469 项、前端 122 项测试及生产构建通过。 |
| 2026-08-25 | 完成 Agent 稳定性架构修正：LangChain4j AI Services 接管模型—工具循环，Runtime 收敛为持久化、幂等、权限、证据和后台任务边界；只读工具压缩为按需 search/read/page，回答改为 answerBlocks 绑定已读来源并由服务端生成引用；补齐论文理解就绪门禁、失败分类和服务端超时收敛；公式证据统一为单个透明虚线框并在异常时退化到编号框。阶段定向后端 58 项、前端 40 项通过，完整回归结果见下方最新基线。 |
| 2026-08-25 | 修复历史失败 Run 的前端恢复死循环：移除 `running` 结束后反向刷新历史的递归触发，按 `runId` 去重自动恢复；`FAILED/CANCELLED` 终态只检查一次，网络中断和页面主动停止观察仍允许后续重连。用真实失败会话 35 验证 3.5 秒内消息数、pending 和错误状态保持稳定；前端 125 项测试及生产构建通过。 |
| 2026-08-25 | 修复失败提示跨对话泄漏和模型无界检索（阶段性止损，后续已由批量证据抽象替代）：错误按 `researchSessionId` 隔离，新对话不继承旧红字，切回失败对话仍可查看；原问题曾以 25.65 秒完成，4 次模型调用、6 次工具调用、2 条可点击证据。 |
| 2026-08-25 | 修正论文 Agent 检索抽象：移除“最多两次搜索”流程补丁，将 `search_paper` 与 `read_source` 合并为支持多个检索意图的 `retrieve_paper_evidence`，一次工具执行完成搜索、去重、读取和可引用来源登记；保留 LangChain4j 轮次仅作异常熔断，同轮等价只读请求复用结果。工具层 4 项、Agent/LangChain4j 定向测试通过，尚未提交。 |
| 2026-08-25 | 批量证据工具改造完成全量回归：后端 `mvnw.cmd clean test` 477 项执行、0 失败、0 错误、4 项跳过；前端 29 个测试文件、125 项单元测试通过，生产构建通过；未提交。 |
| 2026-08-25 | 按人工验收反馈将公式证据跳转改为编号锚点：Agent 证据透传 `formulaNumber`，前端在目标页搜索并高亮 `(21)`、`(1a)` 等编号；支持编号列表的子公式组，原公式坐标仅用于找不到编号时的滚动兜底，不再绘制覆盖正文的大公式框。新增公式编号解析测试；后端 `mvnw.cmd clean test` 477 项执行、0 失败、0 错误、4 项跳过，前端 30 个测试文件、129 项单元测试及生产构建通过，未提交。 |
| 2026-08-25 | 修复 Agent 回答展示与复合公式证据：回答块明确使用 Markdown/LaTeX，引用清洗保护数学区间与数学环境；同一回答块中同页同基础编号的子公式聚合为一个展示证据并可同时高亮全部编号；批量检索先保证各检索需求覆盖再按总分补充，避免单一高分需求挤掉其他证据。定向后端与前端回归通过，尚未提交。 |
| 2026-08-25 | 完成本轮修改验收：后端 `mvnw.cmd clean test` 共 479 项执行、0 失败、0 错误、4 项跳过；前端 30 个测试文件、131 项单元测试通过，生产构建通过；按项目脚本重启后端，health 返回 `UP`，未提交。 |
| 2026-08-25 | 按最终 Agent 方案完成收尾：能力探测改为诊断信息而非每轮硬门禁；LangChain4j 使用 `ToolChoice.AUTO`，回答/澄清共用 `submit_answer` 终止工具；普通问题允许直接回答，论文事实必须结构化绑定本轮已读来源；来源索引按 PDF 版本缓存，批量读取结果设有单次边界，上游过载映射为明确可重试错误。新增能力探测失败仍可直答、统一澄清协议回归；后端 `clean test` 共 491 项执行、0 失败、0 错误、4 项跳过，前端 30 个文件/131 项通过，生产构建通过；未提交。 |
| 2026-08-26 | 简化 Agent 回答协议：删除论文关键词硬门禁和“使用工具后必须结构化提交”的限制；普通 Markdown 始终可作为有效终答，`submit_answer` 仅在需要可点击引用时使用；论文读取异常作为 `unavailable` 工具结果回传，模型继续依据对话、选区和论文画像回答且不得伪造引用。定向 29 项及后端全量 492 项测试通过，0 失败、0 错误、4 项跳过；未提交。 |
| 2026-08-26 | 完成 Agent 功能层重构代码（未进入测评阶段）：拆分 `paper_overview` / `paper_evidence` / `page_action` 三个职责单一的 Skill；论文画像改为按需加载，普通对话不再无条件注入全量画像；合并所有 SystemMessage 以避免 LangChain4j 记忆覆盖；证据工具支持多查询/页面范围批量读取并将无结果作为非阻断状态；新增确定性画像质量校验和独立 `profile_quality_json`。本阶段仅完成功能代码与跳过测试的后端编译，未搭建测评器、未建立测试集、未运行功能或效果测试，未提交。 |
| 2026-08-26 | 建立并执行论文 190 的 Agent 第一批 10 例真实测试：普通问答 2/2 正确且零论文工具误调用；严格总通过 2/10。论文 4/4 分块成功但全局画像连续两次 `PROFILE_GENERATION_FAILED`；overview 无依据幻觉为 SPONGE 论文；4 个证据/方法运行共调用 21 次批量证据但全部失败；另确认工具合同与 4 条返回上限不一致、页面范围参数反复失败、超时后工作线程仍落库回答等架构缺陷。详细结果见 `docs/eval/rsma-batch-1-result.md`；尚未修复这批失败，未提交。 |
| 2026-08-26 | 按已确认的 API/附件方案完成实现：能力测试改为提交 URL、Model、临时 Key 草稿且不保存设置；文档角色继续强制图片能力并单独探测原生 PDF。论文对话附件上传原始 PDF、Word（doc/docx）和 JPG/PNG，后端用解析 API 处理，原生 PDF 不可用时回退为 PDF 页面图片，Word 提取正文/表格/内嵌图片后发送；文本附件和手动 LaTeX 保持轻量直注入。新增能力探测、Word/图片类型、解析 API 输入与 LaTeX 直通测试；定向后端测试与前端 132 项单测/生产构建通过。全量后端回归仍包含此前未修复的 9 项基线失败，未提交。 |
| 2026-08-27 | 完成整篇论文理解链路重构并完成真实回归：论文 184、185、190 均以单个整篇记忆块生成 `READY` 画像，结构化正文与页面图片共同作为解析 API 输入，未再执行旧的分块摘要/画像汇总链路；修复整篇正文同时出现在提示文本和正文附件中的重复传输；后端全量 `mvnw.cmd test` 501 项执行、0 失败、0 错误，跳过项以测试报告为准；重新打包并按项目脚本重启后端，health 返回 `UP`。期间修复画像投影把自由文本写入分类字段导致的数据库截断，并关闭解析模型隐藏自动重试；本轮未提交。 |
| 2026-08-27 | 清理论文 184、185、190 的派生解析数据并先做完整 SQL 备份（备份文件保留在 `backups/research_assistant-before-reparse-20260827.sql`），保留 PDF、论文记录、会话和标注；发现 Kimi Coding 文档模型未应用既有的 `thinking=disabled` 参数，导致 184 首次整篇请求在 180 秒客户端截止处失败。补齐该文档模型参数并重启后，三篇顺序重解析全部成功：184 约 93 秒、185 约 53 秒、190 约 69 秒；均为 `READY`、1/1 整篇块完成、`profile_quality.usable=true`、无无效证据，后端 501 项测试与打包通过，health 为 `UP`，未提交。 |
| 2026-08-27 | 重新备份并清理论文 184、185、190、191 的派生解析数据后，使用同一整篇理解链路顺序重解析四篇：任务耗时分别约 57/56/51/66 秒，全部 `COMPLETED`；记忆均为 `READY`、1/1 整篇块完成、`profile_quality.usable=true`、无无效证据，论文分析记录和版面制品均生成。四篇核心画像与证据绑定可解析，后端 501 项测试通过、health 为 `UP`，未提交。备份为 `backups/research_assistant-before-reparse-184-185-190-191-20260827.sql`。 |
| 2026-08-27 | 导出四篇最新 `paper_memory` JSON 为 UTF-8 可读文本，保存在 `docs/eval/reparse-20260827/`；解析输入/输出 token 分别为 184（15677/1474）、185（13056/1692）、190（41132/1377）、191（40274/1677），总量分别为 17151、14748、42509、41951。理解耗时约 55/55/50/63 秒，任务总耗时约 57/56/51/66 秒。 |
| 2026-08-28 | 以轻量语义 span 完善整篇解析输入：同页相邻碎块确定性重组，明显 FORMULA/CAPTION 误判在读取层纠正，模型只引用 span 并在入库前展开为原始 block ID；同一 span 同时作为证据 Skill 来源并保留每个 block 的 bbox，页面操作可直接解析多个可信矩形。未新增表、二次模型、修复调用或在线语义门禁。四篇版面回放块数下降 27.2%–33.8%，映射/坐标覆盖 100%；四篇真实单调用重解析均 READY、issues 为空，详见 `docs/eval/reparse-20260828/summary.md`。后端全量 507 项执行、0 失败、0 错误、7 项跳过；未提交。 |
| 2026-08-28 | 完成轻量证据链路量化与修复：每轮只执行一个证据批次，重复请求复用首批结果；事件投影增加耗时、复用、需求覆盖、来源 ID 和响应大小等无正文诊断，评测核验最终证据确属首批返回。画像 claim 仅暴露轻量 `claimRef`，证据 Skill 使用原始用户问题与模型查询焦点复用解析阶段已有 block 绑定，不引入向量库、重排模型或新门禁。四篇 15 例 Direct Recall@4 为 15/15、178/178 locator 有效、p95 125 ms；两个原失败真实 Agent 样本定向复测 2/2 通过。交互运行预算收紧至 50 秒，供应商卡住时在 60 秒用户门槛内安全失败。后端全量 513 项执行、0 失败、0 错误、9 项跳过；未提交。 |

## 最近验证基线

- 2026-08-26 最新验证：后端 `mvnw.cmd test` 共 492 项执行、0 失败、0 错误、4 项可选测试跳过；本轮没有前端改动。后端将在本轮最终变更后按项目脚本重启并复核 health；真实供应商连续运行仍须人工验收，不能用自动化结果替代。

- 后端：Agent 清理后的 `mvnw.cmd clean test` 共 469 项执行、0 失败、0 错误、4 项可选测试跳过；增加旧版面缓存 readiness 恢复、双角色模型查询、文档协议自动识别、Agent 后台受理、Runtime 强制工具选择、操作工具显式开放、模型自编号清理和 citation 相邻句回绑覆盖。
- 前端：29 个测试文件、122 项单元测试通过，生产构建通过；覆盖后台 run 受理后轮询、最新对话自动恢复、旧失败 run 不误恢复、证据论文标识映射、模型自编号清理、API 双段切换和 readiness 失败时“开始理解”入口。
- Agent 真实回归：论文 190 的原失败问题“找出文章最重要的一条公式结论”运行 `665eee66-49e8-41f5-8543-fea1858b28ac` 完成；4 次模型调用、7 个只读工具调用、0 个页面操作，正文无模型自编号，4 条证据均携带第 6 页定位，浏览器点击式 (22) 证据由第 3 页跳至第 6 页。
- 数据迁移：备份文件 SHA-256 为 `93DFD36B0B483DBADD23D4E7E80A6AADF9C08B1F74F9F37A0B34E8257CE40749`；真实 MySQL 的 V32–V36 成功，旧四张运行表已删除，论文/版面制品/正式标注数量未变。
- 真实版面回归：论文 189 的 16 页 PDF 以 `pdfbox-layout-v2` 解析为 2237 个块，布局置信度 0.900；式 (16) 所在并排行已拆栏，来源索引检出至少两条 `THEOREM_RESULT`。
- UI 实测：子页面导航展开前后内容区宽度保持不变；文库分隔线保留 7px 拖动命中区；论文助手默认占工作台 40%，键盘调宽与双击复位实测有效；亮暗主题、检索四阶段和研究档案“单论文—多对话”布局通过浏览器检查。
- 运行：后端 health 为 `UP`，前端 HTTP 200。
- 脚本回归：数据库、前端独立启动通过；后端在已运行时完成 PID 切换并恢复 `UP`；整体终止后 3306/8080/5173 均释放，整体启动后三个端口、后端 health 和前端 HTTP 均通过。
- 存储回归：45 个历史 PDF 逐文件 SHA-256 校验后迁至根目录；历史论文下载接口返回 HTTP 200，文献目录接口只返回并打开配置目录。
- 生命周期回归：临时论文的批注写入数据库成功；删除论文后论文行为 404、批注级联清空、对应 `data/papers` PDF 不再存在；路径越界删除测试通过。
- 交互回归：点击既有高亮进入编辑态，点击工具栏后退出；批注真实保存无网络错误；成组按钮保持单行且中间圆角为 0。
- 真实回归：论文 175 的“为我找出 SINR 公式在哪”运行 `025a8ec5-861f-4923-958e-71f909cd2d15` 已一次调用完成，未触发门禁修复；前端归并为 5 个唯一来源，回答和引用均定位第 4 页公式 (4)/(5)。
- 页面回归：100% 缩放下，公式区域与正文短引句点击后均滚动进 PDF 可视区；公式显示一个完整区域框，正文显示精确字符框。
- 最新实测回归：运行 `44982bd8-c7ff-460b-bb36-d9d542e58d7b` 的公共流跨行定义归并为一个来源；“背景噪声”点击返回两个跨行字符框，未再框选后续 SINR 句。
- 真实失败样本隔离验证：论文 186 的失败分块 1 在原文和模型不变、关闭思考后 16.85 秒完成，7070/509 tokens，JSON 与 13 个引用 ID 全部有效；同页双公式区域使用数组协议后 12.67 秒返回两条公式，403/216 tokens。
- 全文判断题回归：原失败问题“你认为该文章最重要的一条公式是什么？”已覆盖查询规划、带编号公式结构召回、跨章节候选与空证据调用保护；普通无关问题的零证据自由对话保持不变。
- 真实端到端复验：论文 187、同一原问题运行 `ed6f4e11-ce34-4c67-920d-b800f0d5f0bc` 完成；检索 18 条证据、覆盖 12 个章节/公式分组，单次模型调用、0 次门禁修复、5505 tokens，回答定位到第 6 页 Theorem 1 的公式 (22) 并绑定可跳转引用。
- 连续追问真实复验：与上轮同一对话的“如果还要再选一条公式呢？”运行 `2bde24ae-d870-4d52-bd7f-bf56ef6d9149` 完成；快照识别为 `FOLLOW_UP`，带入 1 轮服务端历史和 18 条本轮证据，选出第 7 页 Theorem 2 公式 (34)，单次模型调用、0 次修复、门禁覆盖率 1.0。
- 真实版面复验：论文 188 强制生成 `semantic-v4` 版面产物；第 6 页式 (21)/(22)/(31) 与第 7 页式 (34) 均成为独立公式块，式 (34) 后的 `Based on Lemma 6...` 和 `Finally, by applying...` 保持为独立正文块，避免跳转框选中证明文字或把式 (34) 误当作 Theorem 2 结论。

## 下一阶段

由用户按最终验收标准执行真实供应商与人工交互验收；发现失败时先固化可复现样本并修复通用层。阶段 1–7 的实现未经用户命令不提交。

## 2026-08-27 整篇论文理解链路重构（当前未提交）

- 论文理解主链路改为每篇论文一次整篇语义调用；页面/块标记只用于保持顺序和来源定位，不再执行分块摘要、全局汇总或旧 map-reduce 回退。
- 解析模型客户端支持同一请求发送结构化全文与原生 PDF，或发送结构化全文加全部页面图片；图片能力仍由设置页探测结果决定，页面图由后端 PDFBox 生成。
- 旧的分块类暂保留为兼容代码，但 `PaperUnderstandingService` 不再引用其分块方法；一次失败不触发 JSON 修复调用或分块重试。论文理解版本升级为 `paper-understanding-v4-whole-document`，旧画像会重新要求理解。
- `AnalyzePaperSkill` 不再在论文理解完成后触发 RAG 索引；现有版面制品、来源索引、证据跳转和前端兼容投影继续复用。
- 新增整篇输入、单次调用和单元测试；三篇真实论文（184、185、190）的模型测评在后端重启后执行，结果另行记录。本次不提交 Git。
# 2026-08-28 四篇论文临时质量验收

- 增加可删除的 `PaperParsingFourPaperQualityEvalTest` 与小型人工检查点，不建设长期评测平台、不加入生产门禁。
- 当前结果：覆盖 4/4、角色纠正 8/8、碎片重组 2/2、关键事实 8/8、证据定位 181/181、严格直接支撑 8/8。
- 修复论文 184 的图注误引：图注内容继续用于全文理解和页面来源，但不再分配画像可引用 ID；高移动性结论现引用直接正文 `p4-b0049`、`p4-b0050`。提示单独约束经真实重解析证明不稳定，因此未增加在线门禁或二次调用。
- 论文 185 的 ASPA 结果段已重组成单个 span，但仍含行内公式拆分噪声；当前模型可正确理解，暂不扩展生产架构。
- 论文 184 最终单次重解析 READY，13642/1239 tokens、约 34.9 秒、issues 为空；兼容画像已同步，`token_used=14881`。
- 质量验收默认跳过；显式设置 `RUN_PAPER_PARSING_QUALITY_EVAL=true` 时 4 篇全部通过。后端全量测试 508 tests、0 failures、0 errors、8 skipped。
- 未提交 Git。

## 2026-08-28 轻量证据链路验收（当前未提交）

- 复用现有语义 span 来源目录与 `paper_overview` / `paper_evidence` / `page_action` 三个 Skill，没有新增数据库表、向量库、重排模型、证据判断模型或在线质量门禁。
- `paper_overview` 的模型侧结果移除文档哈希、质量报告、section digest、时间戳及 `evidenceBlockIds` 等内部字段，只保留理解论文所需的研究问题、贡献、方法、实验、结论与局限。
- `paper_evidence` 收敛为单次最多 4 个检索需求、最多返回 4 条来源；来源增加 `sourceRole`，CAPTION 明确只作辅助。删除候选数、覆盖统计和顶层截断标志等会诱发模型继续搜索的诊断字段，保留服务端搜索合并、逐需求占位与定位逻辑。
- 明确“可点击/可跳转引用”由 `submit_answer` 生成；只有用户明确要求立即跳转、高亮等页面操作时才调用 `page_action`。真实失败中出现的多余 JUMP 调用由这一职责边界修正，未增加运行时意图门禁。
- 临时确定性测试覆盖四篇论文 12 个证据点：Recall@4 为 12/12、MRR 1.000、正确证据均 rank 1、定位 92/92 有效，响应均低于 16 KiB。基线已全部通过，因此未修改词法检索算法。
- 真实 Agent 初版测试曾得到四篇正向证据问答与一条无依据问题 5/5，但该结论随后撤回：当时把一个触发 5 次证据检索并撞到 6 轮框架上限的困难样本替换成了较容易样本，不能证明工具调用已经收敛。
- 恢复困难样本后的复跑结果为 1/2：困难的“短码长与错误率”本轮以 1 次证据检索完成；原本容易的“最差用户公平性”却随机执行了 4 次证据检索，总计 6 次工具调用后才提交答案。说明召回与答案正确不代表调用行为稳定，当前仍存在触发 `maxToolCallingRoundTrips=6` 并丢失整轮答案的风险，证据链路尚未验收完成。
- 论文解析回归保持覆盖 4/4、角色 8/8、重组 2/2、事实 8/8、直接支撑 8/8、定位 181/181。后端全量测试 510 tests、0 failures、0 errors、9 skipped。
- 测试集属于当前构建期辅助材料，后续可按用户要求清除；当前下一步仍是解决并复验重复工具调用，不进入后续链路；未提交 Git。

### 2026-08-28 工具轮次错误修复

- 按“一次 Skill 调用覆盖多个证据需求”收敛：每轮回答只有一个实际证据批次，Agent 可一次提交最多 4 个需求；Skill 逐需求检索、优先为每个需求保留来源、统一去重并最多返回 6 条证据，结果以 `evidenceNeeds[].found/not_found` 明确覆盖状态。
- 单条检索 query 最多使用 400 字符；超长 query、超过 4 个需求、超过 2 个页码范围和未知可选类型均采用截断、取前 N 项或忽略无效过滤，不再让整批证据工具调用失败。真实失败样本中曾出现的 5587 字符 query 已由此消除。
- 同一 Agent 回答中的后续 `retrieve_paper_evidence` 请求只复用第一批结果并返回 `already_completed`，不执行第二次实际检索；正常主循环为画像、一次证据、提交共 3 轮。
- 若模型仍无视完成状态继续请求，LangChain4j 轮次保险不再向用户抛出 `TOOL_ROUND_LIMIT`：适配器切换到只保留 `submit_answer` 的收尾调用；若收尾仍异常请求工具，则退化为无工具 Markdown 回答。未提高工具轮次上限。
- 困难样本三轮复测共 6 次问答：6/6 未出现工具轮次错误且全部只执行一个实际证据批次；5 次使用 3–4 个模型工具调用完成。严格直接证据质量为 4/6，说明下一问题已从“工具报错”收敛为“首批跨语言/宽查询召回精度”，不能把后者与轮次修复混为一谈。
- 四篇确定性证据回归仍为 Recall@4 12/12、MRR 1.000、定位 131/131；后端全量测试 511 tests、0 failures、0 errors、9 skipped。未提交 Git。

### 2026-08-29 Agent 首轮高延迟定位与轻量修复

- 使用同一论文会话和同一模型服务做对照：普通短问“你好，请只回复：你好。”约 1.1 秒完成且没有论文工具调用；论文证据请求的本地 Skill 检索约 188–208ms，端到端约 8.1/18.2 秒，说明异常延迟主要来自 Agent 的两轮模型响应，不是本地证据检索或模型服务整体不可用。
- 修复证据焦点与画像 claim 的轻量匹配：短码长/块长、错误率/BLER、较短/缩短、较低/降低等常见中英文变体先归一化，匹配到多个声明时优先 `finding` 原始 block 绑定；证据结果增加 `evidenceBatchComplete`、`nextStep=answer` 和 `additionalEvidenceAllowed=false`，提示词明确证据返回后不再读取画像、页面或重复检索。
- 新增画像 claim 优先级与证据批次完成状态回归。论文 190 的两个真实证据用例复测 2/2 通过，每个用例均为 1 次真实证据检索、2 次工具调用，未再出现“证据后读画像/重复证据”；后端全量测试 514 项执行、0 失败、0 错误、9 项跳过。未提交 Git。

## 2026-08-29 Agent 化上下文与 Skill 自主决策修正（当前未提交）

- 移除画像和证据 Skill 的“已调用后禁止再次调用”“每轮只能一个证据批次”等语义工作流限制；Skill 只描述能力边界和轻量参数上限，是否再次读取由 Agent 根据当前上下文决定。
- 同轮缓存仅对完全相同的只读请求生效。不同证据需求、未解决需求或上下文缺失时，Agent 仍可再次检索；不再返回 `already_completed`、`nextStep` 等诱导固定流程的字段。
- 上下文预算统一调整为 110k；LangChain4j 负责当前运行内的 TokenWindow 记忆和工具结果回灌，Runtime 只保留模型调用、工具调用、时间和 Token 的通用资源保险。
- 跨回合恢复当前会话、当前论文版本的最近只读论文结果，作为带明确边界的非指令参考消息注入上下文；同时仅将仍存在于当前来源目录的来源 ID加入可引用集合。没有重建供应商专用 Tool transcript，也没有新增论文状态表或路由器。
- 新增画像 Skill 在画像暂不可用时仍保持暴露、历史工具结果恢复、相同/不同证据请求行为回归；后端全量测试通过，未提交 Git。

## 2026-08-29 证据链路性能复测与超时竞态修复（当前未提交）

- 上一轮完整 10 例中，真正的性能问题是模型在已返回 `found` 证据后继续多轮往返；本地证据 Skill 单次耗时只有几十到三百毫秒，并非检索阻塞。原 8 轮保险线与 50 秒运行时限组合曾触发 `RUN_TIMEOUT`。
- 将 LangChain4j Agent 的通用工具轮次保险收敛为 2 轮能力调用，仍允许 Agent 自主选择画像、证据或不同证据需求；证据一次最多合并 4 个需求，收尾由现有 `submit_answer` 完成，不新增语义工作流。
- 修复超时竞态：运行终态先于助手消息写入；若超时线程已经将 Run 置为 `FAILED`，迟到的模型结果只返回已持久化状态，不再写入假回答。超时扫描遇到并发终态也会安全忽略陈旧候选。
- 因 Agent 模型适配器单次请求上限为 60 秒，将持久化 Run 的最终安全期限调整为 70 秒；正常延迟由 2 轮能力保险控制，不以提高期限换取更多调用。
- 最终真实完整集 `rsma-batch-1`：10/10 进入正常终态、无 `RUN_TIMEOUT`/`FAILED`，P50 26.2 秒、P95 36.3 秒；严格样本通过 6/10，剩余 4 例分别是回答词面、页面操作决策或测试期望与 Agent 自主判断不一致，不属于超时故障。
- 四篇论文最终证据集共 6 例全部正常结束（约 8.1–28.2 秒），证据 Skill 最大耗时 298ms、最大响应 6,162 bytes；严格内容门槛通过 4/6，未出现证据工具失败或运行时超时。后端全量回归 519 项执行、0 失败、0 错误、9 项跳过；未提交 Git。

## 2026-08-29 Agent 化方案执行与当前复测结果（当前未提交）

- 按 Agent 化约束移除应用层固定工具轮次上限；`AgentRunBudget` 中保留的模型/工具数量字段只作运行诊断快照，不参与流程拦截。LangChain4j 仍负责原生工具循环，Runtime 只保留 90 秒最终安全边界、持久化终态和迟到结果丢弃。
- 完善三个 Skill 的模型侧契约：画像明确只负责整篇论文方向理解，证据以 `needs[]` 批量表达需求并为未覆盖需求最多执行一次轻量 fallback，页面操作明确单次一个动作、只接受可信来源对象、不接受模型坐标。没有新增工作流路由、证据门禁或多动作票据协议。
- 新增/更新批量 evidence schema、fallback、页面动作可信目标和 Agent 多工具轮次回归；聚焦测试通过。后端全量 `mvnw.cmd test` 共 521 项执行、0 失败、0 错误、9 项跳过；`git diff --check` 通过。
- 真实 RSMA 10 例全部进入预期终态，0 `FAILED`、0 `RUN_TIMEOUT`，中位耗时约 28.3 秒，最大 58.5 秒；证据工具没有失败。严格通过 5/10，其中一条为测试词面匹配问题，一条为选区无效导致的页面动作失败，其余主要暴露 Agent 重复证据调用和回答完成/术语问题。
- 四篇论文 6 例全部进入 `COMPLETED`，0 `FAILED`、0 证据工具失败；严格通过 5/6。论文 190 的公平性问题重复调用证据 9 次、总耗时约 60.5 秒，虽未触发 90 秒终态保护，但确认 Agent 收敛仍不稳定。该问题已记录，后续先讨论提示/模型行为与是否需要更轻量的收敛提示，不在本轮加硬上限。
- 当前页面动作协议仍为单动作票据；没有为了测试增加 `actions[]` 批协议，因为这会牵涉客户端回执、幂等和标注写入合同，超出本轮轻量修改范围。未提交 Git。

## 2026-08-29 证据覆盖、结构化提交与无新增来源检测（当前未提交）

- 证据 Skill 增加可选 `targets[]`，对公式、图表、章节、指标等明确枚举目标返回 `coveredTargets` 与 `unresolvedTargets`；只有显式目标全部覆盖时才返回 `complete`，开放式命中返回 `found`，部分命中返回 `partial`，避免把“返回了一条来源”误报为完整覆盖。
- 保持一次最多 4 个 needs、每个 need 最多一次 fallback；普通结果最多 6 条来源，带显式目标时最多 8 条，未引入向量库、重排模型、证据表或固定 Agent 轮次。
- Agent 在本轮已拥有的来源集合上计算 `newSourceCount`。重复查询或同义改写未产生新来源时返回 `exhausted=true` 和停止原因，向模型明确停止，不再通过无效查询继续等待。
- 成功读取可用论文证据后，普通 Markdown 不再作为合格终答；必须通过 `submit_answer` 绑定实际来源，否则运行以明确的证据提交契约失败结束。普通问题和未获得可用证据的回答仍可直接输出。
- 运行事件补充 coverage、newSourceCount、exhausted 诊断；新增显式目标、证据契约和无新增来源回归。后端全量 `mvnw.cmd test` 共 523 项执行、0 失败、0 错误、9 项跳过；启用现有四篇论文证据验收后 15/15 Recall@4、169/169 locator 有效、延迟 p95 114ms；未提交 Git。

## 2026-08-29 公式族扩展与单次证据审计试验（当前未提交，未验收）

- 按轻量方案实现通用子公式族扩展：检索命中如 `35a` 的 FORMULA 来源时，候选集一并加入同篇论文中同数字基号的子公式；保留原子来源和 locator，不新增证据表、向量库或复合实体。
- 增加一次候选审计模型试验：只接收问题、needs 和最多 16 条候选，输出 supported/partial/unsupported、最小来源 ID 集、缺口和可选 retryQuery；每次 Skill 最多调用模型一次，无修复重试，输出非法或模型失败时回退到原确定性排序。
- 聚焦单测、应用上下文测试与后端全量测试通过；全量共执行 528 项、0 失败、0 错误、9 项跳过。四篇确定性证据评测继续通过。
- 真实 API 表明公式扩展本身有效：论文 190 的 P0 首次证据返回已包含 `35a–35g` 全部七个子公式。但审计方案未达到收敛与延迟目标：六个原证据案例仅 3/6 在一次证据调用后停止，复杂案例仍调用 2–4 次；单次证据 Skill 因内部模型增至约 2.8–14.9 秒。
- 正确的 P0 真实案例首次已取齐七个公式，但因其他 need 持续被判为 partial，Agent 重复检索 4 次并在约 92.8 秒失败。这说明当前主要缺口是审计模型的完整性判断和 Agent 对停止信号的遵循，不是继续扩展检索规则。按用户要求本轮只记录失败，未为通过测试追加停止门禁或补丁。

### 2026-08-29 内部模型审计撤回

- 根据真实 API 对照结果，已删除在线 `EvidenceAuditService`、审计 schema/输出校验、审计状态与相关测试；证据 Skill 恢复为确定性检索和输出，不再生成 `retryQuery` 或模型语义完整性判断。
- 保留已被 P0 真实案例证明有效的公式族扩展：子公式仍是独立来源和 locator，候选返回可在命中任一子公式后补齐同基号兄弟。
- 聚焦公式族测试、四篇确定性证据评测与后端全量回归均通过；全量共 527 项、0 失败、0 错误、9 项跳过。未提交 Git。

## 2026-08-29 Autonomous Driving 论文 15 条证据案例实测（仅测试，未改生产链路）

- 将论文 190 的临时真实 Agent 证据案例扩展为 15 条，覆盖论文概览、场景动机、干扰机制、FBL 权衡、预编码、闭式下界、公式 (21)/(24)、P1–P4 算法、复杂度、公式族 (35a)–(35g)、公平性、仿真、图 9 块长/BLER，以及 ImageNet 无关问题；测试集文件为 `backend/src/test/resources/eval/agent-evidence-paper-190-expanded.json`。
- 首轮 15 条中有 6 条因测试案例 ID 拼接后的 `initial_message_key` 超过数据库字段长度而直接 500，未进入 Agent；仅缩短测试 ID 后重跑，未修改生产代码、Skill 或检索逻辑。
- 第二轮 15/15 均 `COMPLETED`，0 个工具失败、0 个 `RUN_TIMEOUT`；端到端 P50/P95 为 42.4/70.6 秒，最大 76.6 秒；真实证据检索 57 次，平均每条 3.8 次，最多 7 次。
- 三类问题人工审计（按首批返回是否已包含用户实际所需的直接来源，而不是机械按 `coverage=partial` 判定）：来源确实不存在 1 条（ImageNet，且为预期负例；解析产物全文无 ImageNet）；首轮检索不足 4 条（概览、干扰机制、预编码、最差用户公平性）；首轮已足但仍继续检索 9 条；及时停止且无上述问题 1 条（复杂度）。
- 复合公式案例首次即返回 `35a–35g` 七个公式，但仍进行了 7 次证据检索；这再次确认当前主要问题是 Agent 对首批证据和“partial”语义的收敛判断，不是公式候选缺失。测试结果记录在 `backend/tmp/agent-eval-20260829-expanded-r2/evidence-paper-190-expanded-20260829-213101.json`；本轮不据此实施修改。

## 2026-08-30 paper_evidence Skill 契约与轻量检索调整（当前未提交）

- 将模型侧参数收敛为 `needs[].question` 和可选结构锚点 `anchors[]`（公式、图、表、章节），移除容易把检索词、覆盖目标和自然问题混在一起的 `query/keywords/targets`。一个调用仍最多合并 4 个需求，每个需求只执行主检索和至多一次放宽检索。
- 保留画像 claim 的确定性来源映射和复合公式族扩展；候选扩大到 24 条后在 Skill 内过滤弱命中，并按明确锚点、精确短语/公式号、正文/公式/表格、caption/reference 的通用来源角色排序。返回契约改为顶层及逐需求 `FOUND/PARTIAL/NOT_FOUND`、`coveredAnchors/missingAnchors/fallbackUsed`。
- 移除 Runtime 注入的 `_evidenceFocus`、`newSourceCount/exhausted/stopReason` 以及全局系统提示中的证据 Skill 专用调用规则；Agent 仍可自主调用，Skill 的用途、触发场景、限制和重试语义由 Skill 自身描述承担。未新增模型、向量库、数据库表、调用门禁或工作流编排。
- 聚焦测试与后端全量回归通过：527 项执行、0 失败、0 错误、8 项跳过；四篇确定性证据测试 15/15 Recall@4，149/149 locator 有效，检索 p95 70ms。
- 真实 Kimi 15 例仅 3/15 严格通过，端到端 p50/p95 约 94.7/94.8 秒，89 次工具调用中有 81 次证据工具事件，0 次工具失败。前 4 例完成，随后出现 19–22 次连续检索并触发约 90 秒运行终态；后续请求在模型首轮即超时。轨迹显示 Agent 会在 `FOUND` 后重复完全相同的请求，而进程内缓存仅把重复 Skill 执行降到 2–4ms，不能减少模型往返；由于本轮移除了 Runtime 的 `reused` 注入，测试报告中的 `actualEvidenceSearches` 不再等同于物理检索次数。
- 本轮按约定没有为通过测试追加硬调用上限或临时提示补丁。报告保存在 `backend/tmp/agent-eval-20260830-skill-contract/evidence-paper-190-expanded-20260830-010241.json`；后续需要单独讨论：Skill 自包含返回是否还需更明确的“本次检索已结束”语义，以及当前 Kimi 对可选工具描述的遵循能力。

## 2026-08-30 回归稳定基线并加入轻量无进展保护（当前未提交）

- 回退上一节的实验性 Skill 契约：恢复 `needs/query/keywords/targets/profileClaimRefs`、原有 `found/not_found` 及逐需求覆盖结果、画像 claim 焦点匹配和公式族扩展；移除 `question/anchors`、来源角色路由和实验性 `FOUND/PARTIAL/NOT_FOUND` 契约。这样恢复的是最近一次已验证过的稳定检索行为，不是重新引入固定论文工作流。
- 保留 Agent 化设计：完全相同的只读请求仍只做进程内缓存复用，不同需求仍可继续检索。新增通用的结果进展信号 `reused`、`newSourceCount`、`newSourceObjectIds`、`noProgress`；无新增来源时同时返回 `exhausted` 和 `stopReason`。该信号只帮助 Agent 判断是否继续，不按次数硬拦截，也不由 Runtime 判定答案是否完成。
- 聚焦回归通过；后端全量测试报告为 527 项执行、0 失败、0 错误、9 项跳过，`git diff --check` 通过。
- 真实 API 整批 15 条不能作为有效性能结论：2 条在触发限流前完成，之后 13 条受到 Kimi 组织级 `max RPM: 3` 限流；报告为 `backend/tmp/agent-eval-20260830-baseline-noprogress/evidence-paper-190-expanded-20260830-014649.json`。这不是证据 Skill 检索失败，也不是无进展保护失败。
- 限流间隔后进行的有效隔离样本 `precoding-power` 为 1/1：约 50.5 秒完成，2 次模型工具调用、1 次真实证据检索、0 次工具失败；报告为 `backend/tmp/agent-eval-20260830-baseline-noprogress-isolated/evidence-paper-190-expanded-20260830-014931.json`。相同案例在实验性契约下曾发生 21 次证据调用并约 92.8 秒失败，说明回归稳定契约并提供进展信号后，至少该类轨迹已恢复收敛。
- `formula-35-composite` 的两次隔离复测仍被同一 Kimi RPM 限流影响，不能据此判断公式族或无进展保护质量。当前不再追加代码修改，待 API 限流解除后再用同一 15 条集复测。

## 2026-08-30 旧 Agent 边界清理与轻量 Harness 补齐（当前未提交）

- 单论文对话入口现只保留 `/api/agent/turns` 与附件、动作回执配套接口。原 `AgentController` 中仍有产品用途的论文理解任务、异步任务、检索、对比、Gap 和推荐能力统一迁移到 `/api/research-automation/**`，旧 `/api/agent/chat` 与 `/api/agent/gap/chat` 已移除；前端调用同步更新。
- 原 `AgentOrchestrator` 更名并收敛为非对话式 `ResearchAutomationService`。论文理解从旧 Skill 框架中解耦为 `PaperUnderstandingTaskService`，执行步骤未改变；删除已无生产调用的 `AnalyzePaperSkill`、`ChatSkill`、旧 `ResearchAiService`、JDBC ChatMemory 适配代码及 DTO/Mapper/Entity。为避免无授权删除历史数据，旧 `conversation` 数据表暂未执行 DROP。
- 上下文从“固定最近 12 条”改为滚动压缩：未压缩消息按摘要边界完整加载；超过 24 条时以本地确定性摘要压缩旧消息并保留最近 12 条原文。压缩不调用外部模型，不增加回答延迟或 API 限流风险。
- 全局 system prompt 只保留通用安全、证据绑定与输出规范，具体 Skill 使用规则由能力契约描述，减少多层重复提示。
- LangChain4j 模型循环增加内容无关的逐次调用观测：记录序号、耗时、消息数、工具数、Token、结束原因和错误类型，不保存 prompt、回复正文或私有推理；终态事件可返回该诊断。新增 Flyway `V38__agent_model_call_trace.sql`。
- 工具错误只在超时、限流、服务过载或网络连接异常时标记为可重试；参数、证据校验和业务状态错误不再统一鼓励重试。观测写入失败不会反向破坏一次成功模型响应。
- 本轮未修改论文解析算法、画像 Skill、证据 Skill 或页面操作 Skill 的业务行为，也未运行真实 API 质量评测。针对性测试 47 项通过；后端全量 523 项执行、0 失败、0 错误、9 跳过；前端生产构建通过。未提交 Git。

## 2026-08-31 本地开发环境接管（当前未提交）

- 核对本机原生开发环境并继续使用现有 MySQL 8.4 Windows 服务及 `research_assistant` 数据库；补齐 Temurin JDK 17，前端依赖按锁文件重新安装，不启用 Docker。
- Windows 启动脚本可自动识别 MySQL/MariaDB 服务并在需要时通过 UAC 启动；后端优先选择 `JAVA17_HOME` 或本机 Temurin JDK 17，保留对其他 JDK 17+ 路径的兼容。
- 修正 Agent 边界重构后 3 个仍引用旧 `/api/agent/workflow` 地址的契约测试，统一使用 `/api/research-automation/workflow`。前端 132 项单元测试与生产构建通过；后端全量 523 项执行、0 失败、0 错误、9 项跳过，`git diff --check` 通过。
- 发现本机同名库只有 7 张早期表，不冒险强制 Flyway baseline；先备份为 `backups/research_assistant-local-before-restore-20260831-0208.sql`，再恢复 2026-08-27 最新完整备份。恢复后 34 张表、4 篇论文，启动时 Flyway 成功从 V37 升至 V38。
- 为当前 Windows 用户配置随机 32 字节 `RA_MASTER_KEY`，后端重启后 health 为 `UP`，前端返回 200，论文 API 返回 4 条恢复数据。npm 非破坏性安全修复将 Playwright 升至 1.62.1、Vitest 升至 1.6.1；生产依赖审计为 0 漏洞，剩余开发工具链问题需跨 Vite/Vitest 主版本，本次不强制升级。

## 2026-08-31 Windows 开源克隆启动脚本完善（当前未提交）

- 启动流程改为面向任意 Windows 克隆目录：项目根目录始终由脚本自身位置推导，不保存当前电脑或桌面路径；JDK 17 从本地覆盖、环境变量、注册表、PATH 和常见安装目录发现并校验 `java`/`javac`。
- 增加 Git 忽略的 `scripts/local-config.cmd` 与可提交示例，可覆盖数据库账号、JDK/MySQL 位置、MySQL 服务名和 `RA_MASTER_KEY`，真实密码和密钥不进入仓库，也不永久修改系统环境。
- 数据库脚本会识别/启动已有 MySQL 服务、验证应用数据库，并在账号有权限时自动创建缺失数据库；后续表结构仍只由 Flyway 管理。前端首次启动按锁文件执行 `npm ci`，Node.js 低于 18 时给出明确错误。
- 前端启动增加项目进程归属校验：5173 被其他项目占用时不再把任意网页误判为 Research Assistant，也不会结束未知进程。README 已补充从克隆、可选本机配置到单脚本启动的完整步骤；未启用或引入 Docker。

## 2026-09-06 标准 Agent Skills 渐进式披露（当前未提交）

- 引入 LangChain4j 官方 `langchain4j-skills`，从三个独立 Skill 目录加载 `name`、`description` 和 `SKILL.md`；元数据始终可见，完整说明只在 Agent 调用官方 `activate_skill` 后进入上下文。
- `paper-profile` 只负责整篇论文画像，`paper-evidence` 负责批量证据检索与按需局部图片，`paper-action` 负责一个明确页面操作；业务 Java 类作为激活后的宿主工具，不再伪装成 Skill 脚本或由后端按问题编排调用顺序。
- 当前运行内由 LangChain4j 记忆自动回灌 Skill 指令和工具结果；跨回合仅持久化并恢复最近有效的官方激活 transcript，摘要压缩后 Agent 可自行再次激活缺失的 Skill。Runtime 只做通用持久化、幂等、安全与终态支撑。
- 画像结果保留有效来源 ID，证据结果将局部视觉内容传递给下一次模型请求；页面操作仍只接受可信来源 ID，不接受模型生成坐标。未新增 Skill 脚本、论文专用调用门禁、固定调用顺序或额外模型。
- 聚焦回归与后端全量回归通过：474 项执行、0 失败、0 错误、18 跳过；前端 29 个测试文件共 129 项通过，生产构建成功；`git diff --check` 通过。
- 三个标准 Skill 的元数据简介统一改为中文，并同步更新元数据加载测试断言；`AgentSkillRegistryTest` 4 项通过。证据检索重构仅完成现状分析与方案讨论稿，本轮尚未实施。

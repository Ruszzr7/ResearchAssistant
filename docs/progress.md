# 开发进度

> 只记录当前状态、里程碑和验证基线；完整历史与 diff 由 Git 保存。

## 当前状态

- 论文研究页已切换到持久化通用 Agent：统一 Turn 输入、原生 Tool Calling、自然语言澄清、严格 cite、ActionTicket 回执和刷新恢复共用一条写链路。
- 论文理解由用户打开论文后手动启动；优先达到 `PROFILE_READY`，连续三次真实失败且本地来源可读时才开放明确的受限兜底。
- 对话与文档解析使用双角色模型配置；图片能力必须实测通过，原生 PDF 由连接测试自动探测，不显示 Token 或成本。
- 当前实现不使用 Embedding、向量数据库或独立 RAG；论文事实通过版本化本地来源的确定性搜索、读取、逐字引用和页面定位完成。
- 同一论文可建立多个上下文完全隔离的对话；只共享论文来源和画像，不共享消息、摘要、指代焦点或选区。
- 旧 Workbench run/step、大路由、分类器和重复会话写链路已删除；确定性选区/公式区域能力继续作为 PDF 输入工具保留。
- PDF.js 负责渲染，PDFium 负责选择和搜索，PDFBox 负责服务端版面事实。
- 论文事实由当前 PDF 版本的本轮 evidence 支撑；会话历史和论文画像只帮助理解与检索。
- PDF 页面操作覆盖高亮、下划线、笔记、批注和跳转；所有目标由服务端当前版本来源解析，成功以客户端真实回执为准。
- 项目简化已移除写作助手、旧独立论文对比、Gap 分析、自然语言计划、BibTeX/Obsidian/Zotero 导出、引用网络扩展和废弃的 RAG 诊断；论文研究页保留“添加对比文献”入口，等待后续统一实现。

## 主要里程碑

| 日期 | 交付 |
|---|---|
| 2026-07-13 前 | 完成文库、AI 质量门禁、RAG 版本、可恢复任务、Flyway、部署与安全基线 |
| 2026-07-16 | 建立 PDF 工作台、SelectionAnchor、版面 evidence、固定 Workflow 和 Evidence Gate |
| 2026-07-17 | 完成公式区域确认、研究档案和证据驱动写作 |
| 2026-07-19 | 删除阅读计划，统一笔记/批注，建立结构化论文记忆与服务端对话上下文 |
| 2026-07-22 | 完成 PDF 内容管线验收，并将浏览器文字事实迁移到 PDFium/WASM |
| 2026-07-24 | 修复数学密集选区、可读文本、视觉降级和公式边界问题 |
| 2026-07-25–26 | 优化全文理解和公式识别耗时，完成 KeepAlive 恢复、富文本渲染和多供应商接入 |
| 2026-07-29 | 一个研究会话对应一个独立对话；研究档案改为论文—对话两栏；精简首页看板 |
| 2026-07-30 | 合并论文助手模式，恢复全文自由问答，修复混合术语检索和公式位置证据 |
| 2026-08-02 | 复盘真实 SINR 问答，确认通用检索、引用绑定和目标框仍需结构性重构 |
| 2026-08-02 | 清理临时诊断产物与重复历史文档，将受版本控制的 Markdown 总量由约 155 KB 精简到约 31 KB |
| 2026-08-02 | 完成可定位证据的混合检索、结构化回答块、引用短句校验和无 Embedding 本地索引重构 |
| 2026-08-04 | 修复真实 SINR 验收中“已检索但门禁修复超预算”的通用问答故障；增加服务端引文规范化、精简修复上下文和门禁原因审计 |
| 2026-08-04 | 修复门禁修复超时后的状态机故障：取消无效的首轮预算预留、拒绝非空截断 JSON、限制仅首轮模型调用可安全重放，并确保步骤清空字段真正写入数据库；同时安全清除通用知识块上的误附论文引用 |
| 2026-08-04 | 统一回答内与证据列表的来源级编号；合并同一公式的文本/区域证据，扩展公式目标框，按原文短句定位正文，并在跳页后滚动到实际目标；增加技术标识引文覆盖门禁及修复 evidenceId 的唯一原文回绑 |
| 2026-08-04 | 修正文本证据归并粒度：同一 block 的不同原文句保留独立目标，同一句跨版面 block/物理换行则合并为一个逻辑来源，并由 PDFium 分片搜索全部字符框 |
| 2026-08-05 | 为通用论文问答增加轻量回答契约：正式回答块必须绑定用户当前问题，完整性门禁可拒绝“有引用但未回答”的结果，并复用唯一一次定向修复；真实运行发现独立要求规划调用会耗尽单轮预算后已移除，不增加额外模型调用 |
| 2026-08-05 | 修复普通提问回归：移除额外规划模型后，同一篇论文的“SINR 公式在哪”真实运行由 Token 超限/证据误拒恢复为完成；门禁保留对 SINR、SIC 等事实术语的短引文校验，但不再把 `SYSTEM MODEL` 等章节元数据当作必须重复的事实术语 |
| 2026-08-05 | 按确认设计稿完成前端 UI 重构：主页面使用 220px 导航，子页面使用 52px 图标栏并支持覆盖展开；统一亮暗 Apple 风格，看板、文库、检索、论文工作台和研究档案完成换肤，写作助手保持原样；主题/设置/帮助统一移至左下角并与任务清单分组，页面导航快捷键连续使用 Ctrl+1–6；移除重复的顶部命令搜索框、整块空白顶栏及子页面右上角工具占位，但保留 Ctrl+K 命令面板；未修改后端接口或业务逻辑 |
| 2026-08-07 | Windows 本地运维脚本收敛为五个 `.cmd`：数据库启动、前端启动、后端启动/重启、整体启动和整体终止；删除重复的 Bash、备份、恢复和部署包装脚本，并加入 PID 归属校验、未知端口保护及就绪检查 |
| 2026-08-07 | 本地论文目录由 `backend/data/papers` 迁至项目根目录 `data/papers`；文献库移除“+”导出菜单并替换为受限的“查看文献文件夹”入口，数据库相对文件名保持不变 |
| 2026-08-07 | 论文删除统一纳入事务后文件生命周期：数据库级联删除标注/解析数据，事务提交后删除本地 PDF 和派生图片；同时修复批注保存、标记失焦退出及看板/文库/PDF 工具栏布局细节 |
| 2026-08-07 | 将固定选区改为下一条消息附件；无选区对话按明确指代决定是否继承检索焦点，并允许无论文证据的普通知识回答；证据回链改为唯一原句优先、跨回答块合并物理换行；加入经引用证据约束的“找出并高亮”命令 |
| 2026-08-09 | 用真实失败分块确认 Kimi 隐藏思考挤占结构化输出预算；论文理解改为低推理且取消同参数盲重试，失败时保留成功分块供定向重试。公式框选升级为一个区域内按视觉顺序识别、保存、编辑和确认最多 8 条公式。 |
| 2026-08-09 | 已固定公式改为显式生命周期：普通点击与工具切换不再清除，新的文字/公式选区必须确认覆盖；公式转换和确认按钮移至状态行，公式内容区压缩到助手高度的 34%，优先保障对话空间。 |
| 2026-08-10 | 修复无选区论文问答的零召回：中文提问清理套话，“信噪比”同时检索 SNR/SINR；无选区时按明确指代和技术主题重合判定是否继承前文，无关问题不再注入旧对话。 |
| 2026-08-10 | 回答中的证据限制块改为仅供系统内部判断，不再展示给用户；API 设置新增基于当前 Base URL 与 API Key 的临时模型目录查询，结果列表限高滚动并显示查询状态，不改变原有保存与测试连接逻辑。 |
| 2026-08-10 | 论文对话输入区新增附件、LaTeX 与图标发送入口：附件在浏览器本地提取 PDF/文本内容后作为本轮辅助上下文，最多 3 个且不冒充论文证据；LaTeX 通过独立输入框插入并在用户消息中渲染。 |
| 2026-08-10 | 论文助手进一步让位于核心对话：内容/公式滑动选择器移入顶部标题栏；文字选区确认后折叠，公式在首次“确认固定”或后续“保存校正”成功后折叠，均可重新展开。取消收益有限的对话展开模式。 |
| 2026-08-10 | 修复直接高亮命令与研究档案生命周期：支持“将 XX 对应区域高亮”的自然表达，避免结构化推断标签触发无意义的二次模型修复；工作台运行在执行前绑定当前研究会话，历史回填只接收已有结果的成功运行，失败运行不再生成空白对话。 |
| 2026-08-10 | 重构论文对话路由与命令边界：上下文模式改为显式结果；“高亮 XX”在检索前隔离旧话题，“把它高亮”才继承上轮目标；一个命令可生成多个经引用验证的公式/正文高亮动作。引用列表按 PDF 物理阅读顺序合并跨行原句，不再受模型引用插入顺序影响。 |
| 2026-08-11 | 修复“本文最重要的公式/方法/结果”等全文判断题的零召回：判断类问题进入跨章节综合检索；公式问题直接以带编号公式为结构锚点，组合所属章节与相邻解释段，不再要求中文问题字面命中英文正文；明确论文问题若仍无证据会在模型调用前终止。 |
| 2026-08-11 | 重构连续科研对话的上下文边界：所有通过门禁的轮次（包括普通对话）均持久化，历史展示与检索焦点继承分离；统一识别指代、省略、备选、原因和展开型追问，论文追问零证据时在模型调用前终止。 |
| 2026-08-11 | 采用新的 Research Assistant 品牌标识：从确认的 D3 高清稿精确提取带弧形长腿的衬线 RA 连字和蓝色研究焦点，生成透明底明暗版本并同步侧栏与 favicon；保留旧版资源以便回退。 |
| 2026-08-11 | 统一收起侧栏的品牌标识与导航图标视觉尺寸：D3 标识缩至 30px、导航图标调整为 20px；隐藏标签不再占位，针对字形偏左向右增加 1px 光学补偿，并移除展开/收起按钮的焦点白框。 |
| 2026-08-11 | 修复引用跳转与选区命令边界：公式引用的证明文字只用于展示，点击始终回到公式区域框；“将这段文字高亮”直接绑定本轮规范化选区并以零模型调用执行。PDFium 自绘选区补充 Ctrl+C/右键复制，任务中心按钮支持返回进入前页面，论文对话标题与历史区增加分隔。 |
| 2026-08-11 | 正式落地可信来源索引与命令优先执行：为编号公式建立“定义/提及”和“定理结论/证明步骤”关系，公式型全文问题优先召回定理结果，Evidence Gate 拒绝用证明中间式冒充定理结论；当前选区高亮绕过检索和模型并直接使用浏览器原始字符框。版面语义升级为 v4，真实双栏论文的式 (21)/(22)/(31)/(34) 已与相邻证明文字拆分。 |
| 2026-08-12 | 完成论文助手关键链路重构：建立七类统一轮次路由；上下文装配只消费该路由；编号公式按同页、同阅读流绑定 Theorem/Lemma/Proposition 所有权，再统一为文档来源索引；PDF 操作扩展为高亮、下划线、笔记、批注和跳转，并以确定性证据坐标执行。模型提示改为逐事实回答块与就近引用，不再向用户暴露内部证据/转写状态。 |

| 2026-08-13 | 对话运行改为服务端完成即归档：浏览器离开论文页面只会停止当前观察器，不会取消后台任务或丢失回答；重新进入后从研究会话恢复结果。对话历史新增“回到最新消息”按钮，内容/公式尚未选取时的占位区改为无装饰图标的两行紧凑提示。 |
| 2026-08-13 | 完成统一路由、来源定位与命令链路的必要加固：确定性规则优先、短歧义追问才使用低预算模型兜底；论文独立问题、上下文追问和普通对话共用同一权威路由。跨行来源保留多个物理框，引用按 PDF 阅读顺序编号；门禁会拒绝定理/引理结论与公式编号不匹配的回答。五类 PDF 命令均绕过回答模型执行，笔记/批注正文与目标分离，页码跳转无需伪造证据。 |
| 2026-08-13 | PDFBox 版面解析升级到 v2、语义层升级到 v5；数学字符破坏普通空白检测时，仅对“正文与带编号公式并排”的行启用栏间线拆分。16 页真实双栏论文回归由 2236 个块变为 2237 个块，式 (16) 不再把左右栏合成整页宽证据框，来源索引同时识别至少两条定理结果公式。 |
| 2026-08-21 | 冻结 Agent 重构 v0.9 方案并完成阶段 0：提交重构前基线 `3bdffec`，建立 Agent 轮次黄金集、指标门槛、零容忍停止条件和旧 Workbench/会话依赖清单。 |
| 2026-08-21 | 完成 Agent 重构阶段 1：新增轮次/运行/工具调用 Runtime、附件和版本化会话摘要，建立单活动轮次、条件状态迁移、配置快照和幂等工具调用边界；完整后端回归 598 项通过。 |
| 2026-08-21 | 完成 Agent 重构阶段 2：复用现有版面索引建立稳定来源对象与严格引用回绑；论文改为手动理解，三次失败后才开放本地受限降级，不引入 Embedding/RAG；完整后端回归 602 项通过。 |
| 2026-08-21 | 完成 Agent 重构阶段 3–5：贯通只读 Agent、自然语言澄清、严格 cite、五类 ActionTicket 页面操作，以及对话/文档双角色配置和分项能力探测。 |
| 2026-08-21 | 完成阶段 6 单写切换和清理：前端统一 Turn API，附件改为服务端暂存/认领；备份真实数据库后执行 V32–V36，论文 3、版面制品 3、标注 8 条迁移前后不变。 |
| 2026-08-21 | 完成阶段 7 自动回归：后端全量测试、前端 118 项单元测试及生产构建通过；真实供应商、陌生论文和页面操作保留为发布前人工验收。阶段实现保持未提交。 |
| 2026-08-31 | 按简化范围删除写作助手及已确认退役的研究自动化、导出、引用扩展和 RAG 诊断代码；增加 V39 清理对应数据表，保留论文研究页对比文献入口。后端 456 项测试通过、9 项跳过；前端 129 项测试及生产构建通过。 |
| 2026-08-21 | 修复重构后的两项界面回归：旧版面缓存不可读或 readiness 请求失败时仍稳定显示手动“开始理解”；API 设置改为对话/解析双段切换，每侧仅 URL、Model、Key，均可查询模型，协议由 URL 自动识别。真实 MySQL readiness 返回 200；后端 463 项、前端 119 项测试及生产构建通过。 |
| 2026-08-21 | 收敛模型配置交互：移除供应商预置选择遗留的模型下拉箭头，模型保持自由文本输入；查询结果改为输入框下方横向换行的可点击候选标签。弹窗与独立设置页行为一致，前端 119 项单元测试及生产构建通过。 |
| 2026-08-21 | 修复 Agent 人工验收首批运行问题：同步长请求改为“持久化受理后由专用线程池后台执行”，前端凭 `runId` 轮询并可从历史未完成轮次恢复，页面切换不再拥有或取消服务端运行；移除未判定上下文时误导性的“基于论文理解”标签。后端 465 项、前端 120 项测试及生产构建通过。 |
| 2026-08-21 | 修复 Agent 人工验收第二批问题：进入论文助手时自动恢复该论文最近活跃的对话，无历史才创建新对话，同时保留显式切换/新建入口；Runtime 强制每轮使用原生工具，消除普通问题因模型直接输出文本而发生的重复催促调用；Agent 证据补齐 `paperId`，旧消息缺少该字段时按当前论文兼容跳转。后端 465 项、前端 120 项测试及生产构建通过。 |
| 2026-08-21 | 修复 Agent 人工验收第三批问题：从文库重新进入已缓存的同一论文时重建助手会话面板并恢复最新对话；模型正文中的 `[1]` 等自编号在持久化前和旧消息渲染时清理，只保留系统生成的可点击引用；页面操作工具只在明确操作词出现时开放，并修复 ActionTicket 旧版本冲突、citation 轻微措辞差异误拒、累计 Token 预算与 8 轮上限不匹配及旧失败 run 被误恢复。真实原问题最终以 4 次模型调用完成且证据可跳转；后端 469 项、前端 122 项测试及生产构建通过。 |
| 2026-08-25 | 完成 Agent 稳定性架构修正：LangChain4j AI Services 接管模型—工具循环，Runtime 收敛为持久化、幂等、权限、证据和后台任务边界；只读工具压缩为按需 search/read/page，回答改为 answerBlocks 绑定已读来源并由服务端生成引用；补齐论文理解就绪门禁、失败分类和服务端超时收敛；公式证据统一为单个透明虚线框并在异常时退化到编号框。阶段定向后端 58 项、前端 40 项通过，完整回归结果见下方最新基线。 |
| 2026-08-25 | 修复历史失败 Run 的前端恢复死循环：移除 `running` 结束后反向刷新历史的递归触发，按 `runId` 去重自动恢复；`FAILED/CANCELLED` 终态只检查一次，网络中断和页面主动停止观察仍允许后续重连。用真实失败会话 35 验证 3.5 秒内消息数、pending 和错误状态保持稳定；前端 125 项测试及生产构建通过。 |
| 2026-08-25 | 修复失败提示跨对话泄漏和模型无界检索（阶段性止损，后续已由批量证据抽象替代）：错误按 `researchSessionId` 隔离，新对话不继承旧红字，切回失败对话仍可查看；原问题曾以 25.65 秒完成，4 次模型调用、6 次工具调用、2 条可点击证据。 |
| 2026-08-25 | 批量证据工具改造完成全量回归：后端 `mvnw.cmd clean test` 477 项执行、0 失败、0 错误、4 项跳过；前端 29 个测试文件、125 项单元测试通过，生产构建通过；未提交。 |
| 2026-08-25 | 按人工验收反馈将公式证据跳转改为编号锚点：Agent 证据透传 `formulaNumber`，前端在目标页搜索并高亮 `(21)`、`(1a)` 等编号；支持编号列表的子公式组，原公式坐标仅用于找不到编号时的滚动兜底，不再绘制覆盖正文的大公式框。新增公式编号解析测试；后端 `mvnw.cmd clean test` 477 项执行、0 失败、0 错误、4 项跳过，前端 30 个测试文件、129 项单元测试及生产构建通过，未提交。 |
| 2026-08-25 | 修复 Agent 回答展示与复合公式证据：回答块明确使用 Markdown/LaTeX，引用清洗保护数学区间与数学环境；同一回答块中同页同基础编号的子公式聚合为一个展示证据并可同时高亮全部编号；批量检索先保证各检索需求覆盖再按总分补充，避免单一高分需求挤掉其他证据。定向后端与前端回归通过，尚未提交。 |
| 2026-08-25 | 完成本轮修改验收：后端 `mvnw.cmd clean test` 共 479 项执行、0 失败、0 错误、4 项跳过；前端 30 个测试文件、131 项单元测试通过，生产构建通过；按项目脚本重启后端，health 返回 `UP`，未提交。 |
| 2026-08-25 | 按最终 Agent 方案完成收尾：能力探测改为诊断信息而非每轮硬门禁；LangChain4j 使用 `ToolChoice.AUTO`，回答/澄清共用 `submit_answer` 终止工具；普通问题允许直接回答，论文事实必须结构化绑定本轮已读来源；来源索引按 PDF 版本缓存，批量读取结果设有单次边界，上游过载映射为明确可重试错误。新增能力探测失败仍可直答、统一澄清协议回归；后端 `clean test` 共 491 项执行、0 失败、0 错误、4 项跳过，前端 30 个文件/131 项通过，生产构建通过；未提交。 |
| 2026-08-26 | 简化 Agent 回答协议：删除论文关键词硬门禁和“使用工具后必须结构化提交”的限制；普通 Markdown 始终可作为有效终答，`submit_answer` 仅在需要可点击引用时使用；论文读取异常作为 `unavailable` 工具结果回传，模型继续依据对话、选区和论文画像回答且不得伪造引用。定向 29 项及后端全量 492 项测试通过，0 失败、0 错误、4 项跳过；未提交。 |
| 2026-08-26 | 完成 Agent 功能层重构代码（未进入测评阶段）：拆分 `paper_overview` / `paper_evidence` / `page_action` 三个职责单一的 Skill；论文画像改为按需加载，普通对话不再无条件注入全量画像；合并所有 SystemMessage 以避免 LangChain4j 记忆覆盖；证据工具支持多查询/页面范围批量读取并将无结果作为非阻断状态；新增确定性画像质量校验和独立 `profile_quality_json`。本阶段仅完成功能代码与跳过测试的后端编译，未搭建测评器、未建立测试集、未运行功能或效果测试，未提交。 |
| 2026-08-26 | 按已确认的 API/附件方案完成实现：能力测试改为提交 URL、Model、临时 Key 草稿且不保存设置；文档角色继续强制图片能力并单独探测原生 PDF。论文对话附件上传原始 PDF、Word（doc/docx）和 JPG/PNG，后端用解析 API 处理，原生 PDF 不可用时回退为 PDF 页面图片，Word 提取正文/表格/内嵌图片后发送；文本附件和手动 LaTeX 保持轻量直注入。新增能力探测、Word/图片类型、解析 API 输入与 LaTeX 直通测试；定向后端测试与前端 132 项单测/生产构建通过。全量后端回归仍包含此前未修复的 9 项基线失败，未提交。 |
## 最近验证基线

- 当前测试库保留 15 篇测试论文的 PDF、论文记录、会话和用户标注；所有派生解析、画像、分析和索引均按现行代码重建，没有旧版本迁移兼容层。
- 论文理解使用一次整篇多模态请求；Kimi Coding 与 `kimi-k2.*` 已显式关闭 thinking。54 页论文 203 的诊断请求耗时 119.6 秒成功，当前论文请求默认上限为 150 秒；其余 12 篇（排除 184、185、203）均在上限内完成。
- 15 篇测试论文当前均为 `READY/usable=true`；确定性证据回归以论文 184、185、191、204 的 15 条样本为基线，Recall@4 为 15/15、locator 为 161/161。
- 三个标准 Skill 的简介、正文、工具 schema、系统提示和模型可见状态文案均已中文化；工具名、JSON 字段、状态枚举、来源 ID 与 LaTeX 保留协议形式。
- Python 环境已具备 PyYAML；使用 UTF-8 模式运行 `skill-creator` 官方 `quick_validate.py`，`paper-profile`、`paper-evidence`、`paper-action` 均通过。
- 当前后端全量回归为 487 项执行、0 失败、0 错误、18 项跳过；旧 RSMA 与 2026-08 解析快照评测文件已清理。

## 2026-09-06 标准 Agent Skills 渐进式披露（当前未提交）

- 引入 LangChain4j 官方 `langchain4j-skills`，从三个独立 Skill 目录加载 `name`、`description` 和 `SKILL.md`；元数据始终可见，完整说明只在 Agent 调用官方 `activate_skill` 后进入上下文。
- `paper-profile` 只负责整篇论文画像，`paper-evidence` 负责批量证据检索与按需局部图片，`paper-action` 负责一个明确页面操作；业务 Java 类作为激活后的宿主工具，不再伪装成 Skill 脚本或由后端按问题编排调用顺序。
- 当前运行内由 LangChain4j 记忆自动回灌 Skill 指令和工具结果；跨回合仅持久化并恢复最近有效的官方激活 transcript，摘要压缩后 Agent 可自行再次激活缺失的 Skill。Runtime 只做通用持久化、幂等、安全与终态支撑。
- 画像结果保留有效的候选来源 ID，证据结果将局部视觉内容传递给下一次模型请求；页面操作仍只接受可信来源 ID，不接受模型生成坐标。未新增 Skill 脚本、论文专用调用门禁、固定调用顺序或额外模型。
- 聚焦回归与后端全量回归通过：474 项执行、0 失败、0 错误、18 跳过；前端 29 个测试文件共 129 项通过，生产构建成功；`git diff --check` 通过。
- 三个标准 Skill 的元数据简介统一改为中文，并同步更新元数据加载测试断言；`AgentSkillRegistryTest` 4 项通过。证据检索重构仅完成现状分析与方案讨论稿，本轮尚未实施。

## 2026-09-07 测试论文刷新与中文提示收尾（当前未提交）

- 按用户授权清理当前测试库 15 篇论文的所有派生理解数据，保留论文记录、PDF、会话和用户标注；不新增旧数据迁移或兼容逻辑。
- 曾在 Thinking 修复前把 `paper-memory-whole` 的输入字符预算误当作时间预算，并同时临时放宽客户端超时与输出上限；该实验已撤回，输出上限仍为 10,000，实验结果不作为可接受基线。现已将论文请求超时独立配置化，便于按长论文实测值调整。
- 统一 API 配置是既定设计；回归定位到 Kimi Coding 论文请求漏传 `thinking={"type":"disabled"}`。已在统一论文模型构建中对 Kimi coding 与 `kimi-k2.*` 显式关闭 Thinking，并增加工厂层参数回归测试。
- 受控串行验证：5 页论文 184/185 分别约 43.1/32.2 秒模型请求，输出 1,658/1,332 tokens，均 `READY`；15 页论文 191 约 44.6 秒模型请求、1,624 输出，亦 `READY`。随后按同一限制串行刷新全部 15 篇，14 篇 `READY`，仅 54 页论文 203 在单次 90 秒 HTTP 边界超时，未产生模型 tokens；该失败单独作为超长输入边界，不归因于 Thinking 或证据 Skill。
- 14 篇成功样本的整篇模型请求耗时 P50 约 44.6 秒、最大 84.3 秒；没有通过提高超时或输出上限取得该结果。
- Thinking 修复后的后端全量回归：479 项执行，0 失败，0 错误，18 项跳过；`git diff --check` 通过，未提交 Git。
- 补充长论文诊断：Kimi Thinking 关闭后，仅将 54 页论文 203 的请求窗口临时放宽至 600 秒，整篇模型请求 119.6 秒、总流程 123.1 秒，`READY/usable=true`；最终将 `app.ai.paper-request-timeout-seconds` 默认设为 150 秒。随后排除 5 页论文 184/185 与 54 页论文 203，对其余 12 篇清理派生数据并串行重跑，模型请求均在 150 秒内完成（最慢论文 202 为 123.8 秒），解析制品、理解画像和分析投影全部生成且 12/12 `READY`。
- 统一检查并翻译模型可见的 Skill 文本、工具 schema、系统提示、附件/公式理解提示和工具错误为中文；工具名、JSON 字段、状态枚举、来源 ID 与 LaTeX 保留为协议标识。
- 当前 Python 环境已具备 PyYAML，并以 UTF-8 模式运行 `skill-creator` 官方 `quick_validate.py`，三个 Skill 均校验通过；删除旧 RSMA 与 2026-08 解析快照评测文件，当前验收文档只保留现行协议。

## 2026-09-08 证据 Skill Need 契约与闭环重构（当前未提交）

- Need 增加必填中文 `objective` 和至少一种检索锚点；运行时对字段、长度、枚举、数量、未知来源与画像 claim 进行结构化校验，claim-only 与 source-only 可直接读取。
- 进度改为逐 Need 的 `new_sources / no_match / same_sources / duplicate_request / need_stopped` 与明确下一步动作；事件流只暴露状态、计数和错误码，不泄露论文原文。
- 真实 Agent 失败轨迹推动加入最小硬保护：首次有效检索后冻结 Need ID 集合，停止态不再执行底层检索，同一 Need 连续两次输入校验失败后转入回答；未增加全局固定检索轮数或第二判断模型。
- 四论文 15 条确定性样本保持 Recall@4 15/15、locator 153/153、p95 70ms、响应均小于 16 KiB。真实 Agent 串行验证的公式、TD3、最差用户公平性、91.48% 性能提升和无答案问题 5/5 完成并完成来源回绑；并发供应商超时单独记录，不计入 Skill 正确率。
- 后端全量回归 487 项执行、0 失败、0 错误、18 项跳过；三个 Skill 的官方校验通过。
- 全论文真实 Agent 双重验收完成：15 篇论文均为 `PROFILE_READY`；清理旧 263 个研究会话后，逐篇执行 3 个中文问题，共 45 个计划问题。首轮 44/45 完成，184 第三问重试后成功，最终 45 个回答全部可查看；记录 1 次模型超时、20 次结构化输入纠正和 26 次停止保护，未出现工具调用失败。新验收会话为 302～316，未提交 Git。
- 本轮不提交 Git。

## 2026-09-08 证据完整性与跳转修正（当前未提交）

- 证据返回补齐完整原文 `fullText`、稳定物理证据键、内容类型、文本格式和可靠性标记；短 `quote` 仅作为摘要预览，不再作为唯一证据内容。
- 跨栏断行正文按严格的同页、同阅读流、连字符续写规则合并，同时保留每个物理 block 的定位器；前端检索和跳转逐个定位物理框，不再用跨栏联合大矩形覆盖空白区域。
- 证据去重改为物理身份去重：同一论文版本、完整文本和同一组页面几何定位的重复来源折叠；不同页的同文内容保持独立。
- 公式来源透传 LaTeX 与可靠性；文本提取不可靠或视觉回退的公式显示明确提示并依赖页码/公式编号定位，不把乱码当作可读公式展示。
- 共享证据视图映射同时用于实时回答和历史恢复；后端全量回归 492 项执行、0 失败、0 错误、18 跳过，前端 29 个文件/132 项单测和生产构建通过，三个 Skill 官方校验及 `git diff --check` 通过；论文 204 真实 API 冒烟验证完成。
- 随后的真实浏览器验收发现文本跳转路径存在 `exactBoxes` 未定义的前端回归：点击只翻页而不绘框，公式因提前返回未受影响。现已恢复逐 locator 文本定位，并在论文 204 第 11 页实际观察到跨左右栏的 12 个文本框。
- 会话 302～316 保存的是改造前的旧版证据摘要，历史载荷不含 `fullText`，不能在前端还原完整段落。旧会话现明确标注“只保存了证据摘要”，并以页面几何身份折叠同物理证据；新会话 318 已实测返回并在刷新后恢复 1710 字完整段落。
- 新版“查看依据”可展开完整 `fullText`；完整依据默认收起。跳转等待目标页挂载并重试冷启动公式搜索，论文 204 的正文、跨栏正文和公式均在首次点击后出现定位框。
- 后端新增完全相同引用绑定去重，保留同一来源在不同回答区间的合法复用。后端全量回归 493 项执行、0 失败、0 错误、18 项跳过；前端 29 个文件/135 项单测和生产构建通过。

## 2026-09-09 双问题证据人工验收批次（当前未提交）

- 按用户授权删除此前 17 个活动研究会话及其消息（会话 302～319）；删除后活动会话与归档会话均为 0。论文记录、PDF、解析制品、画像、分析投影和用户标注未删除。
- 为测试库 15 篇论文分别新建一个独立会话（320～334），每篇只调用一次真实 Agent，发送问题“论文主要讲了什么？核心创新点是什么？”。15/15 Run 均为 `COMPLETED`，无重试、无超时；每个会话恰有 1 条用户消息和 1 条助手消息。
- 结构核验：所有新会话的问题文本完全一致；有引用的 10 篇论文共保存 148 条引用、133 条去重后的证据，全部具备 `fullText` 和 locator，未发现重复 `evidenceKey`。P185、P192、P193、P195、P201 的回答未产生引用，保留原始结果供人工判断，不将“无引用”直接判定为内容错误。
- 本批次只验证真实回答与证据展示入口，不替代用户对论文内容、证据完整性和跳转框选的逐篇验收；本轮未提交 Git。

## 2026-09-09 Agent 超时与公式证据收敛（当前未提交）

- Agent Run 创建后先保持 `QUEUED`，由后台 worker 开始执行时再转为 `RUNNING` 并写入 `started_at`；超时扫描分别处理排队超时（`QUEUE_TIMEOUT`）和运行超时（`RUN_TIMEOUT`），不再把线程池排队时间误算为模型运行时间。失败事件额外暴露错误码，便于区分排队、运行和模型请求失败。
- 不可靠公式不再把“文本提取不可靠”等内部诊断文案发送给模型或展示在“查看依据”中；工具与引用视图只返回“公式 (编号)”/“公式区域”标签，具体表达式需要 `includeVisual=true` 后依据局部图像核对。可靠 LaTeX 展示路径保持不变。
- 无编号且仅由孤立数学 glyph（如单个“√”）组成的低质量公式不进入可检索/可引用来源；有编号的视觉公式仍保留编号定位能力。
- 来源目录在检索前按同论文版本、同页、同公式编号和重叠物理区域折叠公式实体与视觉恢复重复项；正文仅折叠同文本且物理区域重叠的重复项，不合并其他页面的同文证据。Ground Evidence 增加同一公式的二次去重保护。
- 定向后端测试通过；前端证据/公式单测 23 项通过，生产构建通过；未执行全量回归、未提交 Git。

## 2026-09-09 证据链修复验收批次（当前未提交）

- 在修改前建立基线提交 `84dc22a`（`feat：strengthen evidence chain`）；本轮修复保留为未提交工作区，便于人工验收后继续调整。
- 文本证据跳转优先使用后端返回的全部物理 locator 框；PDFium 搜索仅在缺少 locator 时降级使用，避免长证据只命中前缀或中间一行。新增前端选择框优先级单测。
- 语义分片、来源目录和引用回绑增加独立字符/短 OCR token 过滤；画像 claim 的来源改为 `candidateSourceObjectIds`，不再自动进入本轮或历史会话的引用允许集合，必须通过 `paper-evidence` 实际读取后才能引用。
- 定向后端测试 31 项通过，前端引用/Agent 测试 25 项通过，前端生产构建通过；未运行全量回归。
- 新建验收会话 335～338，对 184、185、192、197（均不超过 10 页）各提交一次问题“论文的核心创新点是什么？”。四次真实 Run 均 `COMPLETED`；197 返回 5 条引用/5 条证据，其余三篇由 Agent 仅使用画像完成回答且无引用，均保留供人工验收。

## 2026-09-10 证据链最小闭环修复（当前未提交）

- 证据目录保留完整来源文本与全部物理 locator；正文跳转优先使用全部 locator 框，公式同时区分完整公式区域和编号 focus 区域，避免用局部文本命中替换完整框选。
- 物理公式候选优先按实际版面位置确定，语义定理/证明关系只保留为元数据；孤立数学 glyph 不再作为可引用证据，重复来源按论文版本、文本和重叠几何折叠。
- 证据 Skill 增加候选分页 `cursor/nextCursor/hasMore`、逐 Need 进度和明确停止范围；Need 至少提供一种检索锚点。模型读到证据后若直接输出普通文本，执行器最多补发一次结构化 `submit_answer` 提示，不重复检索。
- 候选分页按本次实际暴露的第一个未读候选推进；多 Need 共享来源或同时请求页码范围时，不会因全局 `maxEvidence` 裁剪而跳过候选或挤掉 Need 的首个来源。
- “查看依据”优先显示完整 `fullText`，短 `quote` 仅作为回答标记；三个 Skill 与模型可见协议继续保持中文。前端 29 个测试文件、139 项单测及生产构建通过；后端聚焦证据/协议测试通过，完整回归已在完整 JDK 17 环境复测通过。

## 2026-09-10 结构化答案终态修复（当前未提交）

- 修复跨轮历史证据被模型直接写成普通文本的问题：只要 `submit_answer` 可用，LangChain4j 执行器就要求正常回答以该工具终止；模型首次返回普通文本时最多进行一次结构化提交恢复，恢复仍未提交则以 `ANSWER_SUBMISSION_REQUIRED` 失败，不落库无引用答案。
- `submit_answer` 和答案 Schema 明确允许复用当前论文版本的历史证据来源；一般知识回答提交空 `sourceObjectIds`。补充执行器和错误分类回归，未改变证据检索、解析或前端跳转逻辑。

## 2026-09-10～09-11 阶段 0 稳定验收基线（当前未提交）

- 新增运行时生成的单页固定 PDF 样本：同一证据包含两行正文和两个物理框，并带一个可按编号定位的公式，不写入正式论文目录。
- 新增浏览器级确定性 API 适配层与完整 Playwright 链路：文库导入 → 论文页/PDF → 就绪状态 → Agent 回答 → 完整依据 → 多框跳转 → 刷新恢复；每次清空浏览器存储，所有 `/api/` 请求均由适配层处理，未知请求直接阻断。
- 固定样本补充带编号公式；适配层记录并校验“加载画像 → 合法 Need → 证据读取 → submit_answer”的最小轨迹，自动覆盖正文多框和公式编号跳转。
- 新增 `backend/src/test/resources/application-phase0.yml`，并将现有 `test` profile 的派生文件目录改到临时路径，为后端阶段测试提供独立 H2 内存库、随机端口和临时 PDF/图片/附件目录，不触碰正式 MySQL 与 `data/`。
- Playwright 失败时保留 trace、截图、视频，并附加 API 轨迹和控制台错误；增加 `scripts/test-phase0.cmd`，可自动连续执行三次。
- 增加 `scripts/test-phase0-headed.cmd`，并完成一次可见浏览器人工验收（1/1）。
- 阶段 0 浏览器基线 3/3 通过；前端 139 项单测、生产构建通过；证据/Agent 相关后端定向 44 项通过。
- 安装完整 Temurin JDK 17（含 `javac`、`freetype`、`javajpeg`）后完成后端全量回归：507 项执行、0 失败、0 错误、18 项按条件跳过。

## 2026-09-11 阶段 1 阅读状态、看板和基础页面状态（已提交 `24fa652`）

- 统一阅读状态语义：首次成功打开 PDF 后由 `UNREAD` 进入 `READING`；只有用户明确标记才进入 `READ`；重新打开已读论文和翻到末页均不会降级或自动完成。
- 新增独立阅读状态接口和文库“标记为已读”入口；PDF 仅在 PDF.js 与 PDFium 均成功打开后写入阅读进度，加载失败保持未读。
- 看板补充未分类论文统计，并将旧版 `task_type` 为空的失败记录与当前仍失败任务分开显示；阅读时长文案改为“阅读时长”。
- 看板、文库、论文页和研究档案补充可见的加载失败状态与重试入口；文库从论文页返回时刷新状态，避免 KeepAlive 缓存造成显示滞后。
- 验证：后端全量测试 511 项执行、0 失败、0 错误、18 项按条件跳过；前端 29 个单元测试文件、141 项通过，生产构建成功；阶段 1 浏览器测试 1/1 通过，全部 E2E 3 项通过、1 项按条件跳过；`git diff --check` 通过。
- 阶段 1 修改已提交；主页与阅读状态已人工验收通过，代码审查无阻塞，可进入阶段 2。

## 2026-09-11 阶段 2 研究档案证据复现（当前未提交）

- 研究档案列表改为分页接口，返回 `records/total/current/size/pages`；前端支持切换页码、空页回退和加载失败重试，论文助手会读取当前论文的分页记录。
- 新增持久化消息回放适配层，将历史 `citations/evidence` 还原为实时消息形状，并继续复用 `answerCitations` 与 `evidenceViewModel` 的编号、去重、完整文本、公式可靠性和 locator 规则；从 PDF 返回档案时一次性恢复页码、论文选择和搜索条件。
- 档案详情复用论文助手的依据列表组件；有完整 locator 才显示历史跳转按钮，缺少可靠定位时仍可阅读已保存内容但不伪造框选。跨论文跳转通过会话存储暂存完整 locator，进入 PDF 后消费并执行页码与多框定位。
- 后端消息视图补充消息类型、终态和证据 schema 版本；档案只展示最终用户消息，不展开内部 ToolCall 记录。
- 新增分页服务/控制器测试、历史消息与跨页面 locator 单测、档案回放分页组件测试，以及阶段 2 Playwright 链路（完整依据、正文多框、公式编号和第二页历史会话）。
- 验证：后端全量测试 512 项执行、0 失败、0 错误、18 项按条件跳过；前端 30 个单元测试文件、148 项通过，生产构建成功；全部 E2E 4 项通过、1 项按条件跳过；阶段 2 专项 E2E 通过；`git diff --check` 通过。本阶段修改未提交。

## 2026-09-11 阶段 3 文库文件与数据一致性（当前未提交）

- 上传 PDF 先落在论文存储目录内的临时文件，经 PDFBox 实际打开并确认页数后再移动为唯一正式文件；空文件、损坏文件、伪装扩展名和无法打开的加密文件均返回明确中文错误。
- 新建和覆盖论文统一使用新文件路径；数据库写入失败或事务回滚会清理新文件，覆盖提交后再清理旧 PDF 及其派生图像，失败时保留旧文件。arXiv 下载同样使用 PDF 校验、临时文件和唯一文件名，异步写库失败会清理新文件。
- 论文普通编辑请求不再接收服务端管理的阅读进度、页数、置顶和 PDF 路径；阅读状态、获取方式、标题和文件夹引用在服务层校验，标签绑定在替换前校验论文及全部标签引用并去重。
- 删除研究会话时，在数据库提交后清理该会话的暂存/归档 Agent 附件物理文件，并保留路径越界保护。
- 导入链路增加轻量论文结构审查：明显由图片/流程图短标签组成的非论文 PDF 会被拦截；无法判断的扫描版或特殊排版保留为待确认状态；前端文件选择仅接受 PDF，后端仍以真实 PDF 内容为准。
- 新增文件生命周期、覆盖回滚、论文删除、标签引用、附件清理和请求契约测试；增加阶段 3 浏览器级非法上传、覆盖失败和删除一致性测试。
- 验证：后端全量测试 528 项执行、0 失败、0 错误、18 项按条件跳过；阶段 3 后端定向测试 29 项通过；前端 30 个单元测试文件、148 项通过，生产构建成功；全部 E2E 8 项中 7 项通过、1 项按条件跳过；`git diff --check` 通过。
- 空 MySQL 独立验收库由 Flyway 从空库初始化到 v41，完成论文创建/更新/列表读取、研究会话创建和删除，随后已删除该临时库及文件目录；未触碰现有论文库和正式存储目录。
- 补充论文结构审查后，后端全量回归为 533 项执行、0 失败、0 错误、18 项按条件跳过；前端单元测试 148 项通过，生产构建成功；`git diff --check` 通过。
- 修正 PDF 文件输入 E2E 定位器后，阶段 3 专项 E2E 3/3 通过，全量 E2E 仍为 7 项通过、1 项按条件跳过。

## 2026-09-11 阶段 4 论文助手和三个 Skill 的稳定性闭环（当前未提交）

- 论文产物读取改为绑定当前 PDF 哈希、解析器版本和语义/数学版本；论文覆盖上传会重新进入 `PENDING`，并清空旧 PDF 派生摘要，避免旧解析和画像继续被使用。画像工具、证据候选映射和理解状态查询也拒绝旧理解版本或旧 PDF 版本。
- 论文助手正常回答统一经过 `submit_answer` 终态；普通知识回答提交空来源集合，结构化提交失败不再静默落库为普通文本。
- 增加回答取消闭环：服务端持久化 `CANCELLED`、取消未完成工具调用、尽力中断后台 Future 并写入一次状态消息；前端运行中发送按钮变为停止按钮，取消结果可刷新恢复且不会被迟到结果覆盖。
- 操作票续期读取当前 `paper_action` 的 `actionType` 参数；上下文不重复注入运行状态消息。三个 Skill 仅补充中文的调用边界、去重、完整依据、停止判断和真实客户端回执要求，不改变现有工具或数据架构。
- 新增版本一致性、结构化答案、取消、操作票续期和前端停止入口回归测试；三个 Skill 使用官方校验脚本验证通过（开发环境补装 PyYAML）。
- 验证：后端全量测试 552 项执行、0 失败、0 错误、18 项按条件跳过；前端 30 个单元测试文件、150 项通过，生产构建成功；全部 E2E 8 项中 7 项通过、1 项按条件跳过。本阶段修改未提交。
- 在现有 Agent 链路上增加 `groundingMode`，明确区分论文依据、通用知识和混合回答；`PAPER` 回答块必须绑定已读取来源，阻止论文画像单独成为事实答案。
- 三个 Skill 按“画像规划—证据验证—结构化提交—明确操作”统一边界；补充创新点、公式和图像 Need 的关键决策示例，不增加新检索字段或第二个判断模型。
- 图像来源继续使用 caption 作为语义索引；PDFBox 未产生 FIGURE block 时，在同栏 caption 上方建立保守的实际页面视觉区域，供 `includeVisual=true` 裁剪和跳转，不对图像内容做确定性猜测。
- 修复图像来源在目录去重阶段被普通文本来源删除的问题：图表图注归结构化 FIGURE/TABLE 来源所有，跨类型物理去重不再互相删除；同时排除“Fig. 5 and Fig. 6, respectively”一类正文引用。论文 201 真实产物回归显示 12 个 FIGURE 来源、图 2/图 5 均保留图像区域与完整图注，重复图注文本为 0；真实 PDF 裁剪及 `PaperEvidenceSkillTool → FIGURE → visualSources` 链路验证通过。
- 证据跳转在目标页虚拟化挂载前保留定位框，页面 viewport 就绪即可绘制，避免等待画布和文本层全部完成后才出现框选。
- 真实 API 抽检中，184、191 的核心创新回答能够经过画像、证据和 `submit_answer` 完成；201 首轮直接输出普通文本的问题已通过“零工具时重新进入完整 Skill 链路”的恢复提示修正。
- 精确公式抽检暴露同一 Need 连续扫描和正文重构公式问题：现限制单 Need 为首次加最多三次有效补检索；不可靠公式自动附加局部图像，完整展示公式必须绑定可靠公式或已读公式图像。191 复测在四次检索后主动省略了未能确认的精确表达式。
- 图像链路已补充 FIGURE 来源自动视觉读取、一次 Need ID 纠正机会和最终 FIGURE 引用约束；论文 201 的真实来源目录现可命中 FIGURE。图像主体区域仅用于模型视觉读取，用户可见定位框改为覆盖完整图注；真实 PDF 的图 2 三行图注由两个连续版面块共同定位，近邻分析正文不被并入，图注也不再重复生成 TEXT 证据。
- 证据跳转新增纯显示层几何：同页、同栏且相邻的多行物理框合并为一个完整可读框，原始 locator 仍用于引用身份和页面操作；跳转目标会在页面渲染前预置，避免先翻页、长时间空白后才出现框。
- FIGURE 的模型视觉裁剪同时覆盖图像主体和完整图注，Skill 与模型视觉提示要求逐字核对图号、图题和变量；用户侧跳转仍只框图注，并将 FIGURE 依据标记为“图”。完整依据默认收起。
- 修复页面操作卡死：票据签发已经把工具调用置为 `WAITING_CLIENT`，Agent 不再重复执行非法状态迁移；前端在等待回执期间继续轮询并即时派发操作，运行中的发送按钮切换为停止按钮，状态文案按排队、思考、页面操作变化。
- 同一种高亮或下划线可在一次 `paper_action` 中提交多个已读取来源；每个物理目标仍使用独立票据和回执，全部完成后 Run 才结束，首目标票据也支持续期。
- 修复 Agent 批注坐标错位：`PDF_NORMALIZED` 统一按左上角原点转换，服务端生成的 quad 统一为文本下边优先；下划线按渲染后的下边绘制，并在客户端回执中携带实际矩形、由服务端与可信来源校验。既有错误批注无需迁移，重新渲染即可使用同一坐标契约。
- 下划线增加不改变视觉效果的 14px 透明命中带，解决细线过窄、难以选中的问题。
- 文本标记端点调整改为优先解析 PDFium 字符锚点并复用真实鼠标选字范围；服务端生成的旧式标记会按页内文字和原矩形补建锚点，解析失败时按视觉行和栏边界裁剪，支持换行且不会把端点拖进另一栏。调整保存失败会恢复原几何和锚点。
- 公式下划线标记为 `FORMULA_REGION`，渲染时对完整公式区域取最底部坐标绘制单条下划线；公式编号的跳转焦点仍独立保留。公式来源索引回归覆盖分式/多行公式的最低行。
- 坐标回归验证：后端 `AgentActionReceiptServiceTest`、`AgentLoopServiceTest` 通过；前端坐标、Agent 状态和页面操作相关 53 项单测通过；前端生产构建成功。
- 验证：后端全量 559 项执行、0 失败、0 错误、18 项按条件跳过，修改后的页面操作/来源索引定向测试通过；前端 31 个测试文件、162 项单测和生产构建通过；`paper-evidence`、`paper-action` 官方 Skill 校验及 `git diff --check` 通过。
- 阶段 4 主体代码已经完成，但仍需要重启前后端后进行真实 API 代表样本复测，并人工验收图注框、图题、取消按钮和页面操作回执后才能结束阶段。

## 2026-09-12 阶段 4 页面操作多操作与精确坐标修补（当前未提交）

- `paper_action` 增加 `operations` 数组；一次请求可同时提交不同类型的页面操作，服务端仍按物理目标分别签发票据并等待全部回执，完成消息会汇总各类操作。
- Agent 页面操作目标携带可信原文和精度；前端对文本高亮/下划线按 PDFium 字符范围重新选择并把实际矩形、字符锚点回传，服务端校验其仍位于可信来源范围后保存实际几何；公式区域继续使用原公式区域。
- 算法来源分组提高候选上限，并拒绝跨双栏的 FULL/UNKNOWN 伪合并块，避免右栏正文进入左栏算法证据；新增异构操作和跨栏算法回归测试。
- 修复真实运行中 `paper_action` 顶层 `anyOf` 无法被 LangChain4j 1.15.1 转换的问题：工具定义改用受支持的对象 Schema，单项/多项二选一继续由运行时严格校验，并增加真实工具定义转换测试。
- 已验证：新增真实 Schema 转换测试及相关页面操作定向测试通过；后端全量 562 项执行、0 失败、0 错误、18 项按条件跳过；前端生产构建和 `git diff --check` 通过。后端已重启且健康检查为 `UP`，尚未提交，待人工页面操作验收。

## 2026-09-13 页面操作失败的通用修复（当前未提交）

- 论文理解恢复验证：统一模型能力探测改为明确要求调用测试工具，避免 Kimi 关闭思考时因 `AUTO` 未主动选工具而误判并阻断理解；能力刷新后论文 201 的 v6 版面重新理解成功，状态 `READY`。

- 文本高亮/下划线不再把可信来源框压成一个首尾字符区间。PDFium 新增按每个可信矩形筛选 glyph、按连续字符范围读取文本并按视觉行生成实际矩形；双栏页面不会把另一栏字符带入回执，公式区域路径保持不变。
- 页面操作批次在客户端回执失败后立即停止剩余票据，避免首项失败时继续产生后续标注副作用；普通对话、证据检索和结构化回答不经过该分支。
- 算法来源同页候选不再受固定标题距离截断，改为扫描同页并沿版面栏位、结构标题和自然间距停止；继续拒绝跨栏伪合并块，避免长算法证据缺行或混入右栏正文。
- 新增 PDFium 双栏几何选择单测和长算法来源边界单测。验证：前端全量单测 31 个文件、163 项通过，生产构建成功；后端全量 559 项执行、0 失败、0 错误、18 项按条件跳过；`git diff --check` 通过。后端已重启且健康检查为 `UP`，未提交，待人工验证混合高亮/下划线请求。
- 真实大区域高亮在标注持久化阶段失败：字符定位信息与显示几何已解耦，跨 PDF 文本 run 的 glyph 按视觉行压缩后再显示和保存，回执不再重复携带大量原始子范围。真实运行确认 Agent 选中了正确的算法/公式来源；算法 2 过宽来自来源把间隔后的解释段并入算法，现以“已形成过程块 + 明显纵向间隔 + 转回普通叙述”作为通用结束边界。编号公式若上下文只识别到编号基线，会继续收集附近数学块以覆盖分子和分母；公式初始下划线位于完整区域底部，拖动后转成 PDFium 文本选区，支持与手动标记一致的换行调整。验证：后端全量 565 项执行、0 失败、0 错误、18 项按条件跳过；前端全量 31 个文件、164 项通过，生产构建及 `git diff --check` 通过。
- Agent 页面标记收敛为两条简单路径：公式初始使用解析区域计算底部下划线，手动调节后回到自然文本选区；其余 Agent 高亮/下划线不再使用矩形筛选几何，改为与鼠标拖选相同的 PDFium 首尾字符选区。
- 真实运行中算法 1 + 公式 12 操作成功，算法 2 高亮因矩形边界 hit-test 吸附到可信来源外的字符而被服务端正确拒绝。现改为先筛出可信矩形内的 PDFium glyph，仅取其首尾字符作为自然选区端点，不放宽服务端范围校验。
- 开启服务端精确诊断并亲自复现后确认：PDFium 选中的失败行位于算法 2 左栏且夹在两个可信块之间，实际内容未越界，是解析 locator 漏行与“每行必须直接重叠”校验冲突。校验现要求自然选区首尾必须直接命中可信来源，仅允许同栏、同一纵向跨度内的中间行跨越解析空隙；跨栏或超出首尾仍拒绝。论文 201 真实执行“将算法 2 标亮”已成功并保持在左栏算法主体内；后端全量 563 项执行、0 失败、0 错误、18 项跳过。
- 修复公式编号串联：支持“公式 12”等中文编号表达；明确公式编号检索只接受来源自身的 `formulaNumber`，不再把邻近章节路径中的数字当作命中；带等式和编号但被 PDFBox 标成标题的行改归为可寻址公式，并通过语义版本 v6 使旧版面制品自动失效。公式区域下划线在完整区域最低边缘之外增加小幅显示间距，避免穿过分数/根号等视觉内容。后端全量测试、前端全量 164 项单测通过，未提交。
- 修复相邻编号公式的物理碎片串联：公式区域构建按同页同栏的最近编号公式分配未编号数学块，同一物理块不得同时归属于多个公式；无法明确归属时不跨公式合并。真实论文 201 第 5 页的公式 12 区域已收敛为 `y=0.243224–0.281763`，公式 13 区域为 `y=0.292410–0.356912`，两者不再重叠；新增相邻公式回归测试，相关后端测试通过。
- Agent 自动下划线统一改为可信来源范围内的 PDFium 普通文字选取；公式来源仍保留证据跳转和公式高亮区域模式，但下划线回执明确保存为 `TEXT_RANGE`，不再使用公式区域底部计算线。后端回执几何回归、前端全量单测和生产构建通过。

## 2026-09-13 阶段 4 代码验收与 8 篇真实 API 测试（当前未提交）

- 代码验收：后端全量 568 项测试通过，前端 31 个文件、165 项单测通过，生产构建成功；Playwright 端到端 7 项通过、1 项按真实 PDFium 运行时条件跳过。同步两处已过时的“多子框”断言，使其与当前相邻行合并为一个完整显示框的行为一致。
- 清空全部旧论文理解、旧画像和对话运行数据，保留 PDF 与文库元数据；选择 184、197（2 篇 10 页以下），191、193、194、201、207（5 篇 10～20 页）及 203（54 页）强制重建解析和理解。8/8 解析成功，版面质量分约 0.957；8/8 理解进入 `READY`，54 页论文理解约 106 秒。
- 8 篇各执行三个真实问题。前两问共 16 次，15 次完成、1 次失败；已完成回答的证据 ID/证据键重复数、缺失引用绑定、空全文或空定位器均为 0，且没有输出“文字提取不可靠”类确定性诊断文案。论文 203 的第二问由 Agent 总运行预算 90 秒终止，错误为 `RUN_TIMEOUT`，与论文理解 HTTP 150 秒上限不是同一配置。
- 8 次图片请求均签发单个 `HIGHLIGHT` 操作，并经真实浏览器 PDFium 执行、回执和持久化；每篇仅产生 1 条 AI 高亮，无重复标注和 `WAITING_CLIENT` 残留。测试同时发现：184、193、194、197、201、203 的目标为图注；191 与 207 的 `FIGURE` 来源实际是讨论图的正文段，因此高亮了解释文字而非图注，需作为后续图像来源质量问题处理。
- 真实恢复测试暴露过期操作票据会永久卡在 `WAITING_CLIENT`：后端已有续签接口，但前端未调用且未传递 `runId`。现在恢复/实时派发均携带 `runId`，票据临近过期时先续签，然后继续原 PDFium 执行和回执链路；真实验证 8/8 过期操作均成功恢复。
- 已二次审计并细化阶段 4 Harness 方案：完整会话与模型视图分离；输入、选区和附件设置单对象安全边界，正文选区超限统一提示“选取内容过长”；每次模型调用前按统一总预算、优先级和原子轮次重新装配上下文。画像、证据、Need 进度和 Skill transcript 只在当前 Run 使用，跨轮保留最近完整轮次、结构化历史摘要及最小来源索引。
- 历史压缩改为带版本保护的异步模型增量摘要，不再采用确定性拼接；数据库仍保存完整会话。初始上下文软/硬上限为 12k/16k Token，并受实际模型窗口约束；Agent 正常目标为 5 次模型/7 次工具调用，硬上限为 7/10，证据检索最多 3 次。先通过观测和真实 API 回归评估 90 秒总时限，不直接放宽；暂不引入向量检索。

## 2026-09-13 阶段 4.4 上下文 Harness 精简（当前未提交）

- 前后端统一限制普通输入 2,000 字符、正文选区 4,000 字符、公式选区 2,000 字符；超限明确拒绝，不再静默截断。每条消息最多 2 个附件、合计不超过 10 MB，并按图片、文档和文本类型执行 5/10/1 MB 上限；对话 PDF 附件最多 20 页，解析结果单个最多 3,000 字符、合计最多 4,000 字符。
- 上下文装配升级为 v3：稳定系统前缀与动态论文身份分离；当前问题、有效选区和附件作为必需项；最近对话按完整用户—助手轮次原子装配，通常保留 1～3 轮、短内容允许 4 轮。软/硬输入边界为 12k/16k 估算 Token，必需项或最近完整轮次超限时明确拒绝，不发送残缺上下文。
- 完整论文画像、证据返回、Need 状态和 Skill 激活 transcript 不再跨轮恢复。最近回答仅附带去重后的最小来源句柄，句柄只用于指代理解，重新引用必须在当前 Run 再次读取；上下文快照记录总估算量、分类型估算量、保留/淘汰轮次和摘要版本。
- 旧历史由关闭思考的文档模型异步生成六字段结构化增量摘要，回答主链路不等待摘要；数据库原始消息保持完整。摘要任务以会话摘要 revision 做并发保护，失败或过长时保留上一版摘要和未覆盖原始轮次，不以确定性截断内容冒充语义摘要。
- Agent 正常收敛目标写入稳定提示，硬上限收紧为 7 次模型调用和 10 次工具调用；证据 Skill 每个 Run 最多 3 次有效调用。每次模型请求重新估算文字、工具 Schema 和图像负载，超过 16k 输入边界时在调用供应商前终止；模型调用轨迹记录估算 Token、实际 Token、耗时、工具数和终止状态，不保存提示正文。
- 验证：阶段 4.4 后端定向测试通过；后端全量 579 项执行、0 失败、0 错误、18 项按条件跳过；前端 32 个单元测试文件、168 项通过，生产构建成功；论文助手确定性 Playwright 主链路 1/1 通过；`git diff --check` 通过。未运行真实 API 样本，90 秒总时限保持不变。

## 2026-09-13 阶段 4.4 真实 API 抽检（当前未提交）

- 使用当前编译产物和已配置的 Kimi API，在 5 页论文 184、15 页论文 193、54 页论文 203 上分别建立独立会话并提问“论文的核心创新点是什么？”。三篇首轮均完成，分别使用 4、4、3 次模型调用；最大估算输入分别为 15,326、15,901、10,099 tokens，未发生首轮超时。
- 184 同一会话的第二轮追问在第 6 次模型调用前被统一上下文硬上限拒绝，估算输入 16,673 tokens，返回 `CONTEXT_BUDGET_EXCEEDED`；证据检索本身已返回结果。这确认当前 16k 硬边界会在多轮累积时触发，需要后续优化上下文装配或减少当前 Run 的证据负载。
- 三篇首轮均通过 `submit_answer` 完成并返回证据；但 203 的一条候选证据从表格/公式中间开始，193 的部分证据仍按相邻版面块分段，属于证据完整性问题，不作为 Harness 测试失败判定。
- 本轮仅执行真实 API 抽检，未修改论文和 Skill 逻辑；数据库新增 3 个测试会话及其运行记录。后端进程使用本机授权环境运行，未提交 Git。

## 2026-09-13 阶段 4.4 Harness 精简补齐与真实 API 复测（当前未提交）

- 在现有 `ChatRequest` 转换边界增加按 Run 实例化的确定性薄 Harness；不替换 LangChain4j 自动工具循环。它只改发送给模型的临时消息，不修改 ChatMemory 或数据库中的原始运行/工具审计记录。
- 移除上下文装配器含义模糊的固定 2,500 Token 预留；初始快照记录真实初始消息估算。每次模型调用统计真实消息、工具 Schema、工具结果和图像输入，并使用统一 10,000 Token 软目标、16,000 Token 硬上限；超软目标先做当前 Run 整理，整理后仍超硬上限才终止。
- Skill 激活结果在对应 Skill 工具实际使用后折叠，画像在取得原文后折叠；证据结果按稳定来源对象 ID 和正文去重，保留不同对象、最新未消费结果、`submit_answer` 所需正文/来源 ID，并始终保持工具调用与结果的合法配对。协议恢复最多压缩一次普通文本为短状态。
- 跨轮只由摘要、最近完整用户—助手轮次、少量来源句柄和当前输入组成；新增下一 Run 隔离回归，上一 Run 的 Skill、画像、证据正文、Need 和工具 JSON 不进入模型上下文。未增加摘要模型调用。
- 新增 Harness 单测及画像→两次证据读取→`submit_answer` 的完整自动循环回放；阶段 4.4 定向测试通过。后端全量测试 592 项执行、0 失败、0 错误、18 项按条件跳过，`git diff --check` 通过。
- 真实 API 复测（模型请求 Trace 不保存提示正文，数据库原始工具记录仍保留）：184 首轮 `c75ea992-dd4b-449d-943d-3ba35bd4dcd1` 完成（3 次模型/3 次工具，初始估算 1,032，最大整理后估算 8,096，最大实际输入 6,403，累计实际输入 12,606）；同一追问连续 3 次中 `d5b366bb-f7cf-47f4-af34-99529f6f97ea`、`f43c331b-5dc4-4682-921a-7a36f71bbc84` 仅因未提交结构化答案失败，均未出现上下文超限，`1bad80cd-935b-437d-9bb9-79e87a9b1d7c` 完成（3/3，初始 1,630，最大整理后估算 7,696，最大实际输入 5,772，累计实际输入 13,074）。
- 193 的 `ca1e7c15-0800-4466-9b3a-7e0533061394` 完成（6 次模型/5 次工具，初始估算 1,884，最大整理后估算 10,916，最大实际输入 7,994，累计实际输入 27,858）；203 的 `1d00aa42-fe7e-421f-9f97-3c122904e586` 完成（4/3，初始 1,586，最大整理后估算 7,965，最大实际输入 5,749，累计实际输入 17,362）。四组成功请求的最大实际单次输入均低于 10,000，均未触发 `CONTEXT_BUDGET_EXCEEDED`。
- 剩余风险：184 的两次真实追问仍暴露既有的结构化终态协议恢复失败，但不是 Harness 上下文失败；真实样本较小，193 的最大整理后估算仍略高于软目标但实际输入低于 8,000。当前未提交、未 commit。

## 2026-09-13 阶段 4.4 终态提交协议收紧（当前未提交）

- 在现有 LangChain4j `ChatRequest` 转换边界将工具选择从 `AUTO` 收紧为 `REQUIRED`。这只约束模型必须用一个已注册工具表达下一步，不指定 Skill、证据读取工具或 `submit_answer`，业务选择和参数校验仍由模型与服务端共同完成，因此没有写死答案。
- 保留一次普通文本协议恢复；恢复请求也经过同一工具协议。若供应商仍未提交终态，执行器返回 `ANSWER_SUBMISSION_REQUIRED`，不会把未经结构化校验的普通文本写成最终回答；恢复提示和前一段普通文本在后续请求中只保留短状态。
- 失败 Run 现在也持久化已发生的模型/工具/Token 用量；模型轨迹增加 `responseKind` 和文本长度统计，不保存提示或响应正文，能够区分 `TOOL_CALL`、`TEXT`、`EMPTY`、`ERROR` 与未发送请求。
- 新增真实文本未提交、失败用量保留和 `REQUIRED` 请求断言。后端全量 593 项执行、0 失败、0 错误、18 项按条件跳过；`git diff --check` 通过。
- 由于本机安全策略要求单独授权，尚未用外部 Kimi API 重新发送论文 184、193、203 的内容；此前 Harness 复测数据仍保持原记录，待明确授权后再做最终真实 API 回归。

## 2026-09-13 阶段 4.4 终态提交协议真实 API 验收（当前未提交）

- 已获明确授权后使用当前配置的 Kimi API，通过本地后端完成论文 184 原问题、同一会话连续三次追问，以及论文 193、203 各一次普通问答；6/6 Run 均为 `COMPLETED`，没有 `CONTEXT_BUDGET_EXCEEDED` 或 `ANSWER_SUBMISSION_REQUIRED`。
- 184 四次 Run 的初始估算分别为 2,111、2,727、3,255、3,213；最大实际单次输入分别为 7,852、7,508、8,968、10,971，累计实际输入分别为 20,986、17,484、33,978、27,225。193 初始 2,746、最大实际 9,860、累计 24,323；203 初始 2,235、最大实际 7,430、累计 16,144。最大估算输入虽有个别请求超过 10,000，但均未超过 16,000 硬上限，实际输入均低于 12,000。
- 6/6 Run 的最终工具链均实际完成 `submit_answer`。其中 184 首轮和两次追问、193 首轮曾先提交未读取来源的答案并被服务端拒绝，模型随后继续激活/读取证据并成功提交；这验证了工具协议约束和服务端引用校验协同工作，没有把 `submit_answer` 写死在所有步骤。
- 6/6 初始上下文快照均未包含上一 Run 的工具角色、画像/证据工具名或原始工具 JSON 标记；模型 Trace 仅保留统计和响应类型，不保存提示或响应正文。

## 2026-09-13 阶段 4.4 提交与阶段 4.5 证据链审计

- 阶段 4.4 发布前审计确认代码、测试和真实 API 验收满足计划：后端全量 593 项执行、0 失败、0 错误、18 项按条件跳过；前端 32 个文件/168 项单测和生产构建通过；工作区差异检查通过。已提交 `adccddb`（`feat：完善论文助手上下文与运行稳定性`）。
- 阶段 4.5 只读审计确认图片误定位源于索引阶段的图题误分类，而不是 Agent 回答或操作阶段。论文 191 的 `p9-b0099`、`p9-b0111` 与论文 207 的 `p12-b0045`、`p12-b0114` 均是以 `Fig. X` 开头的讨论正文，却被提升为 `CAPTION/FIGURE`；真实图题通常具有明确的图号分隔符并位于独立视觉区域附近。
- 当前检索已经具备单次调用按来源 ID 合并、逐 Need 请求指纹、已读来源集合、游标、无进展和停止提示，但尚未表达内容完整性和事实覆盖，也未跨 Need 复用同义检索。阶段 4.5 将按“统一图题判定与失败夹具 → 图题/讨论关系 → 检索覆盖与停止信号 → 跨行/栏/页完整性 → 真实图片操作回归”实施，不先替换检索算法。
- 阶段 4.5 第一批已新增统一的结构化图题判定器，语义角色、语义 Span 和来源单元共同使用：图号后有明确标题分隔符时确认图题；无分隔符时必须有同页同栏、位于标题上方且距离受限的独立视觉区域，否则保持 `TEXT`。未通过扩充 `shows/displays/evaluates` 等动词黑名单实现；语义解析版本提升为 `semantic-v7`，来源 Schema 提升为 v7。
- 新增论文 191/207 的只读真实失败夹具：5 个已知讨论块均不再进入 `FIGURE`，7 个指定真实图题仍保留。图题/语义/来源定向测试 43 项通过；后端全量 593 项执行、0 失败、0 错误、19 项按条件跳过。
- 额外启用旧四论文 Span 诊断时先修正了“所有非空坐标轴/字符碎片也必须进入证据”的过时口径，改为所有可引用块恰好归属一次。当前本地数据库缺少论文 185 的版面产物，因此四论文诊断未完整结束；已完成的论文 184 暴露图 3 的第二行 `p4-b0049` 被归入后续正文 Span，导致图题正文不完整且片段重复，将作为 4.5 完整性批次固定样本。
- 阶段 4.5 第二批已为确认图题和讨论正文建立轻量双向关系：`FIGURE` 来源记录规范化 `figureNumber`、`captionOf` 和 `discussedBy`，正文仍保持 `TEXT` 并记录 `discussesFigure`；只有目录中确实存在对应确认图题时才建边。模型可见来源只暴露必要关系字段，图像操作 locator 规则未改变。合成目录/工具视图测试及论文 191、207 真实块关系测试均通过。
- 阶段 4.5 第三批在单次证据工具执行内按规范化查询、内容类型和页码范围复用等价底层搜索；不同 Need 仍分别获得来源归属和 `matchedSearches`，内容类型或页面范围不同的搜索不会误复用。Need 结果新增确定性的 `coverageState` 和返回内容 `contentComplete`，Run 进度新增 `newDistinctSources`，继续由模型判断语义充分性，不增加模型调用。
- 第三批定向回归 `PaperReadToolRegistryTest`、`AgentLoopServiceTest` 共 60 项执行、0 失败、0 错误。随后进入第四批完整性审计：优先把论文 184 的图题续行和现有跨行/栏/页夹具变成明确边界测试；只有可重复证明现有来源构造丢失或误拼正文时才修改解析/Span 逻辑。
- 第四批首先复核论文 184 的原始坐标，确认 `p4-b0049` 实际是同栏紧邻的独立短图题续行，后续正文为间距更大、高度显著更高的 `p4-b0050`，并非单一物理块内混排。统一图题判定器现可沿同页、同栏、左边界对齐、紧邻且行高相容的链路识别续行；语义角色、Span 和来源单元共享该结论。184 的两行标题成为同一 `CAPTION/FIGURE`，`p4-b0050` 保持独立 `TEXT`。
- 跨行完整性定向测试 56 项执行、0 失败、0 错误、2 项按条件跳过；论文 184、191、207 的只读真实图题/讨论/关系回归 1 项通过。既有跨栏仅合并明确断词延续、跨页仅合并同章节未结束句子的严格规则及测试继续保留，本批没有放宽这些边界。
- 阶段 4.5 当前后端全量测试 598 项执行、0 失败、0 错误、19 项按条件跳过，`git diff --check` 通过；前端未修改。后端已重启并加载当前代码。尝试创建论文 191、207 的外部模型图片问答验收时被运行环境安全审查拦截，原因是论文证据会发送到当前配置的外部模型并新增会话/Run 记录；尚未发生该两次外发或记录写入，待用户对此数据范围给出明确授权后继续。
- 已获得图片证据问答外发授权后检查运行态：论文 191、207 的对话请求先被现有就绪门禁拒绝，两个新建会话为空（ID 376、377），未生成 Run。当前就绪状态均为 `NOT_STARTED`，本地来源、检索和画像均未就绪；要继续真实图片问答必须先通过既有论文理解入口处理整篇论文。该步骤涉及完整论文外发及理解结果持久化，尚未执行，需单独确认数据范围。

## 2026-09-14 阶段 4.5 精确图号与真实图片问答收敛修正（当前未提交）

- 已在明确授权后完成论文 191、207 的整篇理解并写入本地记忆：191 的理解请求输入/输出为 25,950/1,734 tokens、15 页；207 为 25,858/2,399 tokens、16 页，二者均为 `READY`。这属于理解阶段数据，不计入 Agent 对话 Run 的单次/累计输入统计。
- 对论文 191（图 4）和 207（图 8）各执行一次真实图片问答。两次均未触发 `CONTEXT_BUDGET_EXCEEDED`，但在 7 次已完成模型调用后，第 8 次被现有 `maxModelCalls=7` 拦截，最终状态为 `RUN_TIMEOUT`；191 最大实际单次输入 7,672、累计 33,331，最大估算 10,663；207 最大实际单次输入 8,168、累计 34,623，最大估算 12,069。两次均未稳定到达 `submit_answer`，因此本轮不判定为真实 API 通过。
- 运行审计进一步确认：207 的一次提交曾把 `discussedBy` 关系中的未读取正文 ID 当作引用，服务端正确拒绝；这与同一批次附带其他图的视觉候选共同增加了无效往返。当前补丁在来源搜索中提高明确 `Fig./Figure N` 的规范图号优先级，在图号目标已命中时只返回该图并只渲染该图，同时在模型视图明确关系 ID 必须先重新读取后才可引用。
- 本批新增图号优先、目标图裁剪和关系 ID 使用提示的回归测试；相关定向测试通过，后端全量测试已达 598 项执行、0 失败、0 错误、19 项按条件跳过。为下一次真实复测创建了隔离空会话 378（191）和 379（207）；尚未向其中发送新的外部模型问答请求。

## 2026-09-14 阶段 4.5 通用性审计与多论文真实 API 复测（当前未提交）

- 静态审计确认生产代码和资源中没有论文 ID、固定页码或单篇论文分支；图号解析、图题/讨论关系、精确目标裁剪和视觉来源选择均按通用来源元数据工作。已知论文 ID 仅存在于真实回归夹具和验收记录，不参与运行时判断。
- 在明确授权后，对论文 184、191、193、203、207 使用独立或隔离会话完成真实 API 验收。184 首轮及同一会话中原失败追问连续 3 次、191 Fig.4 图片问答、207 Fig.8 图片问答、193/203 普通问答共 8 个 Run 全部 `COMPLETED`，每个 Run 的工具链都实际包含 `submit_answer`；没有 `CONTEXT_BUDGET_EXCEEDED`、`ANSWER_SUBMISSION_REQUIRED` 或模型/工具调用上限失败。
- 184 四次 Run 的首次模型请求估算（含工具 Schema）/最大整理后估算/最大实际输入/累计实际输入分别为：`2384/9958/7533/21769`、`3107/9932/7798/20091`、`3567/10110/7709/16716`、`4016/10101/8136/22701`；191 为 `2402/9491/6196/12604`，207 为 `2409/9497/5939/12331`，193 为 `2376/9607/6896/26379`，203 为 `2393/10157/7371/21482`。最大实际单次输入均低于 10,000，未提高 16,000 硬上限。
- 图片问答最终只绑定确认图题来源：191 为 `src_66e15b2f9f69a2454db14823`（第 10 页 Fig. 4），207 为 `src_454cf3dfefddd4a595d44c0b`（第 12 页 Fig. 8）；没有把 `discussedBy` 关系中的未读取正文 ID 当作引用，也没有把相邻图裁剪混入最终视觉来源。
- 本轮仅验证通用代码路径，未为任何论文添加补丁、数据特例或提高总超时/硬上限；数据库新增的理解、会话和 Run 记录属于真实 API 验收数据。`git diff --check` 通过，当前仍未 commit。

## 2026-09-14 submit_answer 结构化文本转义防护（当前未提交）

- 已确认论文 191 的乱码根因是模型工具参数中的嵌套 JSON/LaTeX 转义不稳定：异常请求在原始 `agent_tool_call.arguments_json` 中已出现 `\\u0005psilon`、`\\u0007psilon` 等内容，Jackson 解码后成为不可渲染控制字符；不是前端或论文特例转换。
- `submit_answer` 现在在最终回答落库前拒绝解码后的 C0/C1 控制字符（保留换行、回车、制表符），并通过现有工具错误回路要求模型重新提交；失败工具调用原始参数仍保留在运行审计中，不做 `U+0005 → e` 等字符或论文补丁。
- 已新增“异常提交 → 工具错误反馈 → 合法 LaTeX 提交”回归测试；定向测试 54 项、后端全量测试 607 项均通过（0 失败、0 错误，19 项按条件跳过）。论文 191 的同一图 4 问题在独立会话 393–402 中连续复测 10 次，10/10 完成；4 次首次提交分别因 U+0005（1 次）或 U+0008（3 次）被拒绝后自动重试成功，6 次直接成功。最终回答均无禁止的 C0/C1 控制字符，最大整理后估算输入 9,676、最大实际单次输入 7,535，未出现上下文超限，未增加摘要模型调用。

## 2026-09-14 阶段 4.5 多论文回复与证据质量快速验收（当前未提交）

- 结合原 PDF 的公式、图题、相邻正文和页面渲染，对文库中已有当前理解结果的 184、191、193、194、203、207 进行抽样复核；另检查了此前 197、201 的真实问答记录。没有发现新增论文专属分支、固定页码或针对单篇论文的运行时补丁。
- 新增真实 API 抽样：184 的公式（19）和图 2、193 的公式（9）与图 7、194 的图 4 均完成并绑定了对应的 FORMULA/FIGURE/TEXT 来源；回答中的公式含义、图题、参数和趋势与原文第 3/4/12/13 页一致。191、207 的图像问答沿用此前 10 次乱码复测及图 4/图 8 真实记录，图题来源、页面和高亮链路均已核对。
- 论文 194 的“公式（40）+图 4”联合问题未通过：Run `0cf3400f-ee9e-4e6a-9369-34adffd2d40a` 在两次提交完整公式时都没有绑定可靠公式文本或已实际读取的公式图像，服务端按约束拒绝后最终以 `RUN_TIMEOUT` 结束。图 4 单独问题 Run `60c9402c-fd2e-4df0-8c5a-3acdfe9bc3b2` 正常完成；因此这是公式证据读取/提交协议的通用剩余风险，不是图片误分类或论文专属问题。
- 当前 197、201 的 `paper_memory` 仍只有 `semantic-v6` 产物，按当前 `semantic-v7` 就绪校验显示 `NOT_STARTED`；此前记录可作为旧版本回归参考，但不能宣称已完成当前版本的 4.5 复验。
- 结论：本轮图片证据质量抽样通过，但阶段 4.5 尚不能按“无误”收官；至少需要后续决定是否补齐公式视觉读取/提交协议，以及是否重建 197、201 的当前版本来源后再做最终关闭判断。本轮未新增代码修改、未提交 Git；现有 607 项后端测试报告仍为 0 失败、0 错误、19 项跳过，`git diff --check` 待最终复核。

## 2026-09-14 公式来源提交提示最小修正（当前未提交）

- 未放宽公式证据校验，也未增加 Agent 层级或专用补丁；仅在完整公式因引用错误被拒绝时，把本轮已经实际读取的 FORMULA 来源 ID 明确返回给模型，并要求直接修正 `submit_answer`，不要重新检索已读来源。工具说明同步明确：视觉公式必须绑定对应 FORMULA 来源，邻近 TEXT 不能替代。
- 定向测试 `AgentLoopServiceTest#visuallyReadFormulaMaySupportAnExactDisplayExpression` 通过，覆盖“错误绑定正文来源 → 收到已读公式 ID → 使用公式来源重新提交”的路径。
- 论文 194 同一“公式（40）+图 4”问题真实复测 Run `e9d163e8-396b-4a60-8cbd-bd13e4b660ce` 已 `COMPLETED`：3 次模型调用、3 次工具调用，工具链为 `activate_skill → retrieve_paper_evidence → submit_answer`，公式块绑定 `eq:40:src_6b59edab65f63e9716a984fc`，没有重复检索或提交纠错。本次累计实际输入 15,142 tokens，不代表单次请求输入。
- 该通用公式提交阻塞已消除；阶段 4.5 的图片证据与当前版本公式主链路可按基本能力目标收敛。论文 197、201 仍是旧 `semantic-v6` 数据，升级到当前版本属于数据刷新，不影响本次代码结论。

## 2026-09-14 阶段 4.6 回答与页面操作复合终态（当前未提交）

- `paper_action` 增加可选的结构化 `answer`，直接复用 `submit_answer` 的 `groundingMode`、答案块、来源和公式校验；纯回答仍由 `submit_answer` 结束，纯操作参数与回执流程保持不变。复合请求要求回答实际引用每个操作目标来源。
- Run 等待客户端操作时持久化已校验回答、引用和证据；操作成功后合并操作完成状态，失败或用户取消时也保留回答，不再被单独的操作状态覆盖。前端允许显示“回答有效、页面操作失败”的复合结果。
- 定向测试：`AgentLoopServiceTest`、`AgentActionReceiptServiceTest`、`AgentRunCancellationServiceTest` 共 47 项通过；前端 `usePaperAgent.spec.js` 12 项通过。
- 论文 194 真实复合请求“解释公式（40）并高亮对应原文”Run `4ec0da07-bd94-40c3-9d35-962aafb88059` 已 `COMPLETED`：`activate_skill → retrieve_paper_evidence → activate_skill → paper_action`，回答和高亮共同绑定 `eq:40:src_6b59edab65f63e9716a984fc`，客户端创建高亮记录 147，最终以一条带 `ground-evidence-v2` 证据的 `CHAT` 保存并附加“已完成高亮”。

## 2026-09-14 阶段 5 API 基础能力复核（当前未提交）

- 现有能力测试已使用极小固定文字、图片和工具回执验证文字通信、工具调用、工具结果后的继续回答、结构化输出和图片输入；原生 PDF 单独作为可选能力，不读取论文或创建业务任务。本阶段无需修改代码。
- 当前真实配置 `kimi-for-coding` 测试结果为 `VERIFIED`：chat、toolCalling、continuousTools、toolImageContinuation、structured、image 均为 `true`，pdf 为 `false`，有效期至 2026-09-21；PDF 不支持时按既有设计使用结构化文本和局部图片。
- 使用草稿配置传入明显无效模型名后，当前供应商仍返回能力成功，说明该兼容端点忽略或改写 model 参数，项目无法据此可靠制造“模型不存在”响应；测试前后保存模型仍为 `kimi-for-coding`。论文 15、研究会话 46、Agent turn/run 82 的计数保持不变，仅新增一条按配置签名隔离的能力探测记录。
- `AiCapabilityServiceTest` 与 `SettingsControllerContractTest` 通过；能力探测不会保存草稿设置，也不承担论文解析或理解质量验收。
